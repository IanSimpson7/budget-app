---
phase: 02-income-model-with-two-floors
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/domains/income/parser/adapter.types.ts
  - src/domains/income/parser/checkingAdapter.ts
  - src/domains/income/parser/parseStatement.ts
  - src/domains/income/classify.ts
  - src/domains/income/income.atoms.ts
  - src/domains/income/parser/parseStatement.test.ts
  - src/domains/income/classify.test.ts
  - src/domains/income/income.atoms.test.ts
autonomous: true
requirements: [INC-03, INC-04, INC-06, INC-08, EDGE-01, EDGE-05]
must_haves:
  truths:
    - "The parser splits the real May-2026 statement into the correct candidate rows (block-based, signed amounts)"
    - "Credit/debit is determined deterministically from amount sign with a balance-delta fallback"
    - "Only PAYROLL / known-source credits default-checked; asset sales, cashback, gifts, and debits default-unchecked"
    - "A 3rd payroll check in a calendar month auto-classifies as surplus (date-keyed, local midnight)"
    - "projectedMonth = mtdPayroll + max(0, 2 - landedPayrollCount) × estimatePerCheck"
    - "Backfill fires on payroll-only projection < defended line; gift income does NOT suppress it"
    - "Solvency/surplus computed against the passive floor, never the average or defended line"
  artifacts:
    - path: "src/domains/income/parser/parseStatement.ts"
      provides: "pure (text, adapter) → CandidateRow[]"
      min_lines: 20
    - path: "src/domains/income/classify.ts"
      provides: "default taxability (D-08) + surplus classification (D-12) + conservative auto-check (D-05)"
    - path: "src/domains/income/income.atoms.ts"
      provides: "reactive source atom + full derived chain"
      contains: "atomWithObservable"
  key_links:
    - from: "src/domains/income/income.atoms.ts incomeChecksAtom"
      to: "storage.observeIncomeChecks"
      via: "atomWithObservable(() => storage.observeIncomeChecks(), { initialValue: [] })"
      pattern: "atomWithObservable"
    - from: "src/domains/income/income.atoms.ts backfillActiveAtom"
      to: "projectedMonthPayrollAtom < defendedLine"
      via: "payroll-only comparison (D-09)"
      pattern: "backfillActiveAtom"
---

<objective>
Build the phase's only genuinely-custom logic as pure, unit-tested functions, then express every dashboard number as a read-only derived Jotai atom over a single reactive source atom. Three surfaces: (1) the block-based statement parser (D-02..D-04) with the `StatementAdapter` Phase-3 seam (D-01) and the checking adapter; (2) classification — default taxability (D-08), surplus rule (D-12), conservative auto-check (D-05); (3) the income atom chain — `atomWithObservable + liveQuery` source plus MTD/projected/surplus/backfill derived atoms (D-09/D-11, FOUND-06).

Purpose: This is the correctness core of the phase. Parser + classify are pure and fully testable; the gold May-2026 fixture is the acceptance gate. The reactive source atom de-risks the React-19 `atomWithObservable` path via `initialValue: []` (RESEARCH Pitfall 1). The two-floor split (payroll-only defended line) is a correctness invariant — gift income must never suppress the backfill alert (RESEARCH Pitfall 4).
Output: parser, adapter, classify, atoms, and their tests (gold fixture goes green here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-income-model-with-two-floors/02-RESEARCH.md
@.planning/phases/02-income-model-with-two-floors/02-PATTERNS.md

<interfaces>
<!-- Contracts from 02-01 the executor builds against. Do not re-explore. -->
From src/storage/schema.ts (created in 02-01):
- `type Category = 'payroll' | 'gift' | 'other'`
- `type IncomeCheck = Readonly<{ id?: number; date: string; netAmount: number; source: string; note: string; category: Category; taxable: boolean; surplusOverride?: boolean }>`
- `type KnownSource = Readonly<{ source: string; category: Category; taxable: boolean }>`

From src/domains/income/income.types.ts (created in 02-01):
- re-exports IncomeCheck/Category/KnownSource; local `type CandidateRow = { date?; netAmount?; source?; note?; balanceAfter?: number|null; isCredit: boolean; checked: boolean; category: Category; taxable: boolean; raw: string }`

From src/storage/storage.ts (created in 02-01):
- `observeIncomeChecks(): Observable<IncomeCheck[]>` (db-free seam — atom imports storage, NOT db)
- `getEstimatePerCheck(): Promise<number>` (default 0)

From src/domains/settings/settings.atoms.ts (existing):
- `floorsLoadAtom` (async atom → Floors). Reuse for passive/defended floor values; do NOT copy its refreshCounterAtom pattern for the income chain (liveQuery re-emits automatically).

Regex constants (RESEARCH Pattern 3, lines 286-289):
- `DATE_LINE = /^\d{2}\/\d{2}\/\d{4}/` ; `HEADER = /^date\s+description\s+amount\s+balance/i` ; `TRAILING_NUMS = /(-?[\d,]+\.\d{2})\s+(-?[\d,]+\.\d{2})\s*$/`
</interfaces>
</context>

<feature>
  <name>Pure statement parser + classification + reactive income atom chain</name>
  <files>parser/adapter.types.ts, parser/checkingAdapter.ts, parser/parseStatement.ts, classify.ts, income.atoms.ts (+ tests)</files>
  <behavior>
    Parser: parseStatement(fixtureText, checkingAdapter) yields 6 candidate rows from the gold fixture; the two GLI EAST LANSING rows have netAmount 1127.51 and 1296.59 and isCredit true; VANGUARD SELL / cashback / both VENMO rows are credits but isCredit-by-sign; the header row and blank lines are skipped; multi-line TYPE/CO/ID metadata is folded into the block's raw note.
    Classify: defaultTaxable('payroll') === true, ('gift') === false, ('other') === true. Conservative auto-check: a PAYROLL credit OR a known-source credit defaults checked; all other credits and all debits default unchecked. Surplus: given 3 payroll checks in one local calendar month sorted by date, the 3rd is flagged surplus and excluded from the baseline; surplusOverride forces the flag. Month classification parses `iso + 'T00:00:00'` (LOCAL midnight) so a 2026-06-01 check is June, not May.
    Atoms: with the May fixture committed (2 payroll, $2,424.10, no gift), mtdTotal === mtdPayroll === 2424.10, landedPayrollCount === 2, projectedMonth === 2424.10, backfillActive === true (2424.10 < 3000), surplus === 0. With a $700 gift added, mtdTotal === 3124.10 but backfillActive STILL true (payroll-only). With only 1 payroll check landed and estimatePerCheck 1296.59, projectedMonth === landed + 1296.59.
  </behavior>
  <implementation>
    RED→GREEN→REFACTOR. Write parser + classify + atom tests first (importing the gold fixture from 02-01), confirm RED, then implement.
    `adapter.types.ts`: `export interface StatementAdapter { readonly statementType: 'checking' | 'creditcard'; extractFields(block: string[]): Partial<CandidateRow>; isCredit(row: Partial<CandidateRow>, prevBalance: number | null): boolean }` — the D-01 Phase-3 seam.
    `checkingAdapter.ts`: implements StatementAdapter for Ian's checking format. extractFields: parse MM/DD/YYYY → ISO `date`; TRAILING_NUMS → `(netAmount, balanceAfter)` with commas stripped + sign; `source` ← `CO:` value if present else first description line after "ACH Deposit"; `note` ← block.join('\n'). isCredit: primary = sign of netAmount (>0 credit); fallback when ambiguous = sign of `balanceAfter - prevBalance`. NO `eval`, NO dynamic regex from input (Security V5). statementType 'checking'.
    `parseStatement.ts`: pure `(text, adapter) → CandidateRow[]` per RESEARCH lines 297-313 — split lines, skip blank/header, start a block on DATE_LINE, accumulate metadata, map each block through adapter.extractFields + adapter.isCredit threading prevBalance. Cap input length (e.g. reject > 1,000,000 chars) to bound DoS (T-02-D); anchored regex avoids catastrophic backtracking.
    `classify.ts`: pure exports `defaultTaxable(category): boolean` (D-08), `isInLocalMonth(iso, ref?): boolean` (local-midnight rule), `classifySurplus(payrollChecks: IncomeCheck[]): Set<id>` returning the surplus ids (3rd+ by date order, honoring surplusOverride) (D-12), `defaultChecked(row, knownSources): boolean` (D-05 conservative).
    `income.atoms.ts`: source `incomeChecksAtom = atomWithObservable<IncomeCheck[]>(() => storage.observeIncomeChecks(), { initialValue: [] })` (import atomWithObservable from jotai/utils; import storage, NEVER db). Derived chain: `currentMonthChecksAtom` (filter isInLocalMonth), `mtdTotalAtom`, `mtdPayrollAtom`, `baselinePayrollAtom` (payroll, non-surplus, sorted by date), `landedPayrollCountAtom` (min(baseline.length, 2)), `estimatePerCheckAtom` (settings via storage.getEstimatePerCheck; fallback most-recent payroll amount per D-11), `projectedMonthPayrollAtom` (= mtdPayroll + max(0, 2 - landed) × estimate, D-11), `projectedTotalAtom`, `surplusAtom` (= projectedTotal - passive floor, never average/defended — INC-03), `backfillActiveAtom` (= projectedMonthPayroll < defended floor — D-09 payroll-only). Defended/passive read from `floorsLoadAtom`. Never persist any derived value (FOUND-06).
  </implementation>
</feature>

<verification>
- `npx vitest run src/domains/income/parser/parseStatement.test.ts src/domains/income/classify.test.ts src/domains/income/income.atoms.test.ts` exits 0.
- Gold fixture: parser yields 6 rows, only 2 default-checked; backfillActive true for the May-2026 payroll-only case; surplus 0.
- Gift-doesn't-suppress test: adding a gift row keeps backfillActive true.
- `npx tsc -b --noEmit` exits 0.
- Grep gate (storage boundary): `import { db }` MUST NOT appear in `src/domains/income/income.atoms.ts` — verify the atom imports `storage` only.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted free text → parser | Untrusted, possibly malformed/adversarial text enters the pure parser |
| (no network / no credentials) | Local-only, C2/C3 — N/A categories below |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-D | Denial of Service | parseStatement | mitigate | Pure + linear scan; cap input length (reject > 1,000,000 chars); regexes are anchored (DATE_LINE `^`, TRAILING_NUMS `$`) with no nested quantifiers → no catastrophic backtracking. Empty-result path returns `[]` (no crash). |
| T-02-V | Input Validation | checkingAdapter | mitigate | No `eval`, no `new RegExp` from input; numeric parse strips commas then `Number()` with NaN guard → malformed amounts produce undefined, not a thrown exception. |
| T-02-04 | Integrity | derived atoms | mitigate | All dashboard numbers are read-only derived atoms; nothing computed is written back (FOUND-06). Verified by the atom tests asserting recompute, not persistence. |
| T-02-05 | Tampering (correctness) | backfillActiveAtom | mitigate | Defended-line comparison uses payroll-only projection (D-09); the gift-doesn't-suppress test guards the invariant. |
| T-02-S/R/I | Spoofing/Repudiation/Info-Disclosure | — | N/A | Single-user local app, no auth, no credentials, no multi-user audit (C2). |
</threat_model>

<success_criteria>
- Parser + adapter + classify are pure, dependency-free, and pass against the gold fixture.
- The full derived-atom chain computes MTD/projected/surplus/backfill correctly; backfill is payroll-only.
- Source atom uses `atomWithObservable(initialValue: [])` and imports `storage`, never `db`.
- No derived value persisted (FOUND-06).
</success_criteria>

<output>
After completion, create `.planning/phases/02-income-model-with-two-floors/02-02-SUMMARY.md`.
</output>
