---
phase: 02-income-model-with-two-floors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/migrations.ts
  - src/storage/storage.ts
  - src/domains/income/income.types.ts
  - src/test/storage.income.test.ts
  - src/test/migrations.test.ts
  - src/test/storage.test.ts
autonomous: true
requirements: [INC-01, INC-02]
must_haves:
  truths:
    - "A v1-format JSON backup still imports cleanly into a v2 app (Ian's live backups survive)"
    - "An IncomeCheck can be persisted and read back through the storage abstraction"
    - "The reactive source observable emits the current incomeChecks rows and re-emits on write"
    - "The storage surface still exposes no credential / money-move / floor-lowering method (C2/C3)"
  artifacts:
    - path: "src/storage/schema.ts"
      provides: "IncomeCheck + KnownSource types; CURRENT_SCHEMA_VERSION = 2"
      contains: "CURRENT_SCHEMA_VERSION = 2"
    - path: "src/storage/migrations.ts"
      provides: "migrate_1_to_2 registered at MIGRATIONS[1]"
      contains: "migrate_1_to_2"
    - path: "src/storage/storage.ts"
      provides: "income CRUD + known-source get/save + observeIncomeChecks"
      exports: ["addIncomeCheck", "addIncomeChecks", "listIncomeChecks", "updateIncomeCheck", "deleteIncomeCheck", "getKnownSources", "saveKnownSources", "getEstimatePerCheck", "saveEstimatePerCheck", "observeIncomeChecks"]
  key_links:
    - from: "src/storage/storage.ts observeIncomeChecks"
      to: "db.incomeChecks via liveQuery"
      via: "liveQuery(() => db.incomeChecks.toArray())"
      pattern: "liveQuery"
    - from: "src/storage/storage.ts importAll ladder"
      to: "migrate_1_to_2"
      via: "MIGRATIONS[1]"
      pattern: "MIGRATIONS\\[1\\]"
---

<objective>
Evolve the persistence layer from schema v1 to v2: add the `IncomeCheck` type and known-source/estimate settings, register the `migrate_1_to_2` pure migration so existing v1 backups remain importable, add income CRUD + known-source persistence + a db-free reactive observable to the storage abstraction, and lay the Wave 0 test scaffolds the rest of the phase verifies against.

Purpose: Everything reactive and persistent in Phase 2 stands on this layer. The migration registration is what keeps Ian's live v1 IndexedDB and JSON backups importable (RESEARCH Pattern 4, Assumption A4). The `observeIncomeChecks` seam keeps domain atoms db-free, preserving the C2/C3 structural boundary (RESEARCH Pitfall 5, Open Question 1).
Output: Extended `src/storage/*`, a new `src/domains/income/income.types.ts`, and Wave 0 test files (some intentionally red until later plans land their subjects, but the storage + migration tests go green in this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-income-model-with-two-floors/02-RESEARCH.md
@.planning/phases/02-income-model-with-two-floors/02-PATTERNS.md
@.planning/phases/01-foundation-storage-deploy/SKELETON.md

<interfaces>
<!-- Current storage contract the executor extends. Do not explore — use these. -->
From src/storage/schema.ts:
- `export const CURRENT_SCHEMA_VERSION = 1 as const`  → bump to 2
- `export type Floors = Readonly<{ passive: number; defended: number; foodSeed: number }>`
- `export type SettingsRow = { key: string; value: unknown }`
- `export type SchemaV1Data = { incomeChecks: unknown[]; expenseItems: unknown[]; sinkingFunds: unknown[]; accounts: unknown[]; settings: Record<string, unknown> }`
- `export type ExportEnvelope` / `export class ImportError` (codes: VERSION_TOO_NEW | PARSE_ERROR | INVALID_ENVELOPE)

From src/storage/migrations.ts:
- `export type MigrationFn = (data: SchemaV1Data) => SchemaV1Data`
- `export const MIGRATIONS: Record<number, MigrationFn> = {}`  (currently empty — import ladder in storage.ts lines 123-129 walks it)

From src/storage/storage.ts:
- `getFloors()/saveFloors(floors)` settings-singleton pattern (lines 19-28, key constant `FLOORS_KEY = 'floors'`)
- `importAll(file)` runs the ladder (lines 95-132); `replaceAll(data)` clears + re-seeds settings only (lines 134-153) — income rows currently dropped ("empty in v1")

From src/storage/db.ts:
- `incomeChecks!: Table<unknown, number>` ; `this.version(1).stores({ incomeChecks: '++id, date, source', ... })` ; `this.on('versionchange', () => this.close())` (keep)

From src/domains/settings/settings.types.ts (the exact re-export style to copy):
- `export { DEFAULT_FLOORS, type Floors } from '../../storage/schema'`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump schema to v2 + add IncomeCheck/KnownSource types + register migrate_1_to_2</name>
  <read_first>
    - src/storage/schema.ts (full — extend in place)
    - src/storage/migrations.ts (full — register the migration; obey the CONTRACT block lines 6-15)
    - src/storage/db.ts (full — add .version(2))
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 43-108 (schema/db/migrations analogs + exact CONTRACT)
    - .planning/phases/02-income-model-with-two-floors/02-RESEARCH.md lines 318-349 (Pattern 4 migration contract)
  </read_first>
  <behavior>
    - migrate_1_to_2 maps a v1 envelope-data object to v2 by seeding `settings.knownSources` to `[]` when absent; income rows untouched (v1 had none).
    - importAll of a `schemaVersion: 1` envelope succeeds (MIGRATIONS[1] now exists) and replaceAll runs without throwing INVALID_ENVELOPE.
    - importAll of a `schemaVersion: 3` envelope still throws ImportError('VERSION_TOO_NEW').
    - CURRENT_SCHEMA_VERSION === 2.
  </behavior>
  <action>
    In `schema.ts`: bump `CURRENT_SCHEMA_VERSION = 2 as const`. Add `export type Category = 'payroll' | 'gift' | 'other'` and `export type IncomeCheck = Readonly<{ id?: number; date: string; netAmount: number; source: string; note: string; category: Category; taxable: boolean; surplusOverride?: boolean }>` (D-08 fields + D-12 `surplusOverride`; `date` is ISO yyyy-mm-dd classified by LOCAL calendar month). Add `export type KnownSource = Readonly<{ source: string; category: Category; taxable: boolean }>` (D-06). Keep the header comment contract (lines 1-4) and add NO Dexie import. Keep `SchemaV1Data` loose-typed; add `knownSources?: KnownSource[]` and `estimatePerCheck?: number` as optional members of the `settings` record shape only by leaving `settings: Record<string, unknown>` as-is (settings is iterated by key — no shape change needed).
    In `migrations.ts`: write pure `export function migrate_1_to_2(data: SchemaV1Data): SchemaV1Data` returning `{ ...data, settings: { ...data.settings, knownSources: (data.settings.knownSources as KnownSource[] | undefined) ?? [] } }`. Register `export const MIGRATIONS: Record<number, MigrationFn> = { 1: migrate_1_to_2 }`. NO Dexie import.
    In `db.ts`: chain `this.version(2).stores({ incomeChecks: '++id, date, source', expenseItems: '++id, name, category, protected, cadence', sinkingFunds: '++id, name, payoutDate', accounts: '++id, type', settings: '&key' }).upgrade(async () => {})` after `.version(1)` — indexes unchanged (A2: field-only addition needs no new index) but the version MUST advance so Dexie and the import ladder stay aligned. Optionally retype `incomeChecks!: Table<IncomeCheck, number>`. KEEP the `versionchange` handler.
  </action>
  <verify>
    <automated>npx vitest run src/test/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/test/migrations.test.ts` exits 0.
    - `schema.ts` contains the literal `CURRENT_SCHEMA_VERSION = 2`.
    - `migrations.ts` contains `migrate_1_to_2` and `MIGRATIONS` is non-empty (`{ 1: ... }`).
    - A test importing a verbatim `{ schemaVersion: 1, data: {...} }` envelope via `storage.importAll` resolves (no throw); a `schemaVersion: 3` envelope throws `ImportError('VERSION_TOO_NEW')`.
  </acceptance_criteria>
  <done>Schema is v2, migrate_1_to_2 registered, v1 backups import cleanly, version-too-new still refused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add income CRUD + known-source/estimate settings + observeIncomeChecks to storage.ts; round-trip income through import/export</name>
  <read_first>
    - src/storage/storage.ts (full — extend; copy getFloors/saveFloors pattern lines 19-28, extend collectSchemaV1Data lines 30-49 and replaceAll lines 134-153)
    - src/domains/settings/settings.types.ts (full — exact re-export style to copy for income.types.ts)
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 112-196 (storage.ts + income.types.ts analogs)
    - .planning/phases/02-income-model-with-two-floors/02-RESEARCH.md lines 214-241 (Pattern 1 observeIncomeChecks seam)
  </read_first>
  <behavior>
    - addIncomeCheck persists a check; listIncomeChecks returns it; updateIncomeCheck patches it; deleteIncomeCheck removes it.
    - addIncomeChecks persists a batch in parallel (Promise.all), returns the count or ids.
    - getKnownSources returns [] when unset; saveKnownSources then getKnownSources round-trips the list; same for getEstimatePerCheck/saveEstimatePerCheck (0 default).
    - observeIncomeChecks() returns an object with a `.subscribe` method (Dexie Observable); subscribing yields the current rows and re-emits after addIncomeCheck.
    - exportAll then importAll into a fresh DB reproduces persisted income rows AND knownSources (v2 round-trip).
  </behavior>
  <action>
    In `storage.ts`: add `addIncomeCheck(check: Omit<IncomeCheck,'id'>): Promise<number>`, `addIncomeChecks(checks: Omit<IncomeCheck,'id'>[]): Promise<number[]>` (use `Promise.all` over `db.incomeChecks.add` — parallel-by-default), `listIncomeChecks(): Promise<IncomeCheck[]>` (returns `db.incomeChecks.toArray()`; optional `range?` left for later), `updateIncomeCheck(id: number, patch: Partial<IncomeCheck>): Promise<void>` (`db.incomeChecks.update`), `deleteIncomeCheck(id: number): Promise<void>`. Add settings-singleton helpers following the `FLOORS_KEY` pattern: constants `KNOWN_SOURCES_KEY = 'knownSources'` and `ESTIMATE_KEY = 'estimatePerCheck'`, with `getKnownSources()/saveKnownSources(list)` and `getEstimatePerCheck()` (default 0) `/saveEstimatePerCheck(n)`. Add `export function observeIncomeChecks(): Observable<IncomeCheck[]>` returning `liveQuery(() => db.incomeChecks.toArray() as Promise<IncomeCheck[]>)` — import `liveQuery` and `type Observable` from `dexie`. Extend `collectSchemaV1Data` to populate `incomeChecks` from `db.incomeChecks.toArray()` (no longer hardcode `[]`). Extend `replaceAll` to re-seed `db.incomeChecks` from `data.incomeChecks` so a v2 backup round-trips income rows. Add NO `saveCredentials`/`setApiKey`/`moveMoney`/`executeSweep`/`decreaseFoodFloor` — the absence IS the C2/C3 constraint (keep the header comment intent lines 1-6).
    Create `src/domains/income/income.types.ts` copying `settings.types.ts` verbatim style: `export { type IncomeCheck, type Category, type KnownSource } from '../../storage/schema'` and locally declare `export type CandidateRow = { date?: string; netAmount?: number; source?: string; note?: string; balanceAfter?: number | null; isCredit: boolean; checked: boolean; category: Category; taxable: boolean; raw: string }` (parse-time shape, never persisted).
  </action>
  <verify>
    <automated>npx vitest run src/test/storage.income.test.ts src/test/storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/test/storage.income.test.ts src/test/storage.test.ts` exits 0.
    - `storage.ts` contains `observeIncomeChecks`, `addIncomeCheck`, `addIncomeChecks`, `getKnownSources`, `getEstimatePerCheck`.
    - The extended absence-proof test in `storage.test.ts` asserts `storage.moveMoney`/`storage.saveCredentials`/`storage.executeSweep`/`storage.decreaseFoodFloor` are all `undefined`.
    - A round-trip test: addIncomeCheck → exportAll → wipe DB → importAll → listIncomeChecks returns the same row.
    - `npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>Income CRUD + known-source/estimate persistence + db-free reactive observable exist; income survives an export/import round-trip; C2/C3 absence still proven.</done>
</task>

<task type="auto">
  <name>Task 3: Author Wave 0 test scaffolds + the May-2026 gold fixture</name>
  <read_first>
    - src/test/storage.test.ts (full — copy beforeEach Dexie.delete pattern lines 1-16; extend absence block lines 30-46)
    - .planning/phases/02-income-model-with-two-floors/02-RESEARCH.md lines 421-432 (gold test case), lines 549-557 (Wave 0 gaps)
    - .planning/phases/02-income-model-with-two-floors/02-CONTEXT.md lines 110-113 (real figures + non-income credits)
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 378-388 (test scaffolding pattern)
  </read_first>
  <action>
    Create the Wave 0 test files and fixture so plans 02-05 have targets to turn green. (a) `src/domains/income/parser/__fixtures__/checking-may-2026.txt` — a block-based, tab-delimited checking sample reproducing the documented format: header row `Date<TAB>Description<TAB>Amount<TAB>Balance`; two PAYROLL credits `GLI EAST LANSING` $1,127.51 (05/01/2026) and $1,296.59 (05/15/2026) with `TYPE: PAYROLL`/`CO:`/`ID:` metadata lines; the non-income credits `ACH Deposit VANGUARD SELL` $3,000.00, `Cashback Redeemed from L30` $223.97, `ACH Deposit VENMO CASHOUT` $600.00 and $350.00; signed trailing `(amount, balance)` pairs. NOTE in a top comment of the spawning SUMMARY that this fixture is constructed from the documented figures (the verbatim raw paste was NOT in 02-DISCUSSION-LOG.md, Assumption A3) and must be reconciled against Ian's real paste during UAT. (b) Create placeholder test files with at least one real assertion each so they fail meaningfully until their subject lands: `src/domains/income/parser/parseStatement.test.ts`, `src/domains/income/classify.test.ts`, `src/domains/income/income.atoms.test.ts`, `src/components/IncomeBar.test.tsx`, `src/domains/income/PasteParseFlow.test.tsx`. Each may `it.todo(...)` for not-yet-built behaviors but MUST include the gold-fixture import path so the dependency is wired. (c) Extend `src/test/storage.test.ts` absence block with `executeSweep`/`decreaseFoodFloor` if not already added by Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/test/storage.test.ts && test -f src/domains/income/parser/__fixtures__/checking-may-2026.txt && echo FIXTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The fixture file exists and contains the strings `GLI EAST LANSING`, `1,127.51`, `1,296.59`, `VANGUARD SELL`, `VENMO`, and `TYPE: PAYROLL`.
    - `src/test/storage.test.ts` passes and asserts absence of `moveMoney`/`executeSweep`/`decreaseFoodFloor`.
    - All five placeholder test files exist (later plans replace `it.todo` with real tests).
  </acceptance_criteria>
  <done>Gold fixture + Wave 0 scaffolds exist; storage absence proof passes; downstream plans have concrete test targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported JSON file → storage | Untrusted backup file crosses into persistence on importAll |
| (no network boundary) | Local-only app, no backend, no credentials (C2/C3) — see N/A below |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | importAll envelope | mitigate | Existing shape validation (isPlainObject + numeric schemaVersion + isPlainObject data) gates persistence; migrate_1_to_2 must not widen the accepted surface. v1→v2 round-trip test proves only well-formed data persists. |
| T-02-02 | Tampering / spec violation | storage.ts public surface | mitigate | C2/C3 enforced by ABSENCE of credential/money-move methods; the storage.test.ts absence proof is extended (Task 2/3) so a future money-moving method addition fails the test. |
| T-02-03 | Integrity | derived values | mitigate | No derived financial value persisted by this layer; storage stores only raw IncomeCheck rows + raw settings. (FOUND-06 enforced downstream in 02-02.) |
| T-02-S | Spoofing | — | N/A | No auth, no accounts, single-user local app. |
| T-02-R | Repudiation | — | N/A | No multi-user audit requirement; single user. |
| T-02-I | Information Disclosure | — | N/A | No credentials/secrets stored (C2); IndexedDB is per-device by design. |
| T-02-D | Denial of Service | — | N/A | Parser DoS handled in 02-02 (parser plan); storage CRUD is bounded by user input volume. |
</threat_model>

<verification>
- `npx vitest run` (storage + migration tests green; parser/atom/component placeholders may be `it.todo`).
- `npx tsc -b --noEmit` exits 0.
- v1 backup import + v2 income round-trip both proven by tests.
</verification>

<success_criteria>
- Schema v2 live; migrate_1_to_2 registered; v1 backups importable (keeps Ian's live backups working).
- Income CRUD + known-source/estimate persistence + db-free `observeIncomeChecks` on the storage abstraction.
- C2/C3 absence proof extended and green.
- Gold fixture + Wave 0 scaffolds in place for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-income-model-with-two-floors/02-01-SUMMARY.md`.
</output>
