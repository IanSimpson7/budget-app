---
phase: 02-income-model-with-two-floors
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - src/components/IncomeBar.tsx
  - src/components/MetricCard.tsx
  - src/domains/income/BackfillAlertCard.tsx
  - src/pages/DashboardPage.tsx
  - src/App.tsx
  - src/components/AppShell.tsx
  - src/components/IncomeBar.test.tsx
autonomous: true
requirements: [INC-05, INC-06, EDGE-01]
must_haves:
  truths:
    - "The dashboard renders month-to-date income with a solvency band at the passive floor and a marker tick at the defended line"
    - "When payroll projection < defended line, the surplus card is replaced by the backfill alert"
    - "The backfill alert shows the projected payroll amount and 'below $3,000 — add sessions to defend'"
    - "An empty month renders the empty state, not a crash"
    - "/dashboard is the index route and a nav link is live"
  artifacts:
    - path: "src/components/IncomeBar.tsx"
      provides: "role=meter horizontal bar with floor band + defended tick + ghost projection"
      contains: "role=\"meter\""
    - path: "src/domains/income/BackfillAlertCard.tsx"
      provides: "role=alert backfill card (D-14 copy)"
      contains: "role=\"alert\""
    - path: "src/pages/DashboardPage.tsx"
      provides: "IncomeBar + 3 metric cards over derived atoms"
  key_links:
    - from: "src/pages/DashboardPage.tsx"
      to: "backfillActiveAtom / surplusAtom / projectedMonthPayrollAtom"
      via: "useAtomValue(atom, { delay: 0 })"
      pattern: "backfillActiveAtom"
    - from: "src/App.tsx"
      to: "DashboardPage"
      via: "Route path=/dashboard + index redirect"
      pattern: "path=\"/dashboard\""
---

<objective>
Deliver the dashboard vertical slice: a phone-readable `/dashboard` showing month-to-date income against BOTH floors (solvency band at the passive floor, marker tick at the $3,000 defended line, ghost projection segment) plus three `font-mono` metric cards where the surplus card is replaced in place by the backfill alert when payroll projection drops below the defended line. Repoint the index route to `/dashboard`.

Purpose: SC#3 ("dashboard renders MTD income with two visual markers"), SC#4 ("backfill alert when projected month total falls below $3,000"), INC-05/INC-06/EDGE-01. Vertical slice — after this plan Ian can SEE his income position, the core value of the app. Reads only the read-only derived atoms from 02-02; renders nothing it computes itself (FOUND-06).
Output: IncomeBar, MetricCard, BackfillAlertCard, DashboardPage, `/dashboard` route + index redirect + nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-income-model-with-two-floors/02-UI-SPEC.md
@.planning/phases/02-income-model-with-two-floors/02-PATTERNS.md

<interfaces>
<!-- Derived atoms from 02-02 the dashboard consumes (read-only). -->
From src/domains/income/income.atoms.ts (02-02):
- `mtdTotalAtom`, `projectedMonthAtom`/`projectedTotalAtom`, `projectedMonthPayrollAtom`, `surplusAtom`, `backfillActiveAtom`, `currentMonthChecksAtom`
From src/domains/settings/settings.atoms.ts: `floorsLoadAtom` → `{ passive, defended }`
Consume with `useAtomValue(atom, { delay: 0 })` (Pitfall-1 prophylaxis; source atom already uses initialValue:[]).
Phase-1 token/role analogs: src/components/Toast.tsx lines 16-48 (role + aria + token-only); src/components/NumberInput.tsx lines 27-36 (variant + class composition).
Currency: `new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' })`, rendered in `font-mono`.
Routes/nav to extend: src/App.tsx lines 18-23 (add /dashboard + repoint index); src/components/AppShell.tsx nav list (add Dashboard link).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IncomeBar (role=meter) + MetricCard + BackfillAlertCard</name>
  <read_first>
    - src/components/Toast.tsx (full — role/aria + token-class block to mirror)
    - src/components/NumberInput.tsx (full — variant-prop + class-composition style)
    - .planning/phases/02-income-model-with-two-floors/02-UI-SPEC.md lines 94-108 (income bar color mapping), 119-160 (bar spec + metric cards + backfill card + empty state), 289-298 (a11y: meter ARIA, alert role)
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 271-303 (IncomeBar/MetricCard/BackfillAlertCard analogs)
  </read_first>
  <behavior>
    - IncomeBar renders `role="meter"` with `aria-valuenow` = MTD total, `aria-valuemin="0"`, `aria-valuemax` = projectedMonth, `aria-label="Month-to-date income"`. Segments: solid success fill at (mtd/projected)×100%; warning ghost from fill edge to projected (only when projected > mtd); dashed text-secondary "floor" marker at (passive/projected)×100%; 2px accent "$3k" tick at (defended/projected)×100%. When projected > defended×1.2, clamp track at 100% and show "above defended line by $X" below.
    - MetricCard renders label + `font-mono text-[20px]` value + optional subtext; `variant?: 'default' | 'alert'`.
    - BackfillAlertCard renders `role="alert"`, label "Backfill alert", `font-mono` projected-payroll value, subtext "below $3,000 — add sessions to defend", warning border + bg-warning/10. No icon.
    - All colors are tailwind.config tokens (no inline hex); financial values font-mono; interactive elements (none on the bar) n/a.
  </behavior>
  <action>
    Create `src/components/IncomeBar.tsx` — props `{ mtdTotal, projectedMonth, passiveFloor, defendedLine }`; pure presentation per UI-SPEC §Income Bar Specification; compute segment widths as inline `style={{ width: '...%' }}` percentages (token classes for color, percentage widths are layout not color so allowed); no animated width transition (UI-SPEC reduced-motion). `role="meter"` + aria attrs.
    Create `src/components/MetricCard.tsx` — props `{ label, value, subtext?, variant?: 'default'|'alert' }`; copy NumberInput's variant/class-composition style; surplus value in `success` token when default.
    Create `src/domains/income/BackfillAlertCard.tsx` — `role="alert"`, copy from UI-SPEC Copywriting Contract verbatim, tokens only, `font-mono` dollar figure.
  </action>
  <verify>
    <automated>npx vitest run src/components/IncomeBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/IncomeBar.test.tsx` exits 0.
    - Test: IncomeBar with mtd 2424.10 / projected 2424.10 / passive 2400 / defended 3000 renders an element with `role="meter"`, `aria-valuenow="2424.1"` (or formatted), `aria-valuemax="2424.1"`.
    - `IncomeBar.tsx` contains `role="meter"`; `BackfillAlertCard.tsx` contains `role="alert"`.
    - Grep gate: no inline hex color in the three new files — `grep -v '^//' src/components/IncomeBar.tsx src/components/MetricCard.tsx src/domains/income/BackfillAlertCard.tsx | grep -Ei '#[0-9a-f]{3,6}'` returns nothing (exit 1).
  </acceptance_criteria>
  <done>Bar + cards render from props with correct ARIA, token-only colors, and font-mono values.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardPage wiring + /dashboard route + index redirect + nav</name>
  <read_first>
    - src/pages/SettingsPage.tsx lines 41-45 (page shell pattern)
    - src/App.tsx (full — Route list + index Navigate line 19)
    - src/components/AppShell.tsx (full — nav list)
    - .planning/phases/02-income-model-with-two-floors/02-UI-SPEC.md lines 111-160 (dashboard layout, three cards, surplus↔backfill swap, empty state)
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 320-343 (DashboardPage + route/nav analogs)
  </read_first>
  <action>
    Create `src/pages/DashboardPage.tsx`: section heading "Income · {Month YYYY}" (`font-display`); read derived atoms with `useAtomValue(atom, { delay: 0 })`; render `<IncomeBar>` then three cards — "Month to date" (mtdTotal), "Projected month" (projectedMonth), and conditionally `<MetricCard label="Surplus" .../>` OR `<BackfillAlertCard>` based on `backfillActiveAtom` (in-place replacement, pre-mirrors Phase-5 SURP-07). Add the `SecondaryButton` "Add check" → `/entry`. Empty state (no checks this month, via `currentMonthChecksAtom`): bar at 0% fill, three "$0.00" cards in `text.disabled`, and "No income recorded for {Month YYYY}. Enter your first check." with the link to `/entry`. Currency via `Intl.NumberFormat`. All copy verbatim from UI-SPEC Copywriting Contract.
    Edit `src/App.tsx`: add `<Route path="/dashboard" element={<DashboardPage />} />`; change the index redirect (line 19) and the `*` fallback to `Navigate to="/dashboard"` (now that a dashboard exists — PATTERNS line 343).
    Edit `src/components/AppShell.tsx`: add `<NavLink to="/dashboard" className={navClasses}>Dashboard</NavLink>` as the first nav item. Stay within the 6-route cap (Dashboard, Entry, Settings, Backup = 4).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits 0; `npx vitest run` exits 0.
    - `src/App.tsx` contains `path="/dashboard"` and the index redirect targets `/dashboard`.
    - `src/components/AppShell.tsx` contains a NavLink `to="/dashboard"`.
    - DashboardPage renders BackfillAlertCard (not the Surplus card) when `backfillActiveAtom` is true — assert via a render test that injects a store where backfill is active (the May-2026 fixture state) and queries `role="alert"`.
    - Empty-month render shows the "No income recorded" copy and does not throw.
  </acceptance_criteria>
  <done>/dashboard is the live index route showing income vs both floors with the in-place backfill swap and a designed empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived atoms → presentation | Read-only display of computed values; no new input boundary |
| (no network / no credentials) | Local-only, C2/C3 — N/A categories below |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Integrity | DashboardPage | mitigate | Renders only read-only derived atoms; persists nothing (FOUND-06). No write path on this surface. |
| T-02-05 | Tampering (correctness) | surplus↔backfill swap | mitigate | Swap driven by `backfillActiveAtom` (payroll-only, D-09); render test asserts the alert (not surplus) renders for the below-defended case so a regression to total-income is caught. |
| T-02-X | (XSS) | source/note (not shown here) | N/A | Dashboard renders numeric/derived values only; React escapes any text. |
| T-02-S/R/I/D | Spoofing/Repudiation/Info-Disclosure/DoS | — | N/A | Single-user local app, no auth/sessions/secrets/external input on this surface. |
</threat_model>

<success_criteria>
- Dashboard shows MTD income with solvency band + defended-line tick + ghost projection (SC#3, INC-05).
- Backfill alert replaces the surplus card when payroll projection < defended line (SC#4, INC-06, EDGE-01).
- Designed empty state; phone-readable single-column layout; tokens-only, font-mono numbers.
- `/dashboard` is the index route with a nav link.
</success_criteria>

<output>
After completion, create `.planning/phases/02-income-model-with-two-floors/02-04-SUMMARY.md`.
</output>
