---
phase: 02-income-model-with-two-floors
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/EntryTabBar.tsx
  - src/domains/income/CheckEntryForm.tsx
  - src/domains/income/SourceAutocomplete.tsx
  - src/pages/EntryPage.tsx
  - src/App.tsx
  - src/components/AppShell.tsx
  - src/domains/income/income.atoms.ts
  - src/domains/income/CheckEntryForm.test.tsx
autonomous: true
requirements: [INC-01, INC-07, UI-03, EDGE-05]
must_haves:
  truths:
    - "Ian can type a check (date, netAmount, source, category, taxable, note) and see it persisted"
    - "The manual-entry tab is the default; a Paste & parse tab is present (filled in 02-05)"
    - "A 3rd-payroll-check date shows the inline 'will be classified as surplus' badge"
    - "Selecting a known source auto-fills category + taxable"
    - "/entry route + nav link are live on the HashRouter"
  artifacts:
    - path: "src/domains/income/CheckEntryForm.tsx"
      provides: "manual income entry form with validation + known-source autocomplete + surplus badge"
    - path: "src/pages/EntryPage.tsx"
      provides: "EntryTabBar + manual form (paste tab placeholder)"
    - path: "src/components/EntryTabBar.tsx"
      provides: "ARIA two-tab control"
  key_links:
    - from: "src/domains/income/CheckEntryForm.tsx save handler"
      to: "storage.addIncomeCheck (via a write atom)"
      via: "saveIncomeCheckAtom"
      pattern: "addIncomeCheck"
    - from: "src/App.tsx"
      to: "EntryPage"
      via: "Route path=/entry"
      pattern: "path=\"/entry\""
---

<objective>
Deliver the first end-to-end user capability of the phase: Ian opens `/entry`, types a check, saves it, and it persists (and — via the 02-02 liveQuery source atom — becomes visible to any dashboard reader). Build the two-tab entry shell (Manual default + Paste & parse placeholder), the manual `CheckEntryForm` with validation, known-source autocomplete (D-06), and the 3rd-check surplus badge (D-12), then wire the `/entry` route + nav link.

Purpose: SC#1 ("type a check entry and see it persisted") and INC-01/INC-07. This is a vertical slice — after this plan a real user can record income, not just "lay foundation." The paste tab renders a placeholder until 02-05 fills it.
Output: EntryTabBar, CheckEntryForm, SourceAutocomplete, EntryPage, a save write-atom in income.atoms.ts, and `/entry` routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-income-model-with-two-floors/02-UI-SPEC.md
@.planning/phases/02-income-model-with-two-floors/02-PATTERNS.md

<interfaces>
<!-- Contracts from 02-01/02-02 + Phase-1 primitives. Use directly. -->
From src/storage/storage.ts (02-01): `addIncomeCheck(check)`, `getKnownSources(): Promise<KnownSource[]>`.
From src/domains/income/income.atoms.ts (02-02): `incomeChecksAtom`, `currentMonthChecksAtom`, `landedPayrollCountAtom`, `knownSourcesAtom` (add here if not already), `classifySurplus`/`isInLocalMonth` from classify.ts.
From src/domains/income/classify.ts (02-02): `defaultTaxable(category)`, `isInLocalMonth(iso, ref?)`.
From src/components (Phase 1): `NumberInput` (id/label/helper/value/onChange/error), `PrimaryButton` (onClick/disabled/children), `SecondaryButton`, `Toast` (message/variant/onDismiss; ToastVariant).
Pattern to copy — draft+validate+save+toast: src/pages/SettingsPage.tsx lines 15-82 (note `useAtomValue(atom, { delay: 0 })` Pitfall-1 prophylaxis at line 18).
Pattern to copy — active-tab class toggle: src/components/AppShell.tsx lines 10-19 (navItemBase + navClasses).
Routes/nav to extend: src/App.tsx lines 18-23 (Route list); src/components/AppShell.tsx lines 26-33 (NavLink list).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CheckEntryForm + SourceAutocomplete + save write-atom</name>
  <read_first>
    - src/pages/SettingsPage.tsx (full — draft/validate/save/toast + { delay: 0 } pattern)
    - src/domains/settings/settings.atoms.ts (full — write-only atom shape lines 32-35; do NOT copy refreshCounterAtom for income)
    - src/components/NumberInput.tsx, src/components/PrimaryButton.tsx, src/components/Toast.tsx (props/variants)
    - .planning/phases/02-income-model-with-two-floors/02-UI-SPEC.md lines 171-197 (manual entry fields, validation, surplus badge, autocomplete, copy)
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 218-236 (CheckEntryForm analog)
  </read_first>
  <behavior>
    - Filling date/amount/source/category/taxable + Save persists an IncomeCheck via storage.addIncomeCheck and shows the "Check saved." toast; form resets (date=today, amount empty, source empty).
    - Save is disabled when netAmount is not > 0 or date invalid or source empty (validate on blur for text, on change for amount).
    - Choosing category changes the taxable default (payroll→on, gift→off) but taxable stays user-editable.
    - When the entered date falls in a month already holding 2 payroll checks, the surplus badge "3rd check this month — will be classified as surplus" renders (informational, non-blocking).
    - Selecting a known source from SourceAutocomplete auto-fills category + taxable and shows "Auto-filled from remembered source".
  </behavior>
  <action>
    Add to the existing `src/domains/income/income.atoms.ts` (created in 02-02) the write-atom `saveIncomeCheckAtom = atom(null, async (_get, _set, check) => { await storage.addIncomeCheck(check) })` — NO refreshCounterAtom bump (the 02-02 liveQuery source re-emits automatically; copying the Phase-1 counter for the income chain is forbidden per PATTERNS lines 370-372). Add `knownSourcesAtom` if not present (read storage.getKnownSources; may be a plain async atom — autocomplete tolerates initial empty).
    Create `src/domains/income/SourceAutocomplete.tsx`: text input + dropdown of `knownSourcesAtom` source strings; UI-SPEC §Known-source autocomplete styling (each item `min-h-[44px]`, tokens only); on select, calls back with the matched KnownSource so the form auto-fills category+taxable.
    Create `src/domains/income/CheckEntryForm.tsx`: local draft per IncomeCheck field (date defaults to today, native `<input type="date">`; `NumberInput` for netAmount; `SourceAutocomplete` for source; `<select>` category payroll/gift/other default payroll; taxable `<input type="checkbox">` defaulting via `defaultTaxable`; 3-row note `<textarea>`). Validate per UI-SPEC. On Save: dispatch `saveIncomeCheckAtom`, success Toast "Check saved.", reset, return focus to Date field. Surplus badge: derive from `currentMonthChecksAtom` payroll count ≥ 2 for the entered date's month using `isInLocalMonth`. All inputs token-styled, `font-mono` on amount, `min-h-[44px]` on every interactive element, focus-visible ring (UI-SPEC lines 183-187, 277-285). Copy verbatim from UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>npx vitest run src/domains/income/CheckEntryForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domains/income/CheckEntryForm.test.tsx` exits 0.
    - Test (user-event): fill valid fields → click "Save check" → `storage.addIncomeCheck` was called with the entered values (or listIncomeChecks returns the row) and "Check saved." toast appears.
    - Test: Save disabled with empty source / amount 0.
    - Test: entering a date in a month with 2 existing payroll checks renders the surplus-badge text.
    - `CheckEntryForm.tsx` contains no `import { db }` and no `refreshCounterAtom`.
  </acceptance_criteria>
  <done>Manual entry persists a check via the storage abstraction with validation, autocomplete, and the surplus badge; liveQuery (not a manual counter) drives reactivity.</done>
</task>

<task type="auto">
  <name>Task 2: EntryTabBar + EntryPage + /entry route and nav</name>
  <read_first>
    - src/components/AppShell.tsx (full — navClasses active-state pattern + nav list)
    - src/App.tsx (full — Route list + Suspense fallback already covers async atoms)
    - src/pages/SettingsPage.tsx lines 41-45 (page shell pattern)
    - .planning/phases/02-income-model-with-two-floors/02-UI-SPEC.md lines 163-170, 199-201 (tab control, paste-tab placeholder)
    - .planning/phases/02-income-model-with-two-floors/02-PATTERNS.md lines 306-343 (EntryTabBar + page + route analogs)
    - .planning/phases/01-foundation-storage-deploy/SKELETON.md lines 102-103 (max 6 flat routes)
  </read_first>
  <action>
    Create `src/components/EntryTabBar.tsx`: ARIA tabs (`role="tablist"`/`role="tab"`/`aria-selected`), two full-width tabs "Manual entry" (default) and "Paste & parse", active state `border-b-2 border-accent` copied from AppShell navClasses, `min-h-[44px]`, arrow-key tab movement. Controlled via a `value`/`onChange` prop.
    Create `src/pages/EntryPage.tsx`: section heading "Add Income" (`font-display text-[20px]`), `max-w-[480px]` centered, `EntryTabBar` selecting between `<CheckEntryForm />` (manual) and a paste-tab placeholder (`<div>` with text "Paste & parse — coming up" — REPLACED by PasteParseFlow in 02-05; leave a clearly-labelled mount point).
    Edit `src/App.tsx`: add `<Route path="/entry" element={<EntryPage />} />` (lazy-import or direct import like SettingsPage). Do NOT change the index redirect yet (02-04 repoints `/` → `/dashboard`).
    Edit `src/components/AppShell.tsx`: add `<NavLink to="/entry" className={navClasses}>Entry</NavLink>` to the nav list. Stay within the 6-route SKELETON cap.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits 0; `npx vitest run` exits 0 (existing + CheckEntryForm tests green).
    - `src/App.tsx` contains `path="/entry"`.
    - `src/components/AppShell.tsx` contains a NavLink `to="/entry"`.
    - `EntryTabBar.tsx` contains `role="tablist"` and `aria-selected`.
    - EntryPage renders CheckEntryForm under the default tab and a labelled placeholder under the paste tab.
  </acceptance_criteria>
  <done>/entry is a live route with a working manual-entry tab and a placeholder paste tab; nav link present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input → storage | User-typed check fields cross into persistence |
| (no network / no credentials) | Local-only, C2/C3 — N/A categories below |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-V | Input Validation | CheckEntryForm | mitigate | Validate date is a real date and netAmount > 0 before dispatching save; Save disabled on invalid (UI-SPEC validation). |
| T-02-X | (XSS) | note/source rendering | mitigate | React auto-escapes JSX text; never `dangerouslySetInnerHTML` for source/note. Toast copy never concatenates user input. |
| T-02-06 | Tampering (spec) | save path | mitigate | Save goes through `storage.addIncomeCheck` only; no money-move/credential method touched (C2/C3). |
| T-02-S/R/I/D | Spoofing/Repudiation/Info-Disclosure/DoS | — | N/A | Single-user local app, no auth/sessions/secrets; entry volume is human-bounded. |
</threat_model>

<success_criteria>
- Ian can type a check at `/entry` and it persists (SC#1, INC-01/INC-07).
- Two-tab entry shell present; manual default; paste placeholder mount point ready for 02-05.
- Known-source autocomplete auto-fills category/taxable; 3rd-check surplus badge appears.
- Reactivity via liveQuery source atom — no manual refresh counter in the income chain.
</success_criteria>

<output>
After completion, create `.planning/phases/02-income-model-with-two-floors/02-03-SUMMARY.md`.
</output>
