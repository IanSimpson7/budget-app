---
phase: 03-expense-model-sinking-funds
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/components/FundCard.tsx
  - src/pages/FundsPage.tsx
  - src/App.tsx
  - src/test/FundsPage.test.tsx
  - src/test/FundCard.test.tsx
autonomous: true
requirements: [EXP-04, EXP-05, EXP-06, UI-04, EDGE-06]
must_haves:
  truths:
    - "Funds surface shows the car-insurance fund accruing ~$82/mo toward a $982 annual payout, with an on-track/behind status and a progress bar (SC#3)"
    - "Ian can add a SECOND sinking fund (e.g. car-purchase) via the same form with no code change — it renders as an independent card (SC#5)"
    - "Ian can mark an annual fund paid: balance resets to 0 and the payout date advances one year; the annual cost never appears as a monthly line (SC#4/EDGE-06)"
    - "A oneoff fund marked complete is archived (removed)"
    - "The car-insurance card shows the provisional-target advisory and never locks the target"
    - "The /funds surface has no execute/transfer affordance (C3) — only record actions"
  artifacts:
    - path: "src/pages/FundsPage.tsx"
      provides: "Fund card list + add-fund form + mark-paid/edit/delete + empty state"
      contains: "saveFundAtom"
    - path: "src/components/FundCard.tsx"
      provides: "Single fund card: name, status, progress bar, metrics row, payout date, provisional flag, actions"
      contains: "role=\"progressbar\""
    - path: "src/App.tsx"
      provides: "/funds route registration"
      contains: "/funds"
  key_links:
    - from: "src/pages/FundsPage.tsx"
      to: "src/domains/funds/funds.atoms.ts"
      via: "useAtomValue(sinkingFundsAtom) + useSetAtom(saveFundAtom/markFundPaidAtom)"
      pattern: "markFundPaidAtom"
    - from: "src/components/FundCard.tsx"
      to: "src/domains/funds/funds.atoms.ts"
      via: "isOnTrack(fund) for status + progress fill"
      pattern: "isOnTrack"
    - from: "src/App.tsx"
      to: "src/pages/FundsPage.tsx"
      via: "Route path=/funds"
      pattern: "/funds"
---

<objective>
Ship the `/funds` vertical slice: sinking-fund cards (balance, target, accrual, on-track status, progress bar, payout date, provisional flag), an Add-fund form that proves the generic primitive (SC#5 — a second fund adds with zero code change), and the mark-paid / auto-roll / delete interactions (EDGE-06). Registers the `/funds` route in App.tsx (the nav link was already added by Plan 02).

Purpose: Delivers SC#3 (car-insurance fund accruing toward payout), SC#4 (no monthly shock — annual cost pre-accrued), SC#5 (second instance, no code change), and UI-04 (sinking-fund instances with progress toward payout).
Output: FundCard, FundsPage, /funds route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-expense-model-sinking-funds/03-CONTEXT.md
@.planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md
@.planning/phases/03-expense-model-sinking-funds/03-01-SUMMARY.md
@.planning/phases/03-expense-model-sinking-funds/03-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 + 02. Use directly. -->

From src/domains/funds/funds.atoms.ts (Plan 01):
- `sinkingFundsAtom` (read), `isOnTrack(fund): boolean`, `monthsUntilPayout(payoutDate): number`
- write atoms: `saveFundAtom`, `updateFundAtom`, `deleteFundAtom`, `markFundPaidAtom`
- Type `SinkingFund` from `src/storage/schema.ts`: `{ id?, name, annualAmount, monthlyAccrual, balance, payoutDate (YYYY-MM), cadence: 'annual'|'oneoff', provisional? }`

From Plan 02 (already built — reuse, do NOT recreate):
- `src/components/TextInput.tsx`, `src/components/SelectInput.tsx`
- `src/components/AppShell.tsx` already has the "Funds" NavLink (`to="/funds"`)

From src/components/NumberInput.tsx / PrimaryButton / SecondaryButton / DestructiveButton: existing primitives for the add form + action buttons.

From src/App.tsx (current after Plan 02 — add the /funds route): HashRouter + `<Suspense>`; `/expenses` route now present; index + `*` Navigate to /dashboard.

Copywriting + layout: 03-UI-SPEC.md §Surface 3 — fund card structure (lines order: name + status badge row, progress bar, 3-col metrics row, payout date, provisional flag, actions row), mark-paid confirmation copy (annual vs oneoff), Add-fund form fields, empty-state copy, progress-bar aria spec.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FundCard component — status, progress bar, metrics, provisional flag, action affordances</name>
  <read_first>
    - src/components/MetricCard.tsx (visual language to extend — bg-surface-raised card)
    - src/domains/funds/funds.atoms.ts (isOnTrack contract for status + fill color)
    - .planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md §"Surface 3 / Fund Card" + §"Copywriting Contract" + §"Accessibility" (progressbar aria) + §"Interaction States"
  </read_first>
  <action>
    Create `src/components/FundCard.tsx` rendering one `SinkingFund` as `bg-surface-raised border border-surface-border rounded-sm p-sp-4 flex flex-col gap-sp-3`. Top row (`flex justify-between items-center`): fund name (`font-sans text-sm font-semibold`) + status badge — "On track" `text-success` or "Behind" `text-warning`, computed from `isOnTrack(fund)`; the text label carries the meaning (not color alone). Progress bar: outer `w-full h-[6px] rounded-sm bg-surface-border`, inner fill width `min(100, (balance / annualAmount) × 100)%`, fill `bg-success` when on track else `bg-warning`; `role="progressbar" aria-valuenow={balance} aria-valuemin={0} aria-valuemax={annualAmount}`. Metrics row `grid grid-cols-3 gap-sp-2`: Current (balance), Target (annualAmount), "/ mo accrual" (monthlyAccrual) — each value `font-mono text-[20px] text-text-primary` with a `text-xs text-text-secondary` label. Payout date line `font-sans text-xs text-text-secondary` "Due [Month YYYY]" (format from the YYYY-MM via local `new Date(y, m-1, 1)`). If `fund.provisional`, show `font-sans text-xs text-warning` "Target is provisional — update at renewal." Actions row: SecondaryButton "Edit", SecondaryButton "Mark paid", DestructiveButton "Remove" (all `min-h-[44px]`). Props: `{ fund: SinkingFund, onEdit, onMarkPaid, onRemove }` (handlers wired by FundsPage). NO transfer/execute affordance anywhere (C3). Tokens only.
  </action>
  <verify>
    <automated>npm run test -- --run FundCard</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/FundCard.test.tsx` exits 0: renders the car-insurance fund (balance 0, accrual 82, target 982, payout 2027-03), asserts status text "Behind", a `role="progressbar"` with `aria-valuemax="982"`, and that the provisional advisory renders when `provisional` is true.
    - FundCard contains no text/button matching /transfer|execute|move money/i (C3 structural check).
    - `grep -nE "#[0-9a-fA-F]{3,6}" src/components/FundCard.tsx` finds no inline hex.
    - `npx tsc -b` clean.
  </acceptance_criteria>
  <done>FundCard renders all UI-SPEC fields with correct status/progress and no money-movement affordance.</done>
</task>

<task type="auto">
  <name>Task 2: FundsPage — card list, add-fund form, mark-paid/edit/delete, empty state; /funds route</name>
  <read_first>
    - src/pages/ExpensesPage.tsx (Plan 02 — page layout + add-form + inline-confirmation pattern to mirror)
    - src/domains/funds/funds.atoms.ts (sinkingFundsAtom + write atoms)
    - src/App.tsx (route registration — add /funds)
    - .planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md §"Surface 3" (Add form fields, mark-paid confirmation copy, empty state)
    - .planning/phases/03-expense-model-sinking-funds/03-02-SUMMARY.md (component + route conventions as built)
  </read_first>
  <action>
    Create `src/pages/FundsPage.tsx` at `max-w-[640px] mx-auto flex flex-col gap-sp-6`, heading `<h2 className="font-display text-[20px] ...">Sinking Funds</h2>`. Read `useAtomValue(sinkingFundsAtom)`; render each fund via `FundCard` wired to handlers: onEdit → inline pre-filled mini-form calling `updateFundAtom`; onMarkPaid → inline confirmation (annual copy "Mark [name] as paid? Balance resets and target date advances to [Month YYYY+1]." / oneoff copy "Mark [name] as complete? This fund will be archived.") confirming via `markFundPaidAtom`; onRemove → inline confirmation ("Remove [name]? Accruals will no longer feed the survival floor.") calling `deleteFundAtom`. Add-fund form (inline, below cards or top when empty): TextInput (name), NumberInput (annual target), NumberInput (monthly accrual — pre-populate to `annualTarget/12` and stay editable), NumberInput (current balance, default 0), `type="month"` input (payout date, YYYY-MM), SelectInput (cadence Annual/One-off); PrimaryButton "Add fund" disabled until name + target + payout date filled; on submit call `saveFundAtom`. Helper under annual target: "Provisional targets can be updated anytime." Empty state (no funds) per Copywriting Contract with "Add your first fund" CTA. NO execute/transfer affordance (C3).
    In `src/App.tsx` add `<Route path="/funds" element={<FundsPage />} />` (import FundsPage). The "Funds" nav link already exists from Plan 02.
  </action>
  <verify>
    <automated>npm run test -- --run FundsPage</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/FundsPage.test.tsx` exits 0: renders with the seeded car-insurance fund visible (SC#3); fills + submits the add form to create a SECOND fund and asserts two FundCards render independently (SC#5 — no code change for the second instance); triggers Mark-paid on the annual fund and asserts the confirmation copy names the consequence ("Balance resets"); asserts NO element matches /transfer|execute|move money/i (C3).
    - App.tsx contains `<Route path="/funds"` importing FundsPage.
    - `npx tsc -b` clean; `npm run test -- --run` full suite green.
  </acceptance_criteria>
  <done>/funds is a working route: view funds with status + progress, add a second fund with zero code change, mark-paid/edit/delete — all record-only (C3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Add-fund form input → storage write | User-typed financial fields cross into IndexedDB via Plan 01's guarded addSinkingFund |
| mark-paid action → storage write | Records a user-asserted real-world payout; the app performs NO money movement (C3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | mark-paid / add-fund mutating fund state in a way that simulates a transfer (C3 erosion) | mitigate | Structural: FundsPage calls ONLY save/update/delete/markFundPaid (DB record actions). No transfer/execute affordance in FundCard or FundsPage — verified by the C3 grep assertion in both tests. mark-paid records balance=0 + payoutDate advance, a DB state change, never a money move. |
| T-03-09 | Tampering | annualAmount / monthlyAccrual / balance entered as NaN or negative | mitigate | Plan 01's addSinkingFund throws on non-finite financial fields; the form's NumberInput coerces via Number(); submit disabled until required fields filled. |
| T-03-10 | Repudiation | Provisional car-insurance target silently locked, preventing Ian from updating at renewal | accept | UI never locks the target — Edit always exposes annualAmount; provisional advisory is informational. Aligns with D-05 (fully editable, never locked). Verified by the provisional advisory test + editable target in the edit mini-form. |
</threat_model>

<verification>
- `npm run test -- --run` full suite green.
- `npx tsc -b` clean.
- No-double-count holds end-to-end: car insurance is a fund only (Plan 01 regression test) and never an expense line.
- Manual (deferred to UAT): /funds readable on phone, tap targets ≥44px (VALIDATION manual-only row).
</verification>

<success_criteria>
- SC#3: car-insurance fund visible, accruing ~$82/mo toward $982 with status + progress.
- SC#4/EDGE-06: mark-paid resets balance + advances payout; annual cost never a monthly line.
- SC#5: second fund added via the same form, no code change.
- UI-04: sinking-fund instances show progress toward payout dates.
- C3: no execute/transfer affordance anywhere on /funds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-expense-model-sinking-funds/03-03-SUMMARY.md`
</output>
