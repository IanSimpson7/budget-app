---
phase: 03-expense-model-sinking-funds
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/TextInput.tsx
  - src/components/SelectInput.tsx
  - src/components/ClassificationToggle.tsx
  - src/pages/ExpensesPage.tsx
  - src/pages/DashboardPage.tsx
  - src/App.tsx
  - src/components/AppShell.tsx
  - src/test/ExpensesPage.test.tsx
  - src/test/DashboardPage.test.tsx
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-07, UI-04]
must_haves:
  truths:
    - "Ian can add an expense line (name, amount, cadence, classification) on /expenses and see it land in the correct PROTECTED or GATEABLE column (SC#1)"
    - "Ian can edit and delete an expense line in place"
    - "The dashboard shows a 'Survival floor' metric card whose value recomputes when an expense changes (SC#2)"
    - "Typing 'whey' or 'supplement' in the name field surfaces the EXP-07 advisory (no hard block)"
    - "The four seeded PROTECTED starter rows appear on first run"
  artifacts:
    - path: "src/pages/ExpensesPage.tsx"
      provides: "Add-expense form + protected/gateable categorized view + inline edit/delete"
      contains: "survivalFloorAtom"
    - path: "src/components/ClassificationToggle.tsx"
      provides: "Two-button mutually-exclusive PROTECTED/GATEABLE control"
      contains: "aria-label"
    - path: "src/components/TextInput.tsx"
      provides: "Text input matching NumberInput tokens"
    - path: "src/components/SelectInput.tsx"
      provides: "Styled select for cadence"
  key_links:
    - from: "src/pages/ExpensesPage.tsx"
      to: "src/domains/expenses/expenses.atoms.ts"
      via: "useAtomValue(protectedExpensesAtom/gateableExpensesAtom) + useSetAtom(saveExpenseItemAtom)"
      pattern: "saveExpenseItemAtom"
    - from: "src/pages/DashboardPage.tsx"
      to: "src/domains/expenses/expenses.atoms.ts"
      via: "useAtomValue(survivalFloorAtom)"
      pattern: "survivalFloorAtom"
    - from: "src/App.tsx"
      to: "src/pages/ExpensesPage.tsx"
      via: "Route path=/expenses"
      pattern: "/expenses"
---

<objective>
Ship the `/expenses` vertical slice and the dashboard survival-floor card. Ian gets a real, end-to-end capability: record/classify/edit/delete expense lines, see them split protected-vs-gateable, and watch the survival-floor number on the dashboard recompute live. This plan owns the `/expenses` route plus BOTH new nav links ("Expenses" and "Funds") in AppShell. The `/funds` route itself is registered by Plan 03 (which depends on this plan); registering the Funds nav link early is safe because HashRouter falls back to /dashboard for an unmatched /funds until Plan 03 lands.

Purpose: Delivers SC#1 (categorized view) and SC#2 (survival floor on dashboard).
Output: ExpensesPage, three new form primitives, dashboard card, /expenses route + both nav links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-expense-model-sinking-funds/03-CONTEXT.md
@.planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md
@.planning/phases/03-expense-model-sinking-funds/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (this plan's dependency) + existing components. Use directly. -->

From src/domains/expenses/expenses.atoms.ts (Plan 01):
- `expenseItemsAtom`, `protectedExpensesAtom`, `gateableExpensesAtom` (read), `survivalFloorAtom` (async read)
- write atoms: `saveExpenseItemAtom`, `updateExpenseItemAtom`, `deleteExpenseItemAtom`
- Types `ExpenseItem`, `Classification`, `Cadence` from `src/storage/schema.ts`

From src/components/NumberInput.tsx (clone for TextInput/SelectInput tokens):
- Props `{ id, label, value, onChange, helper?, error? }`; input classes use `bg-surface-raised text-text-primary font-sans ... min-h-[44px] ... border border-surface-border focus:border-accent focus-visible:outline-accent`; label is `font-sans text-sm font-semibold text-text-primary` with `htmlFor`/`id` pairing.

From src/components/MetricCard.tsx (reuse unchanged for survival floor card):
- Props `{ label, value, subtext?, variant?, valueColor? }`; value renders `font-mono text-[20px]`.

From src/pages/DashboardPage.tsx (current — you append one card):
- 3-card grid `grid grid-cols-3 gap-sp-2 sm:gap-sp-3`; uses `useAtomValue(atom, { delay: 0 })`; `currency = new Intl.NumberFormat('en-US', { style:'currency', currency:'USD' })`; async atoms already covered by App.tsx `<Suspense>`.

From src/App.tsx (current — add ONE route):
- HashRouter + `<Suspense>`; existing routes `/dashboard /settings /backup /entry`; index + `*` Navigate to /dashboard.

From src/components/AppShell.tsx (current — add nav links):
- `navClasses({isActive})` helper; existing NavLinks Dashboard/Entry/Settings/Backup; nav is `<nav className="flex gap-sp-6 ...">`.

Copywriting + layout: 03-UI-SPEC.md §Surface 1 (survival floor card label "Survival floor", subtext "fixed + food seed", zero state $0.00 text-text-disabled) and §Surface 2 (form fields, ClassificationToggle spec, two-column list, cadence badge "/ mo" "/ yr" "one-off", seed note, EXP-07 advisory copy, empty-state copy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form primitives — TextInput, SelectInput, ClassificationToggle</name>
  <read_first>
    - src/components/NumberInput.tsx (token/label/focus pattern to clone exactly)
    - .planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md §"Component Reuse Map" (TextInput + SelectInput specs) + §"Surface 2" ClassificationToggle spec + §"Accessibility"
  </read_first>
  <action>
    Create `src/components/TextInput.tsx`: identical structure to NumberInput but `type="text"`, `value: string`, `onChange: (next: string) => void`, no `inputMode="numeric"`; same border/focus/label/helper/error markup and `htmlFor`/`id` pairing. Create `src/components/SelectInput.tsx`: a styled `<select>` with props `{ id, label, value, onChange, options: {value,label}[] }`, classes `bg-surface-raised text-text-primary font-sans text-sm border border-surface-border rounded-sm min-h-[44px] px-[12px] focus:border-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`, label matching NumberInput. Create `src/components/ClassificationToggle.tsx`: two `min-h-[44px]` buttons (PROTECTED default-selected / GATEABLE), active state `bg-surface-raised border border-accent text-text-primary font-semibold`, inactive `bg-transparent border border-surface-border text-text-secondary`; wrapper `role="group" aria-label="Classification"`, each button `aria-pressed`; props `{ value: Classification, onChange: (c: Classification) => void }`. All tokens only — no inline hex.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` reports no errors.
    - ClassificationToggle wrapper has `role="group"` and `aria-label="Classification"`; each option carries `aria-pressed`.
    - `grep -nE "#[0-9a-fA-F]{3,6}" src/components/TextInput.tsx src/components/SelectInput.tsx src/components/ClassificationToggle.tsx` finds no inline hex color (token classes only).
    - All three interactive controls use `min-h-[44px]`.
  </acceptance_criteria>
  <done>Three reusable form primitives exist, typed, token-only, a11y-correct.</done>
</task>

<task type="auto">
  <name>Task 2: ExpensesPage — add form + categorized view + inline edit/delete; /expenses route + both nav links</name>
  <read_first>
    - src/pages/EntryPage.tsx (existing page layout + form-state posture to mirror)
    - src/domains/income/income.atoms.ts (useAtomValue/useSetAtom write-atom usage pattern)
    - src/App.tsx (route registration) and src/components/AppShell.tsx (nav link pattern)
    - .planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md §"Surface 2" + §"Copywriting Contract" + §"Surface 3" nav link names
    - .planning/phases/03-expense-model-sinking-funds/03-01-SUMMARY.md (atom + write-atom names as built)
  </read_first>
  <action>
    Create `src/pages/ExpensesPage.tsx` at `max-w-[640px] mx-auto flex flex-col gap-sp-6`, heading `<h2 className="font-display text-[20px] ...">Expenses</h2>`. Add-expense inline form using TextInput (name), NumberInput (amount), SelectInput (cadence: Monthly/Annual/One-off), ClassificationToggle (default PROTECTED); PrimaryButton "Add expense" disabled until name + amount filled; on submit call `useSetAtom(saveExpenseItemAtom)`. Cadence helper copy per UI-SPEC ("Counted as [amount ÷ 12]/mo..." for annual; "One-off costs are excluded from the survival floor." for oneoff). EXP-07 advisory: when the name field matches /whey|supplement/i show the advisory helper text from the Copywriting Contract (soft advisory, never disables submit — clinical-safety posture, C1). Categorized view: `grid grid-cols-1 sm:grid-cols-2 gap-sp-4`, two columns reading `useAtomValue(protectedExpensesAtom)` / `useAtomValue(gateableExpensesAtom)`; each row `bg-surface-raised border border-surface-border rounded-sm p-sp-3` showing name, `font-mono text-[20px]` amount + cadence badge ("/ mo" "/ yr" "one-off"), inline "Edit" (replaces row with a pre-filled mini-form using updateExpenseItemAtom) and DestructiveButton "Remove" with the inline confirmation copy ("Remove [name]? This will update the survival floor."). Empty-state copy per Copywriting Contract.
    Wire routing: in `src/App.tsx` add ONLY `<Route path="/expenses" element={<ExpensesPage />} />` (import ExpensesPage). Do NOT add the `/funds` route here — Plan 03 owns it and importing the not-yet-built FundsPage would break the build. In `src/components/AppShell.tsx` add two NavLinks "Expenses" (`to="/expenses"`) and "Funds" (`to="/funds"`) using the existing `navClasses`; the Funds link is safe to add now (HashRouter `*` fallback renders /dashboard for unmatched /funds until Plan 03 registers the route).
  </action>
  <verify>
    <automated>npm run test -- --run ExpensesPage</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/ExpensesPage.test.tsx` exists and exits 0: renders the page, submits the add form, asserts a PROTECTED entry appears in the Protected column and a GATEABLE entry in the Gateable column (SC#1); asserts typing "whey protein" surfaces the advisory text AND the "Add expense" button remains enabled.
    - App.tsx contains route `/expenses` (and does NOT import FundsPage); AppShell.tsx contains NavLink `to="/expenses"` and `to="/funds"`.
    - `npx tsc -b` clean.
  </acceptance_criteria>
  <done>/expenses is a working route: add, classify, edit, delete; both new nav links registered; build is green without FundsPage.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard survival-floor metric card</name>
  <read_first>
    - src/pages/DashboardPage.tsx (the file being modified — grid + useAtomValue delay:0 + currency formatter)
    - src/components/MetricCard.tsx (props)
    - .planning/phases/03-expense-model-sinking-funds/03-UI-SPEC.md §"Surface 1" (card label/subtext/zero-state spec)
  </read_first>
  <action>
    In `src/pages/DashboardPage.tsx` add a `survivalFloor = useAtomValue(survivalFloorAtom, { delay: 0 })` read (import from `../domains/expenses/expenses.atoms`). Append a MetricCard `label="Survival floor"`, `value={currency.format(survivalFloor)}`, `subtext="fixed + food seed"`, `variant="default"`. Place it additively — either as a 4th card (grid stays valid; switch the metric grid to a 2-row 2-col layout on small screens if a 4-card row is cramped) WITHOUT replacing any existing card. When survivalFloor is 0, render value `$0.00` with `valueColor="text-text-disabled"`. The async atom is already covered by App.tsx's `<Suspense>`.
  </action>
  <verify>
    <automated>npm run test -- --run DashboardPage</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/DashboardPage.test.tsx` (extend existing) asserts a card labeled "Survival floor" renders, and that after adding a PROTECTED expense the displayed value increases (SC#2 recompute).
    - The three existing cards (Month to date / Projected month / Surplus|Backfill) still render — no card replaced.
    - `npx tsc -b` clean; `npm run test -- --run` full suite green.
  </acceptance_criteria>
  <done>Dashboard shows a live Survival floor card that recomputes on expense change; existing cards intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Expense form input → storage write | User-typed name/amount crosses into IndexedDB via Plan 01's guarded addExpenseItem |
| Derived survivalFloorAtom → dashboard render | Derived value must render a finite currency string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Amount field accepting NaN/empty propagating to survival floor | mitigate | NumberInput coerces via `Number()`; submit disabled until amount filled; Plan 01's addExpenseItem throws on non-finite (defense in depth). Page test covers a normal numeric add. |
| T-03-06 | Information Disclosure | EXP-07 advisory must NOT block the user (clinical-safety posture — never restrict food/supplement entry) | accept | Advisory is soft helper text only; submit stays enabled. Aligns with C1 (no gating). Verified by the whey-advisory page test asserting submit remains enabled. |
| T-03-07 | Denial of Service | Async survivalFloorAtom suspending dashboard without a boundary | mitigate | App.tsx `<Suspense>` already wraps Routes; `useAtomValue(..., { delay: 0 })` matches existing dashboard prophylaxis (RESEARCH pitfall 3). |
</threat_model>

<verification>
- `npm run test -- --run` full suite green.
- `npx tsc -b` clean.
- Manual (deferred to UAT): /expenses + dashboard readable on phone, tap targets ≥44px (VALIDATION manual-only row).
</verification>

<success_criteria>
- SC#1: add an expense → appears in correct protected/gateable column.
- SC#2: Survival floor card on dashboard recomputes on expense change.
- EXP-07 advisory shows for whey/supplement without blocking.
- /expenses route + both nav links registered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-expense-model-sinking-funds/03-02-SUMMARY.md`
</output>
