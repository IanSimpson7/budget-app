---
phase: 04-food-contract-locked-floor
plan: 07
type: tdd
wave: 6
depends_on: [01, 02, 03, 04, 06]
gap_closure: true
files_modified:
  - src/domains/food/food.atoms.ts
  - src/test/food.atoms.test.ts
  - src/domains/food/planParser.ts
  - src/domains/food/planParser.test.ts
  - src/domains/food/costEngine.ts
  - src/domains/food/costEngine.test.ts
  - src/storage/storage.ts
  - src/test/storage.food.test.ts
requirements: [FOOD-06, FOOD-07, FOOD-09, FOOD-12, EDGE-02, EDGE-03]
must_haves:
  truths:
    - "CR-01 (C1): the DISPLAYED protected floor never falls below its all-time high-water on ANY live path — displayed floor = max(computed floor, allTimeHighWater). A shorter month, a lower portion edit, or a leaner plan can never make the displayed protected floor drop."
    - "CR-02 (C1): the monthly floor divides total scheduled cost by DAYS-WITH-MEALS, not the raw calendar span — a plan window with empty/un-logged days no longer understates the floor (dividing by a smaller, correct denominator keeps the daily average conservative)."
    - "WR-05 (C1): the JSON import path validates finiteness of foodFloorMeta / unitCostMap / portionModel numeric fields and rejects NaN/Infinity, matching the guards on the normal write path — a tampered backup cannot poison the floor."
    - "WR-02: the clean-result high-water write-back has a .catch (no unhandled rejection on IDB failure) and the persisted high-water propagates without a stale-by-one-cycle lag."
  artifacts:
    - path: "src/domains/food/food.atoms.ts"
      provides: "displayed floor ratcheted to allTimeHighWater (CR-01); windowDays from mealDays (CR-02); robust write-back (WR-02)"
      contains: "allTimeHighWater"
    - path: "src/domains/food/planParser.ts"
      provides: "ParsedPlan.mealDays — count of distinct calendar days with >=1 meal"
      contains: "mealDays"
---

<objective>
Close the 2 C1 blockers + 2 C1-adjacent warnings from the Phase 4 deep code review (04-REVIEW.md). Ian ratified CR-01 as Reading A (ratchet). All four fixes share the same north star: the protected food floor must NEVER be understated or appear to fall — incomplete/missing data must make it conservative, never lower.

- **CR-01 (BLOCKER, C1):** ratchet the DISPLAYED live floor to the all-time high-water.
- **CR-02 (BLOCKER, C1):** divide by days-with-meals, not calendar span.
- **WR-05 (WARNING, C1):** validate finiteness of food fields on JSON import.
- **WR-02 (WARNING):** robust high-water write-back (.catch + no stale-by-one-cycle lag).

C1 INVARIANT (INVIOLABLE, clinical): no change here may lower the displayed protected floor or introduce a downward-edit affordance. The displayed `floor` only ever ratchets UP or holds. `solvencyFloor` (the solvency consumer, 04-06) keeps its existing realistic semantics and is NOT ratcheted — only the displayed `floor` gets the ratchet.

Output: all fixes landed with TDD, full suite green, `tsc -b` clean. Re-review follows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-REVIEW.md
@.planning/phases/04-food-contract-locked-floor/04-CONTEXT.md
Read these merged sources first:
- src/domains/food/food.atoms.ts (foodFloorAtom live/stale paths; FoodFloorResult; write-back; DEFAULT_FOOD_FLOOR_SEED)
- src/domains/food/planParser.ts + planParser.test.ts (ParsedPlan { windowStart, windowEnd, meals[] }; per-day prose + table formats)
- src/domains/food/costEngine.ts + costEngine.test.ts (computeFloor; windowDays contract; ParsedPlan mirror)
- src/storage/storage.ts + src/test/storage.food.test.ts (importAll/replaceAll path; existing finiteness guards on saveUnitCostMap/savePortionModel; getFoodFloorMeta)
</context>

<tasks>

<task id="1" type="tdd">
**CR-01 — Ratchet the displayed live floor to the all-time high-water.**

In `src/domains/food/food.atoms.ts`, `foodFloorAtom` live path: the returned `floor` MUST be `Math.max(result.floor, meta.allTimeHighWater ?? 0)` — never below the all-time high-water. Apply to BOTH the clean-live and gapped-live returns (the stale path already returns `max(last, highWater)` / seed and is left as-is).

- The write-back (high-water ratchet) still uses `result.floor` (the real clean computation) for `allTimeHighWater = Math.max(prev, result.floor)` — do NOT ratchet the persisted high-water to the clamped display value.
- `solvencyFloor` is UNCHANGED (clean → result.floor; gapped → max(last, highWater, seed)). Only the displayed `floor` is clamped.
- Update the header comment: Reading A ratified by Ian (2026-05-30) — a displayed food floor that visibly falls month-over-month is the restriction signal C1 forbids; high-water is written only on clean results so it is always a real (never fallback-inflated) value.

TDD (RED → GREEN): in `src/test/food.atoms.test.ts` — clean-live where `result.floor < meta.allTimeHighWater` → returned `floor === allTimeHighWater`; clean-live new high (`result.floor > highWater`) → returned `floor === result.floor` AND high-water updates; gapped-live → returned `floor >= allTimeHighWater`; confirm `solvencyFloor` is NOT ratcheted (still realistic). Include the shorter-month case (the suite's existing `result31 > result30` math) to prove the displayed floor holds across a month-length drop.
</task>

<task id="2" type="tdd">
**CR-02 — Divide by days-with-meals, not calendar span.**

1. `src/domains/food/planParser.ts`: extend `ParsedPlan` with `mealDays: number` = the count of DISTINCT calendar days that have at least one meal slot. Single-date plans → 1. Batch plans (both prose `## YYYY-MM-DD` per-day sections AND the table format) → count distinct days that actually carry meals. `mealDays` must be ≤ the calendar span and ≥ 1 for any plan with meals (0 only if the plan has no meals at all).
2. `src/domains/food/costEngine.ts`: update the `ParsedPlan` mirror interface to include `mealDays`. (The `windowDays` parameter of `computeFloor` is unchanged — it remains caller-supplied; the contract comment "distinct days the plan covers" now has a real source.)
3. `src/domains/food/food.atoms.ts`: in the live path, set `windowDays = Math.max(selectedPlan.mealDays, 1)` instead of `planDaySpan(selectedPlan)`. Keep `planDaySpan` ONLY where it is still needed for the most-specific-plan overlap selection (narrowest window). Document that the daily-average denominator is days-with-meals (C1-conservative: skipping a day's log raises, never lowers, the average).

TDD (RED → GREEN): `planParser.test.ts` — assert `mealDays` for each of the 5 fixtures (single-date = 1; the two batch fixtures = their true distinct-meal-day counts; verify a window with a gap day yields `mealDays < span`). `costEngine.test.ts` — unchanged contract, but add a case proving a 7-day window with 3 meal-days divides by 3, not 7. `food.atoms.test.ts` — the live path feeds `mealDays` as `windowDays`.
</task>

<task id="3" type="tdd">
**WR-05 — Validate food-field finiteness on JSON import.**

In `src/storage/storage.ts`, the import/`replaceAll` path: before persisting imported `foodFloorMeta`, `unitCostMap`, and `portionModel`, validate numeric finiteness and reject the import (throw the existing ImportError type / same failure mode used for expense/fund validation) on any non-finite value:
- `unitCostMap[].costPerUnit` finite (mirror the guard in `saveUnitCostMap`).
- `portionModel[].portionSize` finite (mirror the guard in `savePortionModel`).
- `foodFloorMeta.lastComputedFloor` and `foodFloorMeta.allTimeHighWater` finite and ≥ 0.
Reuse the existing validation helpers/path rather than duplicating logic where possible.

TDD (RED → GREEN): `src/test/storage.food.test.ts` — importing a backup with `allTimeHighWater: NaN`, `costPerUnit: Infinity`, or `portionSize: NaN` is REJECTED (throws, no partial write); a clean backup still round-trips. C1 rationale: a tampered/corrupt backup must not be able to poison or zero the protected floor.
</task>

<task id="4" type="tdd">
**WR-02 — Robust high-water write-back.**

In `src/domains/food/food.atoms.ts`, the fire-and-forget `saveFoodFloorMeta` write-back on clean results:
1. Add a `.catch` so an IDB failure cannot raise an unhandled promise rejection (log/swallow — the next read recomputes).
2. Eliminate the stale-by-one-cycle lag in the persisted high-water. PREFERRED: convert `foodFloorMetaAtom` to `atomWithObservable(() => storage.observeFoodFloorMeta(), { initialValue: ... })` (mirror the `mealDefinitionsAtom` liveQuery pattern) and add `storage.observeFoodFloorMeta()` to the storage abstraction, so a write-back auto-propagates to the next read with no refresh counter. If that proves higher-risk than warranted, instead document the eventual-consistency window explicitly and ensure correctness holds because the per-cycle clamp (task 1) uses the meta read at cycle start. Choose the lower-risk option and record the choice in the SUMMARY.

TDD (RED → GREEN): `food.atoms.test.ts` and/or `storage.food.test.ts` — a rejected `saveFoodFloorMeta` does not throw out of the atom read; if the observable path is taken, a foodFloorMeta write is observed by the meta atom without a manual refresh bump.

After all tasks: full `npx vitest run` + `npx tsc -b` green before SUMMARY.
</task>

</tasks>

<verification>
- `tsc -b` clean; full vitest suite green.
- CR-01: grep confirms the live `floor` return is wrapped in `Math.max(..., allTimeHighWater...)`; `solvencyFloor` is not.
- CR-02: `grep mealDays src/domains/food/planParser.ts` present; food.atoms windowDays uses `mealDays`.
- WR-05: import path rejects non-finite food fields (test proves it).
- C1 unchanged: no path lowers the displayed floor; no downward-edit affordance added.
</verification>

<success_criteria>
- [ ] CR-01 displayed-floor ratchet (Reading A) — clean + gapped live paths clamped to high-water; solvencyFloor untouched
- [ ] CR-02 ParsedPlan.mealDays + windowDays uses it
- [ ] WR-05 import finiteness validation for food fields
- [ ] WR-02 write-back .catch + lag eliminated (or documented with correctness argument)
- [ ] Each task committed atomically (RED then GREEN)
- [ ] SUMMARY.md created; full suite + tsc green
- [ ] STATE.md + ROADMAP.md updated (plan 04-07 complete)
</success_criteria>
