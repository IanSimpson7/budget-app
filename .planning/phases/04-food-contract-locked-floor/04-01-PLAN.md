---
phase: 04-food-contract-locked-floor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/migrations.ts
  - src/storage/storage.ts
  - src/domains/food/food.types.ts
  - src/main.tsx
  - src/test/storage.test.ts
  - src/test/migrations.test.ts
  - src/domains/food/planParser.test.ts
  - src/domains/food/costEngine.test.ts
  - src/pages/FoodPage.test.tsx
  - src/domains/food/__fixtures__/
autonomous: true
requirements: [FOOD-01, FOOD-02, FOOD-04, FOOD-05, FOOD-10, FOOD-12, FOOD-13]
must_haves:
  truths:
    - "Schema reports version 4 and a fresh DB opens at v4 without data loss"
    - "Meal definitions, unit-cost map, portion model, food-floor meta, and flavor line round-trip through JSON export/import"
    - "storage.ts exposes NO method that writes the food floor downward (C1 structural enforcement)"
    - "The 14 known meal-name strings seed once into the meal-definition table on first run"
  artifacts:
    - path: "src/storage/schema.ts"
      provides: "Phase 4 types + CURRENT_SCHEMA_VERSION = 4 + SchemaV1Data.mealDefinitions"
      contains: "CURRENT_SCHEMA_VERSION = 4"
    - path: "src/domains/food/food.types.ts"
      provides: "MealDefinition, UnitCostEntry, PortionEntry, FoodFloorMeta, FlavorLine, FloorGap, ParsedPlan, CostEngine I/O types"
      contains: "export type MealDefinition"
    - path: "src/storage/db.ts"
      provides: "version(4) block with mealDefinitions table"
      contains: "this.version(4)"
    - path: "src/storage/migrations.ts"
      provides: "migrate_3_to_4 + MIGRATIONS[3] entry"
      contains: "migrate_3_to_4"
    - path: "src/storage/storage.ts"
      provides: "meal-definition CRUD + 4 food settings singleton get/save + seedMealDefinitionsIfEmpty + export/import wiring"
      exports: ["observeMealDefinitions", "addMealDefinition", "getUnitCostMap", "saveFoodFloorMeta"]
  key_links:
    - from: "src/storage/storage.ts"
      to: "db.mealDefinitions"
      via: "observeMealDefinitions liveQuery + CRUD"
      pattern: "db\\.mealDefinitions"
    - from: "src/storage/storage.ts collectSchemaV1Data/replaceAll"
      to: "mealDefinitions + food settings keys"
      via: "export envelope round-trip"
      pattern: "mealDefinitions"
    - from: "src/main.tsx"
      to: "storage.seedMealDefinitionsIfEmpty"
      via: "startup seed call"
      pattern: "seedMealDefinitionsIfEmpty"
---

<objective>
Lay the Phase 4 storage + type foundation: schema v3→v4 migration, the new `mealDefinitions` Dexie table, four food settings singletons, full CRUD through the storage abstraction, JSON export/import round-trip, the 14-meal seed, and ALL Wave-0 test scaffolds the downstream vertical slices depend on.

Purpose: This is the one unavoidably-horizontal prerequisite. Every later slice (parser, cost engine, UI) joins to these tables and types. Getting the contracts right here is what lets Plans 02–05 run as thin vertical slices without scavenger-hunting the codebase. The C1 invariant is established structurally HERE — the storage surface gets meal/cost CRUD but NO method that can write the floor downward.

Output: schema.ts (v4 + types), food.types.ts (all shared contracts), db.ts (v4 block), migrations.ts (migrate_3_to_4), storage.ts (CRUD + singletons + seed + export/import), main.tsx (seed call), plus Wave-0 test files and SMC fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-CONTEXT.md
@.planning/phases/04-food-contract-locked-floor/04-RESEARCH.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-VALIDATION.md
@.planning/phases/01-foundation-storage-deploy/SKELETON.md

<interfaces>
<!-- Contracts to DEFINE in food.types.ts (Task 1). Downstream plans import these. -->
<!-- Field shapes are fixed by 04-PATTERNS.md "food.types.ts (model)" section. -->

MealType = 'decomposed' | 'flat-cost'
IngredientTag = 'macro-bearing' | 'flavor-condiment'

MealDefinition = Readonly<{ id?: number; mealName: string /* normalized lowercase+trim */; type: MealType; ingredients: string[]; flatCost?: number }>
UnitCostEntry  = Readonly<{ ingredientName: string; costPerUnit: number /* 0 ⇒ unpriced ⇒ fallback-high */; unit: string; tag: IngredientTag }>
PortionEntry   = Readonly<{ ingredientName: string; portionSize: number }>
FoodFloorMeta  = Readonly<{ lastComputedFloor: number; allTimeHighWater: number; lastRefinedFromReceipts: string | null }>
FlavorLine     = Readonly<{ amount: number }>

ParsedPlan = { windowStart: string; windowEnd: string; meals: string[] /* normalized */ }
FloorGap =
  | { type: 'unpriced-ingredient'; ingredientName: string }
  | { type: 'undefined-meal'; mealName: string }
  | { type: 'unset-flat-cost'; mealName: string }
  | { type: 'stale-plan'; lastKnownDate: string | null }
CostEngineInput  = { scheduledMeals: string[]; mealDefinitions: MealDefinition[]; unitCostMap: UnitCostEntry[]; portionModel: PortionEntry[]; daysInMonth: number }
CostEngineResult = { floor: number; gaps: FloorGap[]; isClean: boolean }
FoodFloorResult  = { floor: number; gaps: FloorGap[]; isClean: boolean; planIsCurrent: boolean }
</interfaces>

<analogs>
<!-- Concrete existing code to copy. Excerpts in 04-PATTERNS.md. -->
schema.ts          → self (bump line ~6; extend SchemaV1Data ~82-88)
db.ts              → self v3 block (~38-48) → add v4 block + mealDefinitions Table property
migrations.ts      → self migrate_2_to_3 (~40-48) + MIGRATIONS map (~50-53)
storage.ts CRUD    → self expense CRUD (~66-121, incl Number.isFinite guard ~71-74) + knownSources singleton (~176-194) + collectSchemaV1Data (~212-234) + replaceAll (~319-366) + seedExpensesIfEmpty (~96-121)
storage.test.ts    → self absence-proof block (~30-46)
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: food.types.ts + schema v4 types + version bump</name>
  <read_first>
    - src/storage/schema.ts (the file being modified — read in full; it is &lt;110 lines)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "src/domains/food/food.types.ts (model)" (exact field shapes) AND section "src/storage/schema.ts (model — bump + extend)" (the v3→v4 bump + SchemaV1Data extension excerpt)
    - .planning/phases/04-food-contract-locked-floor/04-CONTEXT.md — D-01, D-03, D-04, D-05, D-13 (field semantics)
  </read_first>
  <action>
    Create `src/domains/food/food.types.ts` exporting the full contract set from the `<interfaces>` block above: `MealType`, `IngredientTag`, `MealDefinition`, `UnitCostEntry`, `PortionEntry`, `FoodFloorMeta`, `FlavorLine`, `ParsedPlan`, `FloorGap` (4-variant discriminated union including `stale-plan`), `CostEngineInput`, `CostEngineResult`, `FoodFloorResult`. Pure types only — NO Dexie/jotai/storage imports. Use `Readonly<{...}>` per the existing schema.ts convention. Document the meal-name normalization contract (lowercase + trim) as a comment on `MealDefinition.mealName` per D-01 and Pitfall 5. In `src/storage/schema.ts`: bump `CURRENT_SCHEMA_VERSION` from `3` to `4`; add `mealDefinitions: unknown[]` to the `SchemaV1Data` type; re-export `MealDefinition` (and the other food types) from schema.ts OR import them where db.ts needs them — keep `food.types.ts` as the single source. Do NOT remove `Floors.foodSeed` (backward compat per PATTERNS Floors note).
  </action>
  <verify>
    <automated>cd projects/budget-app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/domains/food/food.types.ts` exports `MealDefinition`, `UnitCostEntry`, `PortionEntry`, `FoodFloorMeta`, `FlavorLine`, `FloorGap`, `ParsedPlan`, `CostEngineInput`, `CostEngineResult`, `FoodFloorResult`.
    - `FloorGap` union includes a `'stale-plan'` variant with a `lastKnownDate: string | null` field.
    - `grep -v '^//' src/storage/schema.ts | grep -c "CURRENT_SCHEMA_VERSION = 4"` returns 1.
    - `SchemaV1Data` contains a `mealDefinitions` member.
    - `npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>food.types.ts holds every shared Phase 4 contract; schema version is 4; SchemaV1Data round-trips mealDefinitions; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: db.ts v4 block + migrate_3_to_4 + migration test</name>
  <read_first>
    - src/storage/db.ts (the file being modified — read in full)
    - src/storage/migrations.ts (the file being modified — read in full)
    - src/test/migrations.test.ts (extend this — read the v2→v3 case)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — sections "src/storage/db.ts (config)" and "src/storage/migrations.ts (utility)" (verbatim v4 block + migrate_3_to_4 excerpt + MIGRATIONS map extension)
  </read_first>
  <action>
    In `src/storage/db.ts`: add a `mealDefinitions!: Table<MealDefinition, number>` class property (import `MealDefinition` from food.types.ts/schema.ts); add a `this.version(4).stores({...})` block copying the v3 store strings and adding `mealDefinitions: '++id, mealName'`; settings table string stays `'&key'` (the four food singletons live as settings keys, no new table). Leave the v3 block intact (Dexie needs the version ladder). In `src/storage/migrations.ts`: add `export function migrate_3_to_4(data: SchemaV1Data): SchemaV1Data` returning `{ ...data, mealDefinitions: data.mealDefinitions ?? [] }` (data no-op; food settings singletons initialize at first storage.get* via `??` defaults); add `3: migrate_3_to_4` to the `MIGRATIONS` map. In `src/test/migrations.test.ts`: add a case asserting `migrate_3_to_4` preserves existing arrays and defaults `mealDefinitions` to `[]` when absent, and that `MIGRATIONS[3]` is defined.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/test/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "this.version(4)" src/storage/db.ts` returns 1; `grep -c "mealDefinitions: '++id, mealName'" src/storage/db.ts` returns 1.
    - `grep -c "migrate_3_to_4" src/storage/migrations.ts` returns 2 (definition + map entry).
    - `MIGRATIONS[3]` is defined (test asserts `typeof MIGRATIONS[3] === 'function'`).
    - migrations.test.ts new case: `migrate_3_to_4({...without mealDefinitions})` yields `mealDefinitions: []`; existing `incomeChecks`/`expenseItems`/`sinkingFunds` arrays pass through unchanged.
    - `npm run test -- --run src/test/migrations.test.ts` exits 0.
  </acceptance_criteria>
  <done>DB opens at version 4 with the mealDefinitions table; the v3→v4 migration is a verified data no-op wired into the shared MIGRATIONS ladder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: storage.ts CRUD + 4 food singletons + seed + export/import + C1 absence-proofs</name>
  <behavior>
    - meal-definition CRUD round-trips: add → observe emits the row → update patches it → delete removes it.
    - Each of unitCostMap / portionModel / foodFloorMeta / flavorLine: get returns its default when unset, save then get returns the saved value.
    - Non-finite guard: saving a unit cost / portion / flavor amount that is NaN/Infinity throws (mirrors addExpenseItem guard).
    - exportAll envelope includes mealDefinitions + the four food settings keys; importAll restores them (full round-trip into a fresh DB).
    - seedMealDefinitionsIfEmpty seeds exactly the 14 meal-name strings once; a second call is a no-op (sentinel set).
    - C1 absence-proof: storage exports no `setFoodFloor` and no `decreaseFoodFloor`.
  </behavior>
  <read_first>
    - src/storage/storage.ts (the file being modified — read in full; it is the C1 surface)
    - src/test/storage.test.ts (extend the existing absence-proof block — read it)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "src/storage/storage.ts (service)" (CRUD method set, settings singleton get/save, C1 absence comment, collectSchemaV1Data + replaceAll extensions, seed sentinel) and "Shared Patterns" (Financial non-finite guard, Absence-proof test)
    - .planning/phases/04-food-contract-locked-floor/04-CONTEXT.md — Specific Ideas (14 seed meal strings, unit-cost seed values, $550 floor seed, $50 flavor seed)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — Pattern 5 (C1 three enforcement layers) + Pattern 6 (settings singletons)
  </read_first>
  <action>
    In `src/storage/storage.ts` add, following the existing expense/knownSources patterns:
    (1) Meal-definition CRUD: `addMealDefinition(meal: Omit<MealDefinition,'id'>)`, `listMealDefinitions()`, `updateMealDefinition(id, patch)`, `deleteMealDefinition(id)`, `observeMealDefinitions(): Observable<MealDefinition[]>` (liveQuery, same shape as observeExpenseItems).
    (2) Four settings singletons with `const KEY = '...'` constants and `?? default`: `getUnitCostMap`/`saveUnitCostMap` (key `'unitCostMap'`, default `[]`), `getPortionModel`/`savePortionModel` (key `'portionModel'`, default `[]`), `getFoodFloorMeta`/`saveFoodFloorMeta` (key `'foodFloorMeta'`, default `{ lastComputedFloor: 0, allTimeHighWater: 550, lastRefinedFromReceipts: null }` — seed high-water at the $550 floor seed per D-07/FOOD-13 so the very first stale state never shows below seed), `getFlavorLine`/`saveFlavorLine` (key `'flavorLine'`, default `{ amount: 50 }` per FOOD-10).
    (3) Apply the `Number.isFinite` guard (copy the addExpenseItem guard verbatim, renamed) to every numeric input across `saveUnitCostMap` entries, `savePortionModel` entries, and `saveFlavorLine.amount`.
    (4) `seedMealDefinitionsIfEmpty()` using a `MEALS_SEEDED_KEY = 'mealDefinitionsSeeded'` sentinel: seed the 14 meal strings (normalized lowercase+trim) from 04-CONTEXT Specific Ideas — mark "qdoba bowl" as `type: 'flat-cost'`, the other 13 as `type: 'decomposed'` with empty `ingredients` (Ian fills ingredients later in config); also seed an initial `unitCostMap` and `portionModel` from the §5c seed values (90/10 ground beef ~$5.80/lb, chicken breast ~$2/lb) ONLY for the most common ingredients — leave the rest for Ian. Set the sentinel after seeding.
    (5) Extend `collectSchemaV1Data` to include `mealDefinitions` and ensure the four food settings keys flow through the existing `settings` map (they already will, since settings is dumped wholesale — verify and document).
    (6) Extend `replaceAll` to clear + restore `db.mealDefinitions` (add it to the transaction table list and add the restore block, stripping `id` per the existing pattern).
    (7) Confirm NO `setFoodFloor`/`decreaseFoodFloor`/floor-write method exists; update the C1 absence comment block to name the food floor.
    In `src/test/storage.test.ts`: extend the absence-proof block with `expect(s.setFoodFloor).toBeUndefined()` and `expect(s.decreaseFoodFloor).toBeUndefined()`; add round-trip tests for meal CRUD, the four singletons (default + save/get), the non-finite guard, export/import round-trip including mealDefinitions, and the seed-once behavior.
    In `src/main.tsx`: call `void storage.seedMealDefinitionsIfEmpty()` alongside the existing seed calls.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/test/storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - storage.test.ts: `expect(s.setFoodFloor).toBeUndefined()` and `expect(s.decreaseFoodFloor).toBeUndefined()` both pass (V6 absence-proof).
    - Meal CRUD round-trip test passes (add → observe/list → update → delete).
    - Each singleton: get returns default when unset (foodFloorMeta.allTimeHighWater === 550, flavorLine.amount === 50), and save→get returns saved value.
    - Non-finite guard test: saving Infinity/NaN as a unit cost / portion / flavor amount throws.
    - Export→import round-trip into a fresh DB reproduces seeded meal definitions and the four food settings keys.
    - `seedMealDefinitionsIfEmpty` test: first call inserts 14 rows; second call inserts 0 (sentinel honored).
    - `grep -c "seedMealDefinitionsIfEmpty" src/main.tsx` returns 1.
    - `npm run test -- --run src/test/storage.test.ts` exits 0.
  </acceptance_criteria>
  <done>The food domain has a complete, tested persistence surface; the floor is structurally unwritable downward (C1); 14 meals + food settings seed once and survive a JSON backup round-trip.</done>
</task>

<task type="auto">
  <name>Task 4: Wave-0 test scaffolds + SMC fixtures (parser, cost engine, FoodPage)</name>
  <read_first>
    - .planning/phases/04-food-contract-locked-floor/04-VALIDATION.md — V1..V8 (each scaffold maps to specific V-IDs)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — "Wave 0 Gaps" list + "Phase Requirements → Test Map"
    - src/domains/income/classify.test.ts (pure-function colocated-test convention to mirror)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "planParser.ts" (colocated test scaffold) and "costEngine.ts"
    - ../schedule-meal-coordinator/plans/ (the live SMC plan files to copy as fixtures — read at least one single-date file and one date-range file to confirm format before copying)
  </read_first>
  <action>
    Copy 5 live SMC plan files from `../schedule-meal-coordinator/plans/*.md` into `src/domains/food/__fixtures__/` as hermetic read-only snapshots (do NOT symlink — V1 sampling note). Include at least one single-date `YYYY-MM-DD.md` file AND one date-range `YYYY-MM-DD--YYYY-MM-DD.md` file so both parser branches have fixtures. Create the Wave-0 test files as RED scaffolds that import the not-yet-implemented modules and describe expected behavior with `it.todo` or `expect`-on-stub so they fail meaningfully until Plans 02–03 implement them:
    - `src/domains/food/planParser.test.ts` — V1/V2: assert all 14 known meal strings extract across the fixtures; window dates correct; both filename formats; `parsePlanFile` returns null on garbage. Reference fixture filenames.
    - `src/domains/food/costEngine.test.ts` — V3/V4/V5: table-driven gap triggers (unpriced ingredient, undefined meal, unset flat cost) each yield fallback-high + a flag never $0; monthly = daily-avg × daysInMonth for 1-day and 7-day windows; stale path = max(last, highWater).
    - `src/pages/FoodPage.test.tsx` — UI-02/FOOD-12/EDGE-02/EDGE-03: floor renders as a non-`<input>` element with a lock icon; amber badge appears when gaps exist.
    Set each `<verify>`-blocking test that has no implementation yet to a clearly-failing assertion OR `it.todo(...)` so the executor of Plans 02–05 turns them GREEN. Record in the SUMMARY which V-IDs each scaffold covers.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/domains/food/ 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/domains/food/__fixtures__/` contains 5 `.md` files including ≥1 single-date and ≥1 date-range filename.
    - `src/domains/food/planParser.test.ts`, `src/domains/food/costEngine.test.ts`, `src/pages/FoodPage.test.tsx` exist.
    - planParser.test.ts references all 14 meal-name strings from the corpus.
    - costEngine.test.ts has table-driven cases for all three gap triggers (unpriced-ingredient, undefined-meal, unset-flat-cost) plus the 1-day and 7-day monthly-derivation cases and the stale `max(last, highWater)` case.
    - The scaffolds are RED (or `it.todo`) — they do not falsely pass before implementation. Test runner lists them as pending/failing, not skipped-green.
  </acceptance_criteria>
  <done>Every Nyquist Dimension-8 mechanism (V1–V8) has a failing test or fixture in place before its implementation slice runs; SMC fixtures are hermetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SMC plan files → app (build-time glob) | External text bundled at build; malformed/adversarial content must never crash or zero the floor (handled in Plan 02 parser; types defined here) |
| User config input → IndexedDB | Numeric unit-cost/portion/flavor values cross into persistence; must be finite + non-negative |
| Derived floor → storage | The floor is the C1-critical value; there must be NO write path for it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | storage.ts food-floor surface | mitigate | NO `setFoodFloor`/`decreaseFoodFloor` method exists; `saveFoodFloorMeta` persists engine-written metadata only (no user-settable floor). Absence-proof test in storage.test.ts (V6). |
| T-04-02 | Information disclosure (silent undercount) | food settings defaults | mitigate | `foodFloorMeta` default seeds `allTimeHighWater: 550` so the first-ever stale read never shows below the $550 seed (C1: floor never silently low). |
| T-04-03 | Tampering | numeric config inputs | mitigate | `Number.isFinite` guard on every unit-cost/portion/flavor write (copied from addExpenseItem); $0 unit cost is treated as "unpriced" gap downstream, never a valid zero. |
| T-04-04 | Denial of service | JSON import (replaceAll) | accept | Local single-user import of own backup; existing schema-version refusal + try/catch toast (Phase 1) covers malformed envelopes. No new surface. |
</threat_model>

<verification>
- `npx tsc -b --noEmit` exits 0.
- `npm run test -- --run src/test/storage.test.ts src/test/migrations.test.ts` exits 0.
- C1 absence-proof present and green (V6).
- Wave-0 scaffolds for V1–V8 exist and are RED/pending (not falsely green).
</verification>

<success_criteria>
- Schema is v4; mealDefinitions table + 4 food singletons persist and round-trip through export/import.
- storage.ts has meal CRUD + singletons + seed, and structurally cannot write the floor down (C1).
- 14 meals seed once; food settings seed with $550 floor / $50 flavor defaults.
- All downstream slices (Plans 02–05) have the contracts (food.types.ts) and failing tests (V1–V8) they need.
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md` recording: exact new storage exports, the food.types.ts contract surface, which V-IDs each Wave-0 scaffold covers, and the chosen foodFloorMeta default (allTimeHighWater = 550).
</output>
