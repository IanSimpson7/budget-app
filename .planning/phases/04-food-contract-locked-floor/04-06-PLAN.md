---
phase: 04-food-contract-locked-floor
plan: 06
type: tdd
wave: 5
depends_on: [01, 03, 04]
gap_closure: true
files_modified:
  - src/domains/food/costEngine.ts
  - src/domains/food/costEngine.test.ts
  - src/domains/food/food.atoms.ts
  - src/test/food.atoms.test.ts
  - src/domains/expenses/expenses.atoms.ts
  - src/test/expenses.atoms.test.ts
autonomous: true
requirements: [FOOD-06, FOOD-07, FOOD-09, EDGE-02, EDGE-03]
must_haves:
  truths:
    - "Solvency math (survivalFloorAtom) NEVER consumes the fallback-high displayed floor; on any gapped live result it uses a REALISTIC food estimate = max(lastComputedFloor, allTimeHighWater, DEFAULT_FOOD_FLOOR_SEED), so an unconfigured cost map cannot inflate solvency"
    - "The DISPLAYED protected floor (FoodFloorResult.floor) stays conservative-high on gaps (C1 unchanged) — only the solvency consumer is decoupled"
    - "Snack/light eating-occasion slots draw a lower fallback ceiling than full meals, so a protein-shake slot no longer falls back to the full-meal ceiling"
    - "When two or more current plans overlap on today, meals are NOT double-counted: a single most-specific plan is selected and its own day-span is used as windowDays"
  artifacts:
    - path: "src/domains/food/costEngine.ts"
      provides: "FALLBACK_CEILING_MEAL, FALLBACK_CEILING_SNACK, classifyMealKind(name), kind-aware fallback in computeFloor"
      contains: "FALLBACK_CEILING_SNACK"
    - path: "src/domains/food/food.atoms.ts"
      provides: "FoodFloorResult.solvencyFloor; most-specific-current-plan selection (no overlap double-count)"
      contains: "solvencyFloor"
  key_links:
    - from: "src/domains/expenses/expenses.atoms.ts"
      to: "src/domains/food/food.atoms.ts"
      via: "survivalFloorAtom reads solvencyFloor (NOT floor)"
      pattern: "solvencyFloor"
---

<objective>
Checkpoint-surfaced deviation fix for Phase 4 (raised by Ian during the 04-05 human-verify checkpoint). With only 3 of ~15 ingredients seeded, all 16 scheduled meal slots hit the fallback ceiling, computing a food floor of ~$2,480 and pushing the survival floor to $4,265 — a solvency number hostage to an unconfigured cost map. Three independent, complementary fixes:

- **(c) Decouple solvency from fallback-high** — `survivalFloorAtom` must use a realistic food estimate, never the conservative-high displayed floor, until the map is configured. This is the breakage fix.
- **(b) Calibrate the fallback per eating-occasion kind** — a snack/shake slot must not draw the full-meal ceiling.
- **(d) Overlap double-count guard** — when current plan windows overlap on today, do not sum meals across them.

C1 INVARIANT (INVIOLABLE): The DISPLAYED protected floor must never be understated. (c) does NOT lower the displayed floor — it only changes which number the SOLVENCY math consumes. Understating food in solvency is the clinically-safe direction (it never pressures food); overstating it is what breaks the dashboard. (b) only LOWERS the fallback for genuinely lighter occasions and stays conservative (≥ real cost). (d) is a pure correctness guard. None of the three may introduce a downward-editable affordance on the protected floor.

Output: corrected cost engine + atom chain + solvency wire, fully tested, full suite green, `tsc -b` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-CONTEXT.md
@.planning/phases/04-food-contract-locked-floor/04-RESEARCH.md
Read these merged source files first to ground the changes:
- src/domains/food/costEngine.ts (current fallback math; FALLBACK_CEILING_PER_MEAL = 15)
- src/domains/food/costEngine.test.ts (existing fallback assertions — MUST be re-audited per task 1)
- src/domains/food/food.atoms.ts (foodFloorAtom live/stale paths; FoodFloorResult; DEFAULT_FOOD_FLOOR_SEED = 550)
- src/test/food.atoms.test.ts
- src/domains/expenses/expenses.atoms.ts (survivalFloorAtom reads `{ floor }` at line ~67)
- src/test/expenses.atoms.test.ts
- src/storage/storage.ts (SEED_UNIT_COST_MAP, seeded meal definitions — context only; DO NOT change the seed in this plan)
</context>

<tasks>

<task id="1" type="tdd">
**(b) Kind-aware fallback ceiling in the cost engine.**

In `src/domains/food/costEngine.ts`:
1. Replace the single `FALLBACK_CEILING_PER_MEAL = 15` with two exported constants:
   - `export const FALLBACK_CEILING_MEAL = 15` (full meal — unchanged conservative value)
   - `export const FALLBACK_CEILING_SNACK = 5` (snack / shake / light occasion — conservative-high for a light item)
   - Keep `export const FALLBACK_CEILING_PER_MEAL = FALLBACK_CEILING_MEAL` as a `@deprecated` back-compat alias so external references do not break.
2. Add a pure exported helper `export function classifyMealKind(mealName: string): 'meal' | 'snack'`.
   - Returns `'snack'` if the lowercased name contains ANY of these substrings: `shake`, `cereal`, `yogurt`, `rice cake`, `cream pie`, `granola`. Otherwise `'meal'`.
   - Document inline that this is a FALLBACK-ONLY heuristic: it affects the cost ONLY when a meal already triggers a gap (unpriced/undefined/unset). Once ingredients are priced, the exact portion×cost sum supersedes it. Conservative default = `'meal'` for unknown names.
   - Note the heuristic is matched against the normalized corpus (`normalizeMealName` lowercases); verify against the 14-meal corpus that exactly these classify as snack: `Cereal and milk`, `Greek yogurt with granola and berries`, `Oatmeal cream pie and banana`, `Protein shake and banana`, `Protein Slop and Granola`, `Rice cakes with peanut butter and banana`. All others classify as `meal`.
3. In `computeFloor`, every fallback path (undefined-meal, unset-flat-cost, unpriced-ingredient) must add `classifyMealKind(name) === 'snack' ? FALLBACK_CEILING_SNACK : FALLBACK_CEILING_MEAL` instead of the flat constant. Use the scheduled `mealName` for undefined-meal, and `def.mealName` for the flat-cost / decomposed paths.

TDD: First update `costEngine.test.ts` (RED) — audit EVERY existing assertion that expects `15` (or `FALLBACK_CEILING_PER_MEAL`) for a gapped meal: if the meal name classifies as snack, the expected fallback becomes `5`; if it classifies as meal, it stays `15`. Add focused new tests: (a) a snack-classified meal that gaps contributes `FALLBACK_CEILING_SNACK`; (b) a meal-classified meal that gaps contributes `FALLBACK_CEILING_MEAL`; (c) `classifyMealKind` unit tests covering each of the 6 snack corpus strings and at least 3 meal corpus strings; (d) C1 regression: a snack with an unpriced ingredient still falls back (never $0) — just at the snack ceiling. Then implement (GREEN). Commit RED then GREEN.
</task>

<task id="2" type="tdd">
**(c) Decouple solvency + (d) overlap double-count guard in the atom chain.**

In `src/domains/food/food.atoms.ts`:

**(c)** Extend `FoodFloorResult` with `solvencyFloor: number`. This is the number the solvency math consumes — realistic, never fallback-high:
- Live + clean (`isClean === true`): `solvencyFloor = floor` (the real computed value).
- Live + gapped (`isClean === false`): `solvencyFloor = Math.max(meta.lastComputedFloor, meta.allTimeHighWater, DEFAULT_FOOD_FLOOR_SEED)`. NOT the fallback-high `floor`. (lastComputedFloor/allTimeHighWater are written ONLY on clean results — I-03 — so they are realistic, never fallback-inflated.)
- Stale path (`planIsCurrent === false`): `solvencyFloor = floor` (the stale `floor` is already `max(last, highWater)` or the seed — realistic by construction).
- `floor` itself is UNCHANGED on every path (C1: displayed floor stays conservative-high). Update the inline header comment to document the floor-vs-solvencyFloor distinction and that C1 governs `floor` while `solvencyFloor` protects solvency from an unconfigured map.

**(d)** Replace the live-path plan aggregation (currently `currentPlans.flatMap(p => p.meals)` + union-of-days `windowDays`) with single most-specific-plan selection to prevent double-counting when windows overlap on today:
- From `currentPlans` (all plans whose window covers today), select ONE: the narrowest window span (`windowEnd − windowStart` in days, smallest wins); tie-break by latest `windowStart`; final tie-break by last position in the array.
- Use the selected plan's `meals` as `scheduledMeals`, and the selected plan's own day-span (inclusive day count, min 1) as `windowDays`.
- When only one plan covers today (the common case) the selection returns it unchanged — no behavior regression. Add an inline comment explaining the overlap rationale.

TDD (RED → GREEN, commit each): In `src/test/food.atoms.test.ts`:
- (c) gapped-live result exposes `solvencyFloor === max(last, highWater, seed)` and `solvencyFloor < floor` when the fallback inflates `floor`; clean-live result exposes `solvencyFloor === floor`; stale result exposes `solvencyFloor === floor`.
- (d) two current plans overlapping on today (e.g. a single-date plan and a batch range both covering today) select ONE plan and do NOT sum meals across both — assert `windowDays` equals the selected plan's span and the meal count is the selected plan's, not the union sum.
- C1 regression: `floor` on a gapped-live result is still the conservative-high value (unchanged by this task).
</task>

<task id="3" type="tdd">
**(c) wire — survivalFloorAtom consumes solvencyFloor.**

In `src/domains/expenses/expenses.atoms.ts`, change `survivalFloorAtom` to destructure and use `solvencyFloor` instead of `floor`:
```ts
const { solvencyFloor } = await get(foodFloorAtom)
...
return fixedExFood + accruals + solvencyFloor
```
Update the header comment (V7 note) to state that solvency uses the REALISTIC food estimate (`solvencyFloor`), never the fallback-high displayed floor, so an unconfigured cost map cannot inflate the survival floor — while `/food` still displays the conservative-high `floor`.

TDD (RED → GREEN, commit each): In `src/test/expenses.atoms.test.ts`:
- With a GAPPED live food result (fallback-high `floor`, realistic `solvencyFloor`), assert `survivalFloor === fixedExFood + accruals + solvencyFloor` and that it does NOT include the inflated `floor`.
- With a CLEAN live result, assert `survivalFloor` uses the real computed floor (solvencyFloor === floor).
- Solvency-math regression: confirm the existing passive-floor / surplus tests still hold (solvency uses the passive floor, never the $3,000 defended line).

After all three tasks: run the FULL suite (`npx vitest run`) and `tsc -b`; both must be green before SUMMARY.
</task>

</tasks>

<verification>
- `tsc -b` clean; full vitest suite green (expect ~890+ tests).
- `grep solvencyFloor src/domains/expenses/expenses.atoms.ts` returns the survival-floor usage.
- `grep FALLBACK_CEILING_SNACK src/domains/food/costEngine.ts` present and used in computeFloor.
- C1 unchanged: `FoodFloorResult.floor` values on gapped paths are NOT lowered by tasks 2–3 (only `solvencyFloor` is new); no new downward-edit affordance on the protected floor anywhere.
</verification>

<success_criteria>
- [ ] (b) kind-aware fallback: snack ceiling $5, meal ceiling $15, classifyMealKind tested against the corpus
- [ ] (c) FoodFloorResult.solvencyFloor added; survivalFloorAtom consumes it; displayed floor unchanged
- [ ] (d) overlap double-count guard: single most-specific current plan selected
- [ ] Each task committed atomically (RED then GREEN)
- [ ] SUMMARY.md created; full suite + tsc green
- [ ] STATE.md + ROADMAP.md updated (plan 04-06 complete)
</success_criteria>
