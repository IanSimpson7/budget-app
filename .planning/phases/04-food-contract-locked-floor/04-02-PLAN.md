---
phase: 04-food-contract-locked-floor
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/domains/food/planParser.ts
  - src/domains/food/planParser.test.ts
  - vite.config.ts
  - README.md
autonomous: true
requirements: [FOOD-01, FOOD-03]
must_haves:
  truths:
    - "The parser extracts scheduled meal names + plan window from BOTH single-date and date-range SMC plan files"
    - "All 14 known meal-name strings parse out of the live fixtures with 0 dropped slots"
    - "import.meta.glob resolves >0 plan files in a local dev build; CI-fallback (0 files) is documented, not a surprise"
    - "SMC files are read only — no write/trigger path exists anywhere"
  artifacts:
    - path: "src/domains/food/planParser.ts"
      provides: "parsePlanFile(filename, raw) → ParsedPlan | null (pure, no Dexie/glob)"
      exports: ["parsePlanFile"]
    - path: "vite.config.ts"
      provides: "server.fs.allow ['..'] so dev server serves the sibling SMC repo"
      contains: "fs:"
    - path: "README.md"
      provides: "Documented build-locally-then-push flow + CI=fallback behavior (V8)"
      contains: "schedule-meal-coordinator"
  key_links:
    - from: "src/domains/food/planParser.ts"
      to: "ParsedPlan type"
      via: "import from food.types.ts"
      pattern: "ParsedPlan"
    - from: "vite.config.ts server.fs.allow"
      to: "../schedule-meal-coordinator/"
      via: "dev server permits parent dir"
      pattern: "allow"
---

<objective>
Build the pure SMC plan parser and the Vite glob wiring that feeds it — the first vertical capability: the app can read real scheduled meal names + plan windows from the live SMC plan files (read-only), turning the RED V1 scaffold GREEN.

Purpose: This is the ingestion edge of the food floor. Get the parser correct against live fixtures (both filename formats, both body formats, the mixed-separator meal strings, non-decomposable items) before any cost math depends on it. Resolves RESEARCH Open Q2 (single-day `**Food:**` branch) and Open Q3 (CI=0-files deploy reality).

Output: planParser.ts (GREEN against the Plan-01 fixtures), vite.config.ts `server.fs.allow`, README documentation of the build-and-push flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-RESEARCH.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-VALIDATION.md
@.planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md

<interfaces>
<!-- Defined in Plan 01 food.types.ts — import, do not redefine. -->
ParsedPlan = { windowStart: string /* YYYY-MM-DD */; windowEnd: string; meals: string[] /* normalized lowercase+trim */ }
parsePlanFile(filename: string, raw: string): ParsedPlan | null   // null on parse failure → caller filters
</interfaces>

<analogs>
planParser.ts        → src/domains/income/parser/parseStatement.ts (pure transform, returns structured result) + src/domains/income/classify.ts (pure, colocated test)
planParser.test.ts   → src/domains/income/classify.test.ts (no beforeEach, no Dexie — pure fn tests)
glob/frontmatter      → 04-RESEARCH.md Pattern 2 + "Frontmatter Extraction" + "Staleness Check" code examples (verbatim regex)
vite.config.ts fs     → 04-RESEARCH.md Pattern 1 "Vite configuration required" (server.fs.allow ['..'])
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parsePlanFile — frontmatter window + meal extraction (both formats)</name>
  <behavior>
    - Single-date file `YYYY-MM-DD.md`: windowStart === windowEnd === that date; `**Food:**` prose lines extract one normalized meal name each.
    - Date-range file `YYYY-MM-DD--YYYY-MM-DD.md`: window from frontmatter window_start/window_end; table-format Meal column rows extract.
    - Frontmatter window_start/window_end is authoritative when present; filename is the fallback when frontmatter absent.
    - Meal names are normalized to lowercase + trim so they join the meal-definition table (Pitfall 5).
    - All 14 known corpus strings appear (normalized) across the fixtures; 0 dropped slots.
    - Garbage / no-frontmatter / no-meals input → returns null (caller filters), never throws.
    - "Qdoba bowl" extracts as a single normalized meal token (non-decomposable; cost engine handles flat-cost in Plan 03 — parser just emits the name).
  </behavior>
  <read_first>
    - src/domains/income/parser/parseStatement.ts (pure-transform analog)
    - src/domains/income/classify.test.ts (test convention)
    - src/domains/food/planParser.test.ts (the RED scaffold from Plan 01 — make it GREEN)
    - src/domains/food/__fixtures__/ (the 5 live fixtures — read them to confirm exact frontmatter keys + body shape)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — Pattern 2 (parsing rules 1–5) + "Frontmatter Extraction" code example + Pitfall 3 (both filename formats) + Pitfall 5 (case normalization)
    - .planning/STATE.md — "SMC plan format — confirmed" block (frontmatter fields, `**Food:**` prose, table format, 14-meal corpus)
  </read_first>
  <action>
    Implement `src/domains/food/planParser.ts` exporting `parsePlanFile(filename, raw): ParsedPlan | null` (import `ParsedPlan` from `../food/food.types`). Pure — no Dexie, no storage, no React, no glob, no YAML parser. Use targeted regex per the RESEARCH "Frontmatter Extraction" example to read `window_start`/`window_end` (quoted or unquoted). Filename fallback: two regex branches — single-date `^(\d{4}-\d{2}-\d{2})\.md$` (windowStart=windowEnd=date) and date-range `^(\d{4}-\d{2}-\d{2})--(\d{4}-\d{2}-\d{2})\.md$`. Meal extraction with BOTH branches: table format (extract the Meal column from `| # | Time | Meal | Selector |` rows) and slot-prose format (`/^\*\*Food:\*\*\s*(.+)$/m`). Normalize every extracted meal name to lowercase + trim before pushing to `meals[]`. Return null if neither a window nor any meal can be determined. Turn the Plan-01 `planParser.test.ts` scaffold GREEN: assert all 14 corpus strings extract, window dates correct on both formats, null on garbage.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/domains/food/planParser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - V1 success signal: all 14 known meal strings extract across the fixtures; window dates correct; 0 dropped slots (test asserts the union of parsed meals ⊇ the 14-string corpus, normalized).
    - V2 partial: "chicken, rice, and broccoli" and "qdoba bowl" both appear as normalized meal-name tokens (tokenization into ingredients is the cost engine's job in Plan 03 — parser emits names only).
    - Single-date fixture: windowStart === windowEnd. Date-range fixture: windowStart < windowEnd from frontmatter.
    - `parsePlanFile('x.md', 'not a plan')` returns `null` (no throw).
    - planParser.ts imports `ParsedPlan` from food.types.ts and has zero Dexie/storage/react imports.
    - `npm run test -- --run src/domains/food/planParser.test.ts` exits 0.
  </acceptance_criteria>
  <done>The parser reliably turns live SMC plan files (both filename + both body formats) into normalized scheduled-meal names + windows; V1 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Vite glob serving config + README build-and-push / CI-fallback docs</name>
  <read_first>
    - vite.config.ts (the file being modified — read in full)
    - README.md (the file being modified)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — Pattern 1 "Vite configuration required", Pitfall 2 (dev server 403), Open Question 3 (CI=0-files), V8 in VALIDATION
  </read_first>
  <action>
    In `vite.config.ts` add `server: { fs: { allow: ['..'] } }` (merge with any existing `server` key — Phase 1 set `server.open: true`; preserve it) so the dev server can serve `../schedule-meal-coordinator/`. Do NOT add the `import.meta.glob` call here — that lives in food.atoms.ts (Plan 03). In `README.md` add a short "Meal plan data (SMC integration)" section documenting per RESEARCH Open Q3 + V8: (a) plan files are bundled at BUILD time via `import.meta.glob` from `../schedule-meal-coordinator/plans/`; (b) the deployed GitHub Pages app reflects plans as of the last local `npm run build && git push` — there is no live update; (c) the GitHub Actions runner has no SMC checkout, so the deployed app legitimately resolves 0 plan files and shows the fallback floor — this is expected v1 behavior, not a bug; (d) to refresh plan data Ian runs a local build + push.
  </action>
  <verify>
    <automated>cd projects/budget-app && npx tsc -b --noEmit && grep -c "schedule-meal-coordinator" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fs:" vite.config.ts` ≥ 1 and the config still contains the Phase-1 `open: true` (no regression).
    - `npx tsc -b --noEmit` exits 0 (config typechecks).
    - README contains a section mentioning `schedule-meal-coordinator`, "build", and the CI-fallback behavior (V8 documentation requirement).
    - `grep -c "schedule-meal-coordinator" README.md` ≥ 1.
  </acceptance_criteria>
  <done>Dev server can serve SMC files; the build-locally-then-push flow and CI=fallback reality are documented so the deployed-stale state is understood (V8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SMC plan file content → parser | Untrusted external text crosses into the parser; malformed input must not crash or silently zero the floor |
| SMC directory → app | Access must be strictly read-only (C1/SMC design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of service | parsePlanFile on malformed input | mitigate | Parser returns `null` on any unparseable file (never throws); caller filters nulls. Garbage-input test asserts null. A crashed parser must never collapse the floor to 0. |
| T-04-06 | Tampering / integrity | dropped meals → silent undercount | mitigate | V1 test asserts all 14 corpus meals extract with 0 dropped slots; both filename + both body formats covered (Pitfall 3). Missing meals would understate the floor (C1). |
| T-04-07 | Elevation / spoofing | write path to SMC | mitigate | No write/trigger method to `../schedule-meal-coordinator/` exists; access is `import.meta.glob` read-only by construction (FOOD-01). Parser is a pure read-only function. |
| T-04-08 | Information disclosure | bundling sibling repo into public artifact | accept | SMC plan files contain meal names only (no PII, no credentials per C2); bundling prose meal strings into the static build is acceptable. |
</threat_model>

<verification>
- `npm run test -- --run src/domains/food/planParser.test.ts` exits 0 (V1/V2 GREEN).
- `npx tsc -b --noEmit` exits 0.
- README documents the SMC build/CI-fallback flow (V8 doc half).
</verification>

<success_criteria>
- FOOD-03: both single-date and date-range plan filenames parse; both body formats handled.
- FOOD-01: SMC access is read-only; no write/trigger path.
- All 14 corpus meals extract from live fixtures with 0 drops (V1).
- Build-and-push / CI-fallback flow documented (V8).
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-02-SUMMARY.md` recording the parser's regex branches, the confirmed single-day `**Food:**` format finding (Open Q2 resolution), and the documented CI-fallback behavior.
</output>
