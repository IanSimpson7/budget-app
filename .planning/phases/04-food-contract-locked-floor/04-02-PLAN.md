---
phase: 04-food-contract-locked-floor
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/domains/food/planParser.ts
  - src/domains/food/planParser.test.ts
  - src/domains/food/__fixtures__/2026-05-18.md
  - src/domains/food/__fixtures__/2026-05-19.md
  - src/domains/food/__fixtures__/2026-05-21.md
  - src/domains/food/__fixtures__/2026-05-25--2026-05-28.md
  - src/domains/food/__fixtures__/2026-05-29--2026-05-31.md
autonomous: true
requirements: [FOOD-01, FOOD-03]
must_haves:
  truths:
    - "Parser extracts scheduled meal names plus plan window from both single-date and date-range SMC filenames"
    - "All 14 known meal strings are extracted across the 5 live fixtures with zero dropped slots"
    - "Both the table body format and the **Food:** prose body format are handled"
    - "Parser returns null on unparseable input (caller filters), never throws, never silently produces a wrong meal set"
    - "SMC files are read-only — the parser is a pure function with no write path to ../schedule-meal-coordinator/"
  artifacts:
    - path: "src/domains/food/planParser.ts"
      provides: "parsePlanFile(filename, raw): ParsedPlan | null + ParsedPlan interface"
      contains: "export function parsePlanFile"
    - path: "src/domains/food/__fixtures__/2026-05-25--2026-05-28.md"
      provides: "Hermetic snapshot of a live batch-format SMC plan"
      contains: "window_start"
  key_links:
    - from: "src/domains/food/planParser.ts"
      to: "src/domains/food/food.types.ts"
      via: "normalizeMealName import for meal-name normalization (Pitfall 5)"
      pattern: "normalizeMealName"
---

<objective>
Build the pure SMC plan parser: raw `.md` string plus filename produces `ParsedPlan { windowStart, windowEnd, meals[] }` or `null`. Copy 5 live SMC plan files into a hermetic `__fixtures__/` snapshot and prove (TDD) that the parser extracts all 14 known meal strings, both filename formats, and both body formats, falling back to filename-derived dates when frontmatter is absent.

Purpose: V1 (parser) and V2 (tokenizer) are the C1-critical front of the pipeline. A parser that drops a meal silently undercounts the floor. This is the first vertical-slice input — it produces the scheduled-meal data the cost engine consumes. Read-only by construction (FOOD-01): pure function, no filesystem write.

Output: A tested pure parser plus hermetic fixtures. After this plan, given any SMC plan file the app knows which meals are scheduled and whether the plan covers a given date.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-food-contract-locked-floor/04-RESEARCH.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-VALIDATION.md

<interfaces>
Contract this plan CREATES. Plan 03 (cost engine) and Plan 04 (atoms) consume it.

ParsedPlan (interface):
  windowStart: string   // YYYY-MM-DD (authoritative from frontmatter; fallback = filename)
  windowEnd: string     // YYYY-MM-DD
  meals: string[]       // normalized (normalizeMealName) meal-name strings, one per scheduled slot

parsePlanFile(filename: string, raw: string): ParsedPlan | null
  // null on parse failure; caller filters with (p): p is ParsedPlan => p !== null

SMC format facts (STATE.md "SMC plan format — confirmed" plus live fixtures):
  Frontmatter window_start and window_end as key: "value" or key: value (regex per key, NOT full YAML)
  Filenames YYYY-MM-DD.md (single) AND YYYY-MM-DD--YYYY-MM-DD.md (range)
  Body format A (batch): | # | Time | Meal | Selector | table rows, extract the Meal column
  Body format B (single-day): **Food:** prose meal string per slot, regex /^\*\*Food:\*\*\s*(.+)$/m
  14-meal corpus: see STATE.md line 95; assert ALL appear across the 5 fixtures
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy 5 live SMC plan files into hermetic __fixtures__ snapshot</name>
  <files>src/domains/food/__fixtures__/2026-05-18.md, src/domains/food/__fixtures__/2026-05-19.md, src/domains/food/__fixtures__/2026-05-21.md, src/domains/food/__fixtures__/2026-05-25--2026-05-28.md, src/domains/food/__fixtures__/2026-05-29--2026-05-31.md</files>
  <read_first>
    - ../schedule-meal-coordinator/plans/2026-05-18.md (single-day source; confirm **Food:** body format, Open Question 2 / RESEARCH A3)
    - ../schedule-meal-coordinator/plans/2026-05-25--2026-05-28.md (batch source; table body format)
    - ../schedule-meal-coordinator/docs/schemas/plan-v1.2.md (plan-file schema for frontmatter + slot structure)
    - 04-VALIDATION.md "Sampling / Coverage Notes" (copy as read-only snapshots; do NOT symlink — tests must be hermetic)
  </read_first>
  <action>Read each of the 5 live plan files under ../schedule-meal-coordinator/plans/ and write a verbatim copy into src/domains/food/__fixtures__/ with the same filename. These are hermetic test snapshots; the parser tests load these, never the live SMC directory. Confirm the single-day files (2026-05-18.md, 2026-05-19.md, 2026-05-21.md) use the **Food:** prose body format and the date-range files use the table body format; if a single-day file actually uses the table format, record it in the SUMMARY so the parser branch matches reality (resolves RESEARCH Open Question 2 / A3).</action>
  <verify>
    <automated>cd C:/Users/simps/Documents/AI/projects/budget-app; node -e "const fs=require('fs');const d='src/domains/food/__fixtures__';const f=fs.readdirSync(d);if(f.length<5)throw new Error('expected 5 fixtures got '+f.length);console.log('fixtures',f.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 fixture files exist under src/domains/food/__fixtures__/ with the exact source filenames.
    - Each fixture contains the original frontmatter (window_start / window_end present where the source had them).
    - SUMMARY records the confirmed single-day body format (Food prose vs table).
  </acceptance_criteria>
  <done>Five hermetic SMC plan fixtures are committed; the single-day body format is confirmed and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD parsePlanFile — both filename formats, both body formats, tokenization, null-on-failure (V1 + V2)</name>
  <files>src/domains/food/planParser.ts, src/domains/food/planParser.test.ts</files>
  <read_first>
    - src/domains/income/parser/parseStatement.ts (analog; pure transform function, returns structured data; 04-PATTERNS.md "planParser.ts" section)
    - src/domains/income/classify.test.ts (analog; pure-function colocated test, no Dexie/beforeEach)
    - src/domains/food/food.types.ts (Plan 01; import normalizeMealName for the join-normalization contract)
    - src/domains/food/__fixtures__/ (the 5 snapshots from Task 1; import via Vite ?raw or fs read in the test)
    - 04-RESEARCH.md Pattern 2 (parser rules), Code Examples "Frontmatter Extraction", Pitfall 3 (both filename formats)
    - 04-VALIDATION.md V1 (parser: all 14 meal strings, window dates, 0 dropped slots) and V2 (tokenizer: mixed separators, non-decomposable flag)
  </read_first>
  <behavior>
    - RED to GREEN to REFACTOR.
    - Test (V1, batch fixture): parsePlanFile on 2026-05-25--2026-05-28.md returns windowStart 2026-05-25, windowEnd 2026-05-28, and a meals[] containing the table Meal-column values normalized.
    - Test (V1, single-day fixture): parsePlanFile on 2026-05-21.md extracts the **Food:** prose meal strings, one per slot, normalized; windowStart === windowEnd === the file date.
    - Test (V1, corpus): across all 5 fixtures the union of extracted meals covers all 14 known meal strings from STATE.md line 95 (assert each is present in normalized form).
    - Test (filename fallback): a raw string with NO frontmatter but filename 2026-05-19.md yields windowStart === windowEnd === 2026-05-19.
    - Test (V2 tokenization contract — documented for the cost engine): "chicken, rice, and broccoli" tokenizes to [chicken, rice, broccoli] on the mixed separators (`,` / and / with); "qdoba bowl" is flagged non-decomposable. (Tokenization helper lives in planParser.ts and is unit-tested here; the cost engine in Plan 03 consumes the meal-definition ingredients[], not raw tokens — RESEARCH anti-pattern.)
    - Test (null path): malformed input (no frontmatter, no recognizable date in filename, no meal lines) returns null and does not throw.
  </behavior>
  <action>Write planParser.test.ts FIRST loading the 5 fixtures. Then implement planParser.ts: export the ParsedPlan interface; extractFrontmatter via a per-key regex for window_start/window_end (RESEARCH Code Example); a filename date branch handling both (^\d{4}-\d{2}-\d{2})\.md$ and (^\d{4}-\d{2}-\d{2})--(\d{4}-\d{2}-\d{2})\.md$ as the fallback when frontmatter is absent (Pitfall 3); meal extraction with TWO branches — table format (Meal column from | # | Time | Meal | Selector | rows) and slot-prose format (/^\*\*Food:\*\*\s*(.+)$/m); normalize every extracted meal name via normalizeMealName; and an exported tokenizeMealName helper splitting on mixed separators (`,`, " and ", " with ") for the V2 contract, returning a non-decomposable flag for whole-meal strings. Return null on total parse failure so the atoms layer filters it (fallback-high follows in Plan 03/04). Keep this a pure function: no Dexie, no React, no storage imports beyond the food.types normalization helper.</action>
  <verify>
    <automated>cd C:/Users/simps/Documents/AI/projects/budget-app; npx tsc -b; npm run test -- --run src/domains/food/planParser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --run src/domains/food/planParser.test.ts passes all behaviors (V1 + V2).
    - The corpus test asserts all 14 known meal strings appear across the fixtures (0 dropped).
    - The null-path test confirms no throw on malformed input.
    - planParser.ts has no imports from dexie, jotai, react, or storage (only ./food.types).
  </acceptance_criteria>
  <done>The parser handles both SMC filename formats and both body formats, extracts all 14 meals across the live fixtures, tokenizes per the V2 contract, and returns null on failure — proven by colocated unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SMC plan file to parser | Untrusted external text (machine-generated, not validated by this app) crosses into the floor pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | parsed meal set | mitigate | A malformed or partially-readable plan returns null rather than a partial/wrong meal list; null routes to fallback-high (Plan 03/04), never to a silent undercount. C1. |
| T-04-06 | Denial of Service | parser on adversarial input | mitigate | Pure regex extraction with no unbounded backtracking on the targeted patterns; parser cannot crash the app (null on failure). Fixtures are hermetic snapshots, not live reads, so test runs are deterministic. |
| T-04-07 | Tampering | SMC source files | mitigate | Parser is read-only; no write/trigger path to ../schedule-meal-coordinator/ exists anywhere in the food domain (FOOD-01, C1 one-way guarantee). |
</threat_model>

<verification>
- npx tsc -b clean.
- npm run test -- --run src/domains/food/planParser.test.ts green.
- All 14 meals extracted across fixtures; both filename and body formats covered (V1/V2).
- No write path to SMC anywhere in the food domain.
</verification>

<success_criteria>
- Pure parser returns ParsedPlan or null for both SMC filename formats and both body formats.
- 14-meal corpus fully extracted across the 5 hermetic fixtures.
- Tokenizer V2 contract proven; non-decomposable items flagged.
- Read-only: no SMC write path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-02-SUMMARY.md`
</output>
