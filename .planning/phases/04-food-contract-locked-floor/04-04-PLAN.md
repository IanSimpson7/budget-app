---
phase: 04-food-contract-locked-floor
plan: 04
type: execute
wave: 4
depends_on: [01, 03]
files_modified:
  - src/pages/FoodConfigPage.tsx
  - src/App.tsx
  - src/components/AppShell.tsx
  - src/test/FoodConfigPage.test.tsx
autonomous: true
requirements: [FOOD-04, FOOD-05, FOOD-08, FOOD-13, EDGE-03]
must_haves:
  truths:
    - "Ian can edit the unit-cost map, portion model, and meal-definition table in-UI; the floor recomputes immediately"
    - "Unpriced ingredient rows are visibly flagged (warning row treatment), never silently treated as $0"
    - "The config surface is framed as accuracy/convergence tooling ('Meal cost configuration'), never as a budget lever"
    - "lastRefinedFromReceipts timestamp is visible and can be set via 'Mark refined today' (records a date, not a dollar edit)"
  artifacts:
    - path: "src/pages/FoodConfigPage.tsx"
      provides: "Three editable tables (meal definitions, unit-cost map, portion model) + FOOD-13 timestamp"
      contains: "Meal cost configuration"
    - path: "src/App.tsx"
      provides: "/food/config route"
      contains: "/food/config"
    - path: "src/components/AppShell.tsx"
      provides: "Food nav link (config reached through /food, not nav)"
      contains: "/food"
  key_links:
    - from: "src/pages/FoodConfigPage.tsx"
      to: "save*Atom write atoms"
      via: "useSetAtom → storage save → liveQuery/refreshCounter recompute"
      pattern: "useSetAtom"
    - from: "src/App.tsx"
      to: "FoodConfigPage"
      via: "Route path=/food/config"
      pattern: "FoodConfigPage"
---

<objective>
Build the config surface (`/food/config`) — the accuracy/convergence tooling where Ian edits the three tables that feed the floor — plus the routing/nav wiring. Vertical slice: a real user can open the config page, add/edit a unit cost, and watch the floor recompute.

Purpose: FOOD-04/05 (editable tables) and FOOD-13 (refined-from-receipts timestamp) live here. Critically, this surface is the place C1's framing rule is most at risk — it must read as "Meal cost configuration", never "adjust food budget". Unpriced rows must be loudly flagged (EDGE-03). This plan is deliberately separated from the locked-floor panel (Plan 05) so the C1 read-only display and the editable inputs ship as distinct, independently-verifiable surfaces.

Output: FoodConfigPage.tsx (3 tables + timestamp), App.tsx route, AppShell Food nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-UI-SPEC.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md
@.planning/phases/04-food-contract-locked-floor/04-03-SUMMARY.md

<interfaces>
<!-- Write/read atoms from Plan 03 food.atoms.ts: -->
unitCostMapAtom / saveUnitCostMapAtom
portionModelAtom / savePortionModelAtom
mealDefinitionsAtom / saveMealDefinitionAtom / updateMealDefinitionAtom / deleteMealDefinitionAtom
foodFloorMetaAtom (+ saveFoodFloorMeta via storage for the timestamp)
<!-- Existing primitives: NumberInput, TextInput, SelectInput, PrimaryButton, SecondaryButton, DestructiveButton, Toast -->
</interfaces>

<analogs>
FoodConfigPage.tsx  → src/pages/FundsPage.tsx (EditRow inline-edit pattern, empty state, add/delete) + src/pages/ExpensesPage.tsx (currency formatter, table layout, tailwind tokens)
App.tsx route       → self (existing /expenses, /funds Route lines) — add /food/config
AppShell nav        → self (existing NavLink block) — add Food link
test                → src/test/FundsPage.test.tsx / src/test/ExpensesPage.test.tsx (RTL page tests)
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routes + Food nav link</name>
  <read_first>
    - src/App.tsx (the file being modified — read the full Route block)
    - src/components/AppShell.tsx (the file being modified — read the NavLink block)
    - .planning/phases/04-food-contract-locked-floor/04-UI-SPEC.md — Surface 1 routing decision (Food nav link → /food; config reached THROUGH the food panel, NOT a direct nav link)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "src/App.tsx (config)"
  </read_first>
  <action>
    In `src/App.tsx`: import `FoodConfigPage` from `./pages/FoodConfigPage` and add `<Route path="/food/config" element={<FoodConfigPage />} />` in the existing route block. (The `/food` route + FoodPage import is added in Plan 05 — if Plan 05 has not run yet, add only the `/food/config` route here; do not import a FoodPage that does not exist.) In `src/components/AppShell.tsx`: add a `Food` `NavLink` to `/food` after the `/funds` link, using the existing `navClasses` pattern. Do NOT add a direct `/food/config` nav link — per UI-SPEC, config is reached through the food panel only.
  </action>
  <verify>
    <automated>cd projects/budget-app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/food/config" src/App.tsx` returns 1.
    - `grep -c "to=\"/food\"" src/components/AppShell.tsx` returns 1 (Food nav link to /food).
    - No direct `/food/config` NavLink in AppShell.
    - `npx tsc -b --noEmit` exits 0 (no import of a not-yet-existing FoodPage if Plan 05 hasn't run).
  </acceptance_criteria>
  <done>The config route is registered and a Food nav entry exists; config is reachable through the food panel, not the nav.</done>
</task>

<task type="auto">
  <name>Task 2: FoodConfigPage — three editable tables + FOOD-13 timestamp</name>
  <read_first>
    - src/pages/FundsPage.tsx (EditRow inline-edit, add/delete, empty state — the structural analog)
    - src/pages/ExpensesPage.tsx (currency formatter, table tokens, NumberInput usage)
    - src/components/SelectInput.tsx, src/components/NumberInput.tsx, src/components/TextInput.tsx (primitive APIs)
    - .planning/phases/04-food-contract-locked-floor/04-UI-SPEC.md — Surface 2 (Tables A/B/C column specs, FOOD-13 block, Copywriting Contract incl NEVER-USE list, Interaction States, Accessibility)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "FoodConfigPage.tsx (component)" + "Shared Patterns" (Tailwind tokens, seed sentinel)
  </read_first>
  <action>
    Create `src/pages/FoodConfigPage.tsx` per UI-SPEC Surface 2. Heading: "Meal cost configuration" (`font-display text-[20px]`); subtext: "Edit these tables for accuracy. Changes recompute the protected floor immediately." Render THREE tables stacked with `gap-sp-8`, using the FundsPage inline-EditRow pattern, real `<table>` semantics (`<th scope>`), the ExpensesPage `currency` formatter, all values `font-mono`, all interactive elements `min-h-[44px]`, tokens from tailwind.config.ts (no inline hex):
    - Table A Meal definitions: columns Meal name | Type badge (Decomposed=success / Flat cost=secondary) | Cost/Ingredients (flat-cost → NumberInput; decomposed → ingredient name list) | Actions (Add meal definition / Delete). Stub/undefined rows render with `text-warning` name + "Needs ingredients" warning badge.
    - Table B Unit-cost map: columns Ingredient | Tag (SelectInput Macro-bearing / Flavor / condiment) | Unit | Cost/unit (NumberInput inline) | Actions. Unpriced rows (cost 0/null) get `bg-warning bg-opacity-10 border-l-2 border-warning` + a visible text flag (color is never the sole signal) — EDGE-03.
    - Table C Portion model: columns Ingredient | Portion (NumberInput) | Unit | Actions. Ingredients = union of macro-bearing ingredients; add happens via Table B (no independent add).
    - Below Table B: "Last refined from receipts: {date or 'Not yet recorded'}" + a SecondaryButton "Mark refined today" that writes `lastRefinedFromReceipts` (today ISO) via foodFloorMeta save (a timestamp, NOT a dollar edit) — FOOD-13.
    Wire all edits to the Plan-03 write atoms via `useSetAtom`; reads via `useAtomValue`. Use the UI-SPEC copywriting verbatim. Do NOT use any "cut/reduce/trim/save on food" framing (C1 NEVER-USE list). Empty states from the Copywriting Contract.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/test/FoodConfigPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - FoodConfigPage renders the heading text "Meal cost configuration" (C1 framing — verified by test query).
    - Test asserts NONE of "Cut", "Reduce", "Trim", "Save on food", "budget" (downward framing) appear in the rendered config copy (C1 NEVER-USE).
    - FOOD-04: adding an ingredient + cost in Table B calls `saveUnitCostMapAtom` (test asserts the write atom fires / row appears).
    - FOOD-05: editing a portion in Table C calls `savePortionModelAtom`.
    - EDGE-03: a unit-cost row with cost 0 renders the warning row treatment AND a visible text flag (not color alone) — test asserts the flag text/class.
    - FOOD-13: "Mark refined today" sets `lastRefinedFromReceipts`; the timestamp label updates (test asserts).
    - All interactive elements carry `min-h-[44px]`; financial values use `font-mono`.
    - `npm run test -- --run src/test/FoodConfigPage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Ian can converge the floor's accuracy through three editable tables framed as tooling (never a budget lever); unpriced rows are loud; the receipts timestamp is recordable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The /food/config surface: three editable tables (meal definitions, unit-cost map, portion model) with inline editing, unpriced-row warning treatment, and the "Last refined from receipts" timestamp with "Mark refined today". Edits recompute the floor live (visible on /food after Plan 05, or via the dashboard Survival-floor card now).</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:5173/budget-app/#/food/config` (port per Vite output).
    2. Confirm the heading reads "Meal cost configuration" and the page contains NO "cut/reduce/trim/save on food" language anywhere.
    3. In Table B, add an ingredient with a cost — confirm it persists after a hard refresh.
    4. Add an ingredient with cost 0 — confirm the row shows the amber warning treatment AND a readable text flag (not color only).
    5. Edit a portion in Table C — confirm it saves.
    6. Click "Mark refined today" — confirm the "Last refined from receipts" date updates and persists across refresh.
    7. Confirm every input/button is comfortably tappable on a phone-width viewport (44px targets).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (copy, framing, warning visibility, tap targets).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config inputs → IndexedDB | Numeric cost/portion edits cross into persistence; validated in Plan 01 storage guard |
| Config framing → user | C1-relevant: the surface must never read as a food-budget-cutting lever (BED safety) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Spoofing / social (C1 framing) | FoodConfigPage copy | mitigate | Heading "Meal cost configuration"; test asserts the NEVER-USE downward-framing terms are absent. The surface edits accuracy inputs, never sets the floor. |
| T-04-14 | Information disclosure (silent $0) | unpriced unit-cost row | mitigate | Cost-0 rows render warning treatment + a text flag (color is never the sole signal, a11y). The cost engine (Plan 03) treats 0 as a gap, never a valid zero. |
| T-04-15 | Tampering | numeric input persistence | mitigate | Writes go through Plan-01 storage `Number.isFinite` guard; NaN/Infinity rejected before persistence. |
</threat_model>

<verification>
- `npm run test -- --run src/test/FoodConfigPage.test.tsx` exits 0.
- `npx tsc -b --noEmit` exits 0.
- C1 framing test (no downward-framing copy) GREEN.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- FOOD-04/05: unit-cost map + portion model editable in-UI; floor recomputes.
- FOOD-08/EDGE-03: unpriced ingredient visibly flagged.
- FOOD-13: lastRefinedFromReceipts visible + settable.
- C1 framing: config is tooling, never a budget lever.
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-04-SUMMARY.md` recording the three-table layout, the FOOD-13 timestamp wiring, and the C1-framing copy decisions.
</output>
