---
phase: 04-food-contract-locked-floor
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/domains/food/food.atoms.ts
  - src/test/food.atoms.test.ts
  - src/domains/expenses/expenses.atoms.ts
  - src/test/expenses.atoms.test.ts
  - vite.config.ts
  - README.md
autonomous: true
requirements: [FOOD-01, FOOD-03, FOOD-06, FOOD-07, FOOD-09, FOOD-11, EDGE-02]
must_haves:
  truths:
    - "foodFloorAtom derives live over (meal definitions + parsed plans + unit-cost map + portion model + meta) and recomputes when ANY input changes"
    - "When a current plan covers today the live computed floor is shown; when stale or missing, max(last-computed, high-water) is shown with a stale flag"
    - "A change to a meal cost propagates through to survivalFloorAtom (foodFloorAtom replaces floors.foodSeed)"
    - "import.meta.glob resolves the SMC plan files in a local dev/build; CI (no SMC checkout) resolves 0 files yielding the fallback state, documented as expected v1 behavior"
    - "The food floor is never persisted as a live value — only lastComputedFloor and allTimeHighWater metadata are persisted"
  artifacts:
    - path: "src/domains/food/food.atoms.ts"
      provides: "parsedPlansAtom (glob loader), mealDefinitionsAtom, unit/portion/meta/flavor singleton atoms, foodFloorAtom, badge status, write atoms"
      contains: "export const foodFloorAtom"
    - path: "vite.config.ts"
      provides: "server.fs.allow for dev-server cross-dir glob serving"
      contains: "allow"
  key_links:
    - from: "src/domains/expenses/expenses.atoms.ts"
      to: "src/domains/food/food.atoms.ts"
      via: "survivalFloorAtom imports foodFloorAtom (one-way; never reverse — Pitfall 4)"
      pattern: "from '../food/food.atoms'"
    - from: "src/domains/food/food.atoms.ts"
      to: "../schedule-meal-coordinator/plans/*.md"
      via: "import.meta.glob raw eager:false (read-only, build-time)"
      pattern: "import.meta.glob"
    - from: "src/domains/food/food.atoms.ts"
      to: "src/storage/storage.ts"
      via: "observeMealDefinitions + food singleton get/save (never imports db directly)"
      pattern: "from '../../storage/storage'"
---

<objective>
Wire the food-domain reactive atom chain: the `import.meta.glob` plan loader, the liveQuery meal-definitions atom, the four settings-singleton atoms (with refresh counters plus write atoms), and the derived `foodFloorAtom` that calls the Plan 03 cost engine — choosing live vs `max(last, high-water)` fallback by D-08 staleness and persisting only metadata. Then replace `floors.foodSeed` with `foodFloorAtom` in `survivalFloorAtom`, configure Vite (`server.fs.allow`), and document the CI = 0-files build reality (V8).

Purpose: This is the integration spine — it joins parser plus engine plus storage into a single derived value and feeds it into the existing survival-floor math (V7). It also resolves the last architectural unknown (glob load / CI fallback) concretely.

Output: A live, reactive food floor wired into solvency math; Vite configured; the deploy reality documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-RESEARCH.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-VALIDATION.md
@.planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md
@.planning/phases/04-food-contract-locked-floor/04-02-SUMMARY.md
@.planning/phases/04-food-contract-locked-floor/04-03-SUMMARY.md

<interfaces>
Contract this plan CREATES. Plan 05 (UI) consumes foodFloorAtom plus the badge/gap atoms plus the write atoms.

FoodFloorResult:
  floor: number
  gaps: FloorGap[]            // from costEngine (Plan 03) plus a stale-plan gap when applicable
  isClean: boolean
  planIsCurrent: boolean

foodFloorAtom: atom yielding Promise<FoodFloorResult>     // derived, READ-ONLY, NEVER persisted
mealDefinitionsAtom: atomWithObservable<MealDefinition[]>   // liveQuery, initialValue:[], NO refreshCounter
unitCostMapAtom / portionModelAtom / foodFloorMetaAtom / flavorLineAtom : async atoms, each with its own refreshCounter
parsedPlansAtom: atom yielding Promise<ParsedPlan[]>      // glob loader, filters nulls
foodBadgeStatusAtom: atom yielding 'clean' | 'needs-attention'   // derived from foodFloorAtom.gaps
save/update/delete write atoms for meal definitions plus save atoms for the four singletons

CRITICAL PATH FIX (planner-verified via node path.relative): the glob path is FOUR levels up.
From src/domains/food/food.atoms.ts: ../../../../schedule-meal-coordinator/plans/*.md
(food to domains to src to budget-app to projects, then into schedule-meal-coordinator).
RESEARCH A1 said three (../../../) which is WRONG. The path depth is [ASSUMED] until verified at runtime.
HARD V8 CHECK (I-02): the executor MUST verify the glob returns >0 keys in a dev build via a dev-only
assertion/log — a wrong depth silently returns {} and is INDISTINGUISHABLE from the intended CI 0-files
fallback, so it must not pass undetected. If 0 in dev with SMC present, adjust the depth until >0.

DAYS-IN-MONTH CONTRACT (I-01): foodFloorAtom computes `today` and `daysInMonth` FRESH at read time
(new Date() inside the atom body), never a module-level memoized constant, so the monthly figure is
correct for whatever month the app is opened in. computeFloor (Plan 03) is pure and trusts the supplied value.

WRITE-BACK CONTRACT (I-03): saveFoodFloorMeta is the ONLY write path to foodFloorMeta in the whole app.
On a CLEAN (gap-free) computation it persists lastComputedFloor = floor and allTimeHighWater = max(prev, floor)
(Math.max — never a lower high-water). It does NOT write back on a gapped result (a fallback-padded floor
must not poison lastComputedFloor or the high-water mark). The live floor itself is never persisted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: food.atoms.ts — glob loader, source/singleton atoms, foodFloorAtom, badge status (V8, FOOD-11/EDGE-02)</name>
  <files>src/domains/food/food.atoms.ts, src/test/food.atoms.test.ts, vite.config.ts</files>
  <read_first>
    - src/domains/income/income.atoms.ts (analog, EXACT: atomWithObservable+liveQuery source atom lines 25-28, derived async atom lines 84-89, write-only atom lines 143-147, file header comment 1-12; 04-PATTERNS.md "food.atoms.ts" section)
    - src/domains/settings/settings.atoms.ts (analog; refreshCounter + plain-async-singleton pattern lines 20-27)
    - src/storage/storage.ts (Plan 01; observeMealDefinitions + the four food singleton get/save; import via the storage abstraction ONLY, never db)
    - src/domains/food/planParser.ts (Plan 02; parsePlanFile, ParsedPlan)
    - src/domains/food/costEngine.ts (Plan 03; computeFloor, isPlanCurrent, fallbackFloor, FALLBACK_CEILING_PER_MEAL)
    - vite.config.ts (file being modified; add server.fs.allow merging with the Phase-1 server.open:true; analog = RESEARCH Pattern 1 config block + STATE.md 01-01 vite base note)
    - 04-RESEARCH.md Pattern 1 (glob), Pattern 4 (atom chain), Code Examples "Glob Import Initialization" + "Fallback-High Logic (D-07)", Pitfalls 1/2/7, Open Question 3 (CI fallback)
    - 04-VALIDATION.md V8 (glob resolves >0 locally; CI=fallback documented)
  </read_first>
  <behavior>
    - Test: foodFloorAtom returns the live computeFloor result when a parsed plan covers today (mock parsedPlansAtom + storage getters; assert the floor matches engine output and planIsCurrent === true).
    - Test (FOOD-11/EDGE-02): when no plan covers today, foodFloorAtom returns floor === max(lastComputed, highWater) (from mocked foodFloorMeta) with a stale-plan gap and planIsCurrent === false.
    - Test (FOOD-07/09): changing the mocked meal or unit-cost input changes the derived floor (recompute proven without a stored stale copy).
    - Test (Pitfall 7): a live computation lower than high-water still shows the LIVE floor (not ratcheted) when planIsCurrent; high-water only governs the stale path.
    - Test (foodBadgeStatusAtom): gaps non-empty yields 'needs-attention'; empty yields 'clean'.
    - Test (I-03 write-back): a clean computeFloor result triggers saveFoodFloorMeta with lastComputedFloor === floor and allTimeHighWater === max(prevHighWater, floor); a result WITH gaps triggers NO write-back (lastComputedFloor and high-water unchanged); and a clean floor lower than the prior high-water still leaves allTimeHighWater at the prior (higher) value.
    - Test (I-01 month-boundary): with a mocked/injected "today" in a 30-day month vs a 31-day month, computeFloor receives the matching daysInMonth and foodFloorAtom's floor scales accordingly (proves daysInMonth is derived from the current date at read time, not memoized at write).
  </behavior>
  <action>Create food.atoms.ts following the income.atoms.ts structure: const RAW_PLAN_GLOB = import.meta.glob('../../../../schedule-meal-coordinator/plans/*.md', { as: 'raw', eager: false }) (FOUR ../ — planner-verified; see the interfaces note) cast to Record<string, () => Promise<string>>. HARD V8 CHECK (I-02): the glob path depth is [ASSUMED] in RESEARCH — a wrong depth silently returns {} and is indistinguishable from the intended CI 0-files fallback, so it cannot be allowed to pass undetected. Add a DEV-ONLY guard: when import.meta.env.DEV, after building RAW_PLAN_GLOB, read Object.keys(RAW_PLAN_GLOB).length and console.error (or throw a dev-only assertion) if it is 0 while the SMC repo is expected present locally; the message must instruct the executor to correct the ../ depth. This guard is stripped/no-op in production (where 0 files is the documented CI fallback). parsedPlansAtom async-loads then parses then filters nulls; mealDefinitionsAtom = atomWithObservable(() => storage.observeMealDefinitions(), { initialValue: [] }) (NO refreshCounter — liveQuery re-emits); unitCostMapAtom/portionModelAtom/foodFloorMetaAtom/flavorLineAtom each a plain async atom reading its own refreshCounterAtom; matching write atoms (saveUnitCostMapAtom, savePortionModelAtom, saveFlavorLineAtom, saveMealDefinitionAtom/updateMealDefinitionAtom/deleteMealDefinitionAtom) — write atoms for the liveQuery table do NOT bump a counter; singleton write atoms DO bump theirs. foodFloorAtom = atom(async (get) => ...): read all inputs, compute today (local YYYY-MM-DD, same convention as income) FRESH at read time (new Date() inside the atom body — NOT a module-level memoized constant), derive daysInMonth from that current date (new Date(year, month+1, 0).getDate()) and pass it into computeFloor so the monthly figure is correct for whatever month the app is opened in (I-01); filter plans covering today; if none then { floor: fallbackFloor(meta), gaps:[{type:'stale-plan', lastKnownDate}], isClean:false, planIsCurrent:false }; else call computeFloor. WRITE-BACK CONTRACT (I-03): persist ONLY on a clean (gap-free) computation — when result.isClean (gaps.length === 0). On a clean result, fire-and-forget saveFoodFloorMeta({ ...meta, lastComputedFloor: result.floor, allTimeHighWater: Math.max(meta.allTimeHighWater, result.floor) }). Do NOT write back when result has gaps. saveFoodFloorMeta is the ONLY write path to foodFloorMeta in the entire app, and because allTimeHighWater uses Math.max it can NEVER be written lower than its prior value. NEVER persist the live floor itself as an editable value (anti-pattern). Add foodBadgeStatusAtom deriving 'clean'/'needs-attention' from gaps. Import from storage only (never db, never liveQuery from dexie — grep gate). Add server: { fs: { allow: ['..'] } } to vite.config.ts (merge with existing server.open). Write food.atoms.test.ts mocking storage + parsedPlansAtom for the behaviors above.</action>
  <verify>
    <automated>cd C:/Users/simps/Documents/AI/projects/budget-app; npx tsc -b; npm run test -- --run src/test/food.atoms.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --run src/test/food.atoms.test.ts passes all behaviors.
    - food.atoms.ts source contains the glob pattern with FOUR ../ levels and { as: 'raw', eager: false }.
    - food.atoms.ts imports from ../../storage/storage and NOT from ../../storage/db and NOT liveQuery from dexie (grep gate: grep -c "storage/db" returns 0).
    - vite.config.ts contains server.fs.allow including '..' AND still contains the Phase-1 open: true (no regression).
    - foodFloorAtom never calls a setFoodFloor-style method; only saveFoodFloorMeta is used for persistence.
    - (I-02, HARD V8) A dev-time assertion or build/console log confirms Object.keys(RAW_PLAN_GLOB).length > 0 when the SMC repo is present locally (gated on import.meta.env.DEV). A wrong path depth — which silently returns {} and mimics the CI 0-files fallback — must trip this guard, not pass undetected. The executor MUST run a dev build and confirm >0 plan files load locally before marking this task done; if 0, adjust the ../ depth until >0.
    - (I-03) The high-water write-back is precise: on a clean (gap-free) computation, saveFoodFloorMeta persists lastComputedFloor = floor and allTimeHighWater = max(prev, floor). This is the ONLY write path to foodFloorMeta, it does not run on a gapped result, and it never writes a lower high-water (Math.max). Asserted in food.atoms.test.ts.
    - (I-01) foodFloorAtom computes daysInMonth from the CURRENT date at read/computation time (a fresh new Date() inside the atom), NOT a module-load-time memoized value. The plan documents that month-boundary recompute relies on app re-mount / read-time evaluation: a session left open across midnight on the last day of a month will not auto-recompute until the next read (re-mount or input change). This is acceptable for v1 because the app is re-opened daily; the limitation is stated here explicitly so it is a known gap, not a silent bug.
  </acceptance_criteria>
  <done>The food atom chain is live: glob loader, liveQuery + singleton atoms, and a derived foodFloorAtom that computes live or falls back to max(last, high-water), persisting only metadata. Vite serves the cross-dir glob in dev.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: survivalFloorAtom integration (V7) + CI-fallback documentation (V8)</name>
  <files>src/domains/expenses/expenses.atoms.ts, src/test/expenses.atoms.test.ts, README.md</files>
  <read_first>
    - src/domains/expenses/expenses.atoms.ts (file being modified; current survivalFloorAtom lines 54-59 using floors.foodSeed; 04-PATTERNS.md "expenses.atoms.ts" section with the exact before/after)
    - src/test/expenses.atoms.test.ts (file being modified; existing survivalFloorAtom tests to update for the foodFloorAtom term)
    - src/domains/food/food.atoms.ts (Task 1; foodFloorAtom whose { floor } replaces floors.foodSeed)
    - 04-RESEARCH.md Pattern 7 (survivalFloorAtom replacement), Pitfall 4 (import direction food->settings, expenses->food, never reverse), Open Question 3 (CI=0-files build reality)
    - 04-VALIDATION.md V7 (a meal-cost change propagates to survivalFloorAtom) and V8 (document CI fallback)
    - README.md (file being modified; add the "Food floor + SMC plan data" deploy-flow section)
  </read_first>
  <behavior>
    - Test (V7): survivalFloorAtom returns fixedExFood + accruals + foodFloorAtom.floor; changing the mocked foodFloorAtom floor changes survivalFloorAtom output (propagation proven).
    - Test (regression): existing survivalFloorAtom expectations updated so prior income/expense/funds tests stay green.
  </behavior>
  <action>In expenses.atoms.ts replace the floors.foodSeed term in survivalFloorAtom with the computed floor: import foodFloorAtom from ../food/food.atoms (one-way import direction per Pitfall 4 — expenses depends on food, never the reverse), read const { floor } = await get(foodFloorAtom), and return fixedExFood + accruals + floor. If floorsLoadAtom is now unused in this file remove the import; if other atoms still need it, keep it. Update expenses.atoms.test.ts so the survivalFloorAtom test mocks foodFloorAtom and asserts propagation (V7). In README.md add a short "Food floor and SMC plan data" section documenting the v1 deploy reality (RESEARCH Open Question 3 / V8): import.meta.glob bundles ../schedule-meal-coordinator/plans/*.md at build time from the local machine; the GitHub Actions runner does NOT have the SMC repo, so a CI deploy resolves 0 plan files and the deployed app shows the fallback floor (expected v1 behavior); to publish new plan data Ian runs npm run build then git push locally; live wiring is SMC-01, deferred to v2.</action>
  <verify>
    <automated>cd C:/Users/simps/Documents/AI/projects/budget-app; npx tsc -b; npm run test -- --run src/test/expenses.atoms.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --run src/test/expenses.atoms.test.ts passes including the V7 propagation test.
    - expenses.atoms.ts source imports foodFloorAtom from '../food/food.atoms' and survivalFloorAtom no longer references floors.foodSeed.
    - No circular import: food.atoms.ts does NOT import from expenses.atoms.ts (grep gate).
    - README.md contains a "Food floor" / SMC section stating the CI=fallback build reality and the local build-then-push flow (V8).
    - Full suite green: npm run test -- --run has no regressions in income/expenses/funds/dashboard.
  </acceptance_criteria>
  <done>survivalFloorAtom now uses the computed food floor (a meal-cost change propagates to solvency math, V7); the CI = 0-files build reality is documented so the deployed-stale state is understood, not a surprise (V8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled plan glob to floor atom | Build-time bundled plan strings cross into the live floor derivation |
| food floor to survival floor | The protected floor feeds solvency math; a wrong value mis-states solvency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | persisted floor | mitigate | foodFloorAtom persists ONLY lastComputedFloor + allTimeHighWater metadata via saveFoodFloorMeta, and only on a clean computation (I-03); the live floor is never written, so a stale lower value can never be read back as the floor. C1. |
| T-04-12 | Spoofing | empty glob result | mitigate | 0-files (CI deploy or missing SMC) deterministically routes to the fallback-high stale state with a visible flag (Plan 05 badge), never a $0 or absent floor; a dev-only guard (I-02) trips when a wrong path depth yields 0 locally so it cannot masquerade as the CI fallback; documented in README (V8). C1. |
| T-04-13 | Elevation of Privilege | circular atom import | mitigate | One-way import direction enforced (food to settings, expenses to food); grep gate confirms food.atoms.ts never imports expenses.atoms.ts (Pitfall 4) — prevents a cycle that could throw at runtime and blank the floor. |
</threat_model>

<verification>
- npx tsc -b clean.
- npm run test -- --run full suite green (no regressions).
- foodFloorAtom: live when current, max(last, high-water) when stale; only metadata persisted, clean-only write-back (I-03).
- survivalFloorAtom uses the computed floor (V7).
- Glob path is FOUR ../ levels with a dev-only >0 guard (I-02); daysInMonth derived at read time (I-01); vite fs.allow set; README documents CI fallback (V8).
</verification>

<success_criteria>
- food.atoms.ts derives a live reactive floor with the correct glob path and a dev-only hard V8 guard (I-02).
- Stale path returns max(last, high-water) with a flag (FOOD-11/EDGE-02).
- daysInMonth derived from the current date at read time, with the documented re-mount limitation (I-01).
- Clean-only high-water write-back via the sole foodFloorMeta write path, never lower (I-03).
- survivalFloorAtom integration proven by propagation test (V7).
- CI = 0-files build reality documented (V8).
- No circular imports; storage-only import boundary held.
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-04-SUMMARY.md`
</output>
</content>
