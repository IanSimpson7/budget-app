---
phase: 04-food-contract-locked-floor
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/domains/food/costEngine.ts
  - src/domains/food/costEngine.test.ts
  - src/domains/food/food.atoms.ts
  - src/domains/expenses/expenses.atoms.ts
files_locked_note: "Touches expenses.atoms.ts (survivalFloorAtom). Plan 04/05 must not also edit this file in the same wave — they are scheduled after."
autonomous: true
requirements: [FOOD-06, FOOD-07, FOOD-08, FOOD-09, FOOD-11, EDGE-02, EDGE-03]
must_haves:
  truths:
    - "Meal cost = Σ over macro-bearing ingredients (portion × unit cost); flavor-tagged ingredients are excluded; flat-cost meals use their flat cost"
    - "Every gap (unpriced ingredient, undefined meal, unset flat cost) falls back HIGH and emits a flag — never $0, never silent undercount"
    - "Monthly floor = daily-average cost × days-in-current-month (1-day and 7-day windows both yield a full month)"
    - "When no current plan covers today, the floor = max(last-computed, all-time high-water) and never drops below it"
    - "A change to any input (meal table, plan, unit-cost map, portion model) recomputes the floor live; the floor is NEVER persisted as a stale copy"
    - "survivalFloorAtom uses the computed foodFloorAtom value in place of floors.foodSeed"
  artifacts:
    - path: "src/domains/food/costEngine.ts"
      provides: "computeFloor(input) + isPlanCurrent(plans, today) — pure, no Dexie"
      exports: ["computeFloor", "isPlanCurrent"]
    - path: "src/domains/food/food.atoms.ts"
      provides: "glob loader + reactive atom chain → foodFloorAtom (derived, read-only)"
      exports: ["mealDefinitionsAtom", "parsedPlansAtom", "foodFloorAtom"]
    - path: "src/domains/expenses/expenses.atoms.ts"
      provides: "survivalFloorAtom term swap foodSeed → foodFloorAtom.floor"
      contains: "foodFloorAtom"
  key_links:
    - from: "src/domains/food/food.atoms.ts foodFloorAtom"
      to: "computeFloor + max(last, highWater) fallback"
      via: "derived atom over plan/meal/cost/portion inputs"
      pattern: "computeFloor"
    - from: "src/domains/expenses/expenses.atoms.ts survivalFloorAtom"
      to: "foodFloorAtom"
      via: "await get(foodFloorAtom) → floor term"
      pattern: "get\\(foodFloorAtom\\)"
    - from: "food.atoms.ts"
      to: "high-water persistence"
      via: "storage.saveFoodFloorMeta on live compute"
      pattern: "saveFoodFloorMeta"
---

<objective>
Build the cost engine (pure) and the reactive food-floor atom chain, then wire the computed floor into the survival-floor solvency math — the core C1-critical capability: a live, ingredient-keyed, fallback-high food floor that can only ever err high.

Purpose: This is where the protected floor becomes real. The cost engine encodes the three never-undercount guards (D-02/D-04/D-08) and the monthly derivation (D-06); the atom chain makes it recompute live (FOOD-07/09) without ever persisting a stale value (FOUND-06); and the survivalFloorAtom swap (Pattern 7) makes the dashboard's existing Survival-floor card reflect real food costs with no layout change. Turns RED V3/V4/V5/V7 scaffolds GREEN.

Output: costEngine.ts (GREEN), food.atoms.ts (glob + chain), expenses.atoms.ts (term swap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-RESEARCH.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-VALIDATION.md
@.planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md
@.planning/phases/04-food-contract-locked-floor/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 food.types.ts — import, do not redefine. -->
CostEngineInput  = { scheduledMeals: string[]; mealDefinitions: MealDefinition[]; unitCostMap: UnitCostEntry[]; portionModel: PortionEntry[]; daysInMonth: number }
CostEngineResult = { floor: number; gaps: FloorGap[]; isClean: boolean }
FoodFloorResult  = { floor: number; gaps: FloorGap[]; isClean: boolean; planIsCurrent: boolean }
FloorGap variants: unpriced-ingredient | undefined-meal | unset-flat-cost | stale-plan
parsePlanFile (from planParser.ts, Plan 02)

<!-- Chosen constant (resolves RESEARCH Open Q1 / Assumption A4): -->
FALLBACK_CEILING_PER_MEAL = 15.00   // module constant in costEngine.ts. Conservative full-meal ceiling.
                                    // Deterministic (NOT "most expensive defined meal" — that is non-deterministic at empty-table).
</interfaces>

<analogs>
costEngine.ts          → src/domains/funds/funds.atoms.ts (fundStatus/isOnTrack pure logic) — no-imports-except-types discipline
costEngine.test.ts     → src/domains/income/classify.test.ts (pure, table-driven)
food.atoms.ts chain    → src/domains/income/income.atoms.ts (atomWithObservable+liveQuery source atom; refreshCounter+plain-async singletons; async derived atom; write-only atoms)
glob loader            → 04-RESEARCH.md "Glob Import Initialization" + "Fallback-High Logic (D-07)" code examples (verbatim)
survivalFloorAtom swap → src/domains/expenses/expenses.atoms.ts lines ~54-59 (exact current def) + 04-RESEARCH.md Pattern 7
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: costEngine.ts — Σ cost, fallback-high gaps, monthly derivation, staleness</name>
  <behavior>
    - Decomposed meal: cost = Σ over macro-bearing ingredients of (portionSize × costPerUnit). Flavor-condiment-tagged ingredients are EXCLUDED (D-05, Pitfall 6) — never double-counted with the flavor line.
    - Flat-cost meal ("qdoba bowl"): cost = its flatCost; if flatCost unset → FALLBACK_CEILING_PER_MEAL + gap {type:'unset-flat-cost', mealName}.
    - Unpriced ingredient (costPerUnit 0/missing): that ingredient contributes FALLBACK_CEILING_PER_MEAL worth (or a per-ingredient conservative share) and emits gap {type:'unpriced-ingredient', ingredientName}; the meal is never undercounted, never $0.
    - Undefined meal (scheduled name with no row): priced at FALLBACK_CEILING_PER_MEAL + gap {type:'undefined-meal', mealName}.
    - Monthly: floor = (Σ scheduled-meal cost / number of distinct plan days) × daysInMonth. 1-day plan and 7-day plan both yield a ~full-month figure (D-06), NOT a literal sum of scheduled days.
    - isPlanCurrent(plans, today): true iff some plan window covers today (windowStart ≤ today ≤ windowEnd).
    - computeFloor never returns NaN/Infinity/negative; isClean === (gaps.length === 0).
  </behavior>
  <read_first>
    - src/domains/funds/funds.atoms.ts (pure-function discipline analog)
    - src/domains/food/costEngine.test.ts (the RED scaffold from Plan 01 — make GREEN)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — Pattern 3 (CostEngine I/O), "Staleness Check (D-08)" example, Pitfall 6 (flavor double-count), Pitfall 7 (high-water ratchet — keep high-water out of the LIVE path; live = raw computeFloor), Open Q1 (ceiling constant)
    - .planning/phases/04-food-contract-locked-floor/04-CONTEXT.md — D-02, D-04, D-05, D-06, D-08
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "costEngine.ts"
  </read_first>
  <action>
    Implement `src/domains/food/costEngine.ts` exporting `computeFloor(input: CostEngineInput): CostEngineResult` and `isPlanCurrent(plans: ParsedPlan[], today: string): boolean`. Import only types from `./food.types`. Declare `const FALLBACK_CEILING_PER_MEAL = 15.00` (document the Open-Q1 rationale in a comment). Cost logic: for each scheduled meal name, find its MealDefinition; if none → undefined-meal gap + ceiling; if `type === 'flat-cost'` → use flatCost or (unset → ceiling + unset-flat-cost gap); if `type === 'decomposed'` → Σ over its `ingredients` filtered to macro-bearing entries in unitCostMap (look up portionSize in portionModel, costPerUnit in unitCostMap; costPerUnit 0/absent → unpriced-ingredient gap + ceiling contribution). Skip flavor-condiment-tagged ingredients from the Σ. Monthly derivation per D-06: dailyAverage = totalScheduledCost / distinctPlanDays; floor = dailyAverage × daysInMonth. Dedupe gaps by (type, name). Make the Plan-01 costEngine.test.ts scaffold GREEN: all three gap triggers yield ceiling + flag never $0 (V3); 1-day and 7-day monthly derivations (V4); isPlanCurrent staleness boundary (feeds V5).
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/domains/food/costEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - V3: table-driven test — each of unpriced-ingredient, undefined-meal, unset-flat-cost yields a contribution ≥ FALLBACK_CEILING_PER_MEAL (never 0) AND a matching gap entry; `isClean === false`.
    - V6 (flavor): a flavor-condiment-tagged ingredient in a meal's ingredient list does NOT add to that meal's cost (Pitfall 6 test).
    - V4: a 1-day plan (single meal-day) × daysInMonth ≈ monthly figure; a 7-day plan averages then × daysInMonth — neither is a literal sum of scheduled days.
    - `isPlanCurrent([{windowStart:'2026-05-01',windowEnd:'2026-05-31',...}], '2026-05-15')` === true; `'2026-06-01'` === false.
    - computeFloor output is finite and ≥ 0 for all test inputs; `isClean === (gaps.length===0)`.
    - costEngine.ts imports only from `./food.types` (no Dexie/storage/react/jotai).
    - `npm run test -- --run src/domains/food/costEngine.test.ts` exits 0.
  </acceptance_criteria>
  <done>The cost engine computes an ingredient-keyed, flavor-excluded, fallback-high, monthly-extrapolated floor with a complete gap list; V3/V4/V6 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: food.atoms.ts — glob loader + reactive chain → foodFloorAtom + high-water persistence</name>
  <read_first>
    - src/domains/income/income.atoms.ts (atomWithObservable+liveQuery, refreshCounter singletons, async derived atom, write-only atoms — the canonical chain to copy)
    - src/domains/settings/settings.atoms.ts (refreshCounter + plain-async singleton pattern)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — Pattern 1 (glob), Pattern 4 (chain), "Fallback-High Logic (D-07)" verbatim example, Pitfall 1 (path depth `../../../`), Pitfall 4 (import direction food→settings only), Pitfall 7 (high-water governs ONLY the stale path)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "food.atoms.ts (store)" (every atom shape + import-direction note + file comment block)
    - .planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md (exact storage export names + foodFloorMeta default)
  </read_first>
  <action>
    Create `src/domains/food/food.atoms.ts` per PATTERNS "food.atoms.ts". Import `* as storage` from `../../storage/storage`, `floorsLoadAtom` from `../settings/settings.atoms` (food→settings only — never reverse, Pitfall 4), `parsePlanFile` from `./planParser`, `computeFloor`/`isPlanCurrent` from `./costEngine`, types from `./food.types`. Declare `RAW_PLAN_GLOB = import.meta.glob('../../../schedule-meal-coordinator/plans/*.md', { as: 'raw', eager: false })` (verify the `../../../` depth resolves from this file location — Pitfall 1). Atoms: `mealDefinitionsAtom` (atomWithObservable over `storage.observeMealDefinitions()`, `{initialValue:[]}`, NO refreshCounter); `unitCostMapAtom`/`portionModelAtom`/`foodFloorMetaAtom`/`flavorLineAtom` (each plain-async + own refreshCounter, reading the Plan-01 getters); paired write-only atoms (`saveUnitCostMapAtom`, `savePortionModelAtom`, `saveFlavorLineAtom`, `saveMealDefinitionAtom`/`updateMealDefinitionAtom`/`deleteMealDefinitionAtom`); `parsedPlansAtom` (async glob loader → `parsePlanFile` → filter nulls); `foodFloorAtom` (async derived `FoodFloorResult`): compute `today` (ISO local), filter current plans via `isPlanCurrent`; if none current → `{ floor: max(meta.lastComputedFloor, meta.allTimeHighWater), gaps:[{type:'stale-plan', lastKnownDate}], isClean:false, planIsCurrent:false }`; else call `computeFloor` over current-plan meals + daysInCurrentMonth, then fire-and-forget `storage.saveFoodFloorMeta` updating `lastComputedFloor` always and `allTimeHighWater = max(highWater, floor)` — but the LIVE returned floor is always the raw computeFloor output, never max'd with high-water (Pitfall 7). Include the file-level comment block from PATTERNS.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/domains/food/ && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - food.atoms.ts exports `mealDefinitionsAtom`, `unitCostMapAtom`, `portionModelAtom`, `foodFloorMetaAtom`, `flavorLineAtom`, `parsedPlansAtom`, `foodFloorAtom`, and the write-only save/update/delete atoms.
    - `grep -c "import.meta.glob" src/domains/food/food.atoms.ts` returns 1 with the `../../../schedule-meal-coordinator/plans/*.md` pattern.
    - `mealDefinitionsAtom` uses `atomWithObservable` with `initialValue: []` and NO refreshCounter (liveQuery handles re-emit).
    - V5 (stale path): `foodFloorAtom` stale branch returns `max(lastComputedFloor, allTimeHighWater)` with a `stale-plan` gap — verified by a test feeding a non-current plan set (a costEngine/atom test asserting displayed ≥ max(last, highWater)).
    - Import direction: food.atoms.ts imports settings.atoms (not the reverse); no `import { db }` and no direct `import { liveQuery } from 'dexie'`.
    - `npx tsc -b --noEmit` exits 0; `npm run test -- --run src/domains/food/` exits 0.
  </acceptance_criteria>
  <done>The food floor recomputes live over all inputs, never persists the live value, and falls back to max(last, high-water) when stale — V5 GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: survivalFloorAtom term swap (foodSeed → computed floor)</name>
  <read_first>
    - src/domains/expenses/expenses.atoms.ts (the file being modified — read survivalFloorAtom and its imports in full)
    - src/test/expenses.atoms.test.ts (extend — the survival-floor test)
    - .planning/phases/04-food-contract-locked-floor/04-RESEARCH.md — Pattern 7 (exact before/after) + Pitfall 4 (import direction)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "expenses.atoms.ts (store)"
  </read_first>
  <action>
    In `src/domains/expenses/expenses.atoms.ts`: import `foodFloorAtom` from `../food/food.atoms`. Change `survivalFloorAtom` to `const { floor } = await get(foodFloorAtom)` and return `fixedExFood + accruals + floor` in place of `floors.foodSeed`. If `floorsLoadAtom` was imported ONLY to provide `foodSeed` to survivalFloorAtom, remove that now-dead import; if other atoms in the file still use it, keep it. Add/extend a test in `src/test/expenses.atoms.test.ts`: changing a meal cost (via unit-cost map or a flat cost) propagates a new value through `survivalFloorAtom` (V7).
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/test/expenses.atoms.test.ts && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - V7: a test changes a food-cost input and asserts `survivalFloorAtom` recomputes to include the new computed floor (not the static foodSeed).
    - `grep -c "get(foodFloorAtom)" src/domains/expenses/expenses.atoms.ts` returns 1; `survivalFloorAtom` no longer references `floors.foodSeed`.
    - No circular-import error at runtime or compile (import direction expenses→food preserved, Pitfall 4).
    - `npx tsc -b --noEmit` exits 0; `npm run test -- --run src/test/expenses.atoms.test.ts` exits 0.
  </acceptance_criteria>
  <done>Solvency math now uses the live computed food floor; the dashboard Survival-floor card reflects real ingredient costs with no layout change (V7).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cost-engine inputs → floor value | The single most C1-critical computation; any path that yields $0 or an undercount is the highest-severity failure (BED trigger) |
| Live compute → high-water persistence | High-water must never ratchet the live floor; mis-application freezes a stale high value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information disclosure (silent undercount) | computeFloor gap handling | mitigate | All three gaps (unpriced ingredient / undefined meal / unset flat cost) contribute ≥ FALLBACK_CEILING_PER_MEAL and emit a flag; V3 table-driven test proves never-$0. This is the core C1 structural guard. |
| T-04-10 | Tampering | flavor double-count | mitigate | Cost engine filters to macro-bearing ingredients before Σ; flavor-tagged excluded (Pitfall 6 test). Prevents both over- and under-statement drift vs receipts. |
| T-04-11 | Information disclosure | floor drops on a gap/stale plan | mitigate | Stale path returns `max(lastComputed, highWater)` + stale flag; live path is raw computeFloor but high-water persisted so the next stale read never drops (D-07). V5 property-style test: displayed ≥ max(last, highWater). |
| T-04-12 | Tampering | persisting the derived floor | mitigate | `foodFloorAtom` is never persisted; only `lastComputedFloor`/`allTimeHighWater` metadata is saved (FOUND-06). A stale stored floor could be read back low — architecturally prevented. |
</threat_model>

<verification>
- `npm run test -- --run src/domains/food/ src/test/expenses.atoms.test.ts` exits 0 (V3/V4/V5/V6/V7 GREEN).
- `npx tsc -b --noEmit` exits 0.
- No circular import between expenses.atoms and food.atoms.
</verification>

<success_criteria>
- FOOD-06/07/09: ingredient-keyed meal cost, live recompute, new priced meals auto-price.
- FOOD-08/EDGE-03: unpriced ingredient → fallback-high + flag.
- FOOD-11/EDGE-02: stale/missing plan → max(last, high-water) + staleness flag, never lower.
- Survival floor uses the computed floor (V7).
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-03-SUMMARY.md` recording the chosen FALLBACK_CEILING_PER_MEAL value, the foodFloorAtom stale/live branch logic, the high-water-only-on-stale rule (Pitfall 7), and confirmation the glob path depth resolves.
</output>
