---
phase: 04-food-contract-locked-floor
plan: 05
type: execute
wave: 5
depends_on: [01, 03, 04]
files_modified:
  - src/pages/FoodPage.tsx
  - src/pages/FoodPage.test.tsx
  - src/App.tsx
autonomous: false
requirements: [FOOD-10, FOOD-12, UI-02, EDGE-02]
must_haves:
  truths:
    - "The protected food floor renders as a locked, read-only value with a Lock icon — no input, no edit affordance, no downward path anywhere on it"
    - "The flavor line ($50/mo, PROTECTED) shows separately, editable upward only, excluded from per-meal pricing"
    - "The gateable discretionary food layer renders side-by-side with the locked floor (presentation join from Phase 3 expense lines)"
    - "A single status badge expands an itemized list naming each unpriced ingredient, undefined meal, and the staleness reason — every uncertainty is individually identifiable, never silent"
    - "When no plan covers today, the floor shows the fallback value + a staleness flag (never lower)"
  artifacts:
    - path: "src/pages/FoodPage.tsx"
      provides: "Locked floor card + flavor line + gateable layer + status badge with expandable gap list"
      contains: "Computed from your meal plan — protected"
    - path: "src/App.tsx"
      provides: "/food route"
      contains: "/food"
  key_links:
    - from: "src/pages/FoodPage.tsx"
      to: "foodFloorAtom"
      via: "useAtomValue → read-only span (no input)"
      pattern: "foodFloorAtom"
    - from: "src/pages/FoodPage.tsx gateable layer"
      to: "gateableExpensesAtom (food + gateable)"
      via: "presentation join, summed beside floor"
      pattern: "gateable"
    - from: "src/pages/FoodPage.tsx status badge"
      to: "foodFloorAtom.gaps"
      via: "badge + expandable detail list (D-11)"
      pattern: "gaps"
---

<objective>
Build the `/food` panel — the locked-floor surface that is the visible heart of C1: the protected floor rendered read-only with a lock, the flavor line, the side-by-side gateable layer, and the one-badge/expandable-list uncertainty surface. This is the final vertical slice and the phase's C1 acceptance gate.

Purpose: UI-02 (locked floor + gateable side-by-side), FOOD-12 (locked, rent-like, no downward affordance), FOOD-10 (flavor line), EDGE-02 (staleness flag) all land here. The derived-value architecture from Plan 03 IS the C1 enforcement — this surface must add NO affordance that could undo it. The 6 C1 visual conditions from UI-SPEC are independently verified. Turns RED V6 (UI half) and the FoodPage scaffold GREEN.

Output: FoodPage.tsx (locked floor + flavor + gateable + badge), App.tsx /food route. Ships with a human-verify checkpoint (autonomous: false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-food-contract-locked-floor/04-UI-SPEC.md
@.planning/phases/04-food-contract-locked-floor/04-PATTERNS.md
@.planning/phases/04-food-contract-locked-floor/04-01-SUMMARY.md
@.planning/phases/04-food-contract-locked-floor/04-03-SUMMARY.md
@.planning/phases/04-food-contract-locked-floor/04-04-SUMMARY.md

<interfaces>
<!-- From Plan 03 food.atoms.ts: -->
foodFloorAtom: FoodFloorResult = { floor; gaps: FloorGap[]; isClean; planIsCurrent }
flavorLineAtom / saveFlavorLineAtom   (FlavorLine = { amount })
<!-- From Phase 3 expenses.atoms.ts: -->
gateableExpensesAtom   (filter to food category for the discretionary-food layer — D-10 presentation join)
<!-- lucide-react Lock icon; existing primitives NumberInput/SecondaryButton; Intl currency formatter -->
</interfaces>

<analogs>
FoodPage.tsx        → src/pages/ExpensesPage.tsx (page scaffold, currency, two-column grid, tokens) + src/components/MetricCard.tsx (read-only value display = span, not input)
status badge        → no existing component — build inline per UI-SPEC "Status Badge" (success/warning tokens, aria-expanded, role=list)
gateable join       → src/domains/expenses/expenses.atoms.ts gateableExpensesAtom (filter to food)
test                → src/pages/FoodPage.test.tsx (RED scaffold from Plan 01) + src/test/ExpensesPage.test.tsx pattern
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FoodPage — locked floor card, flavor line, gateable layer, status badge</name>
  <read_first>
    - src/pages/ExpensesPage.tsx (page scaffold, currency formatter, grid, tokens)
    - src/components/MetricCard.tsx (read-only value-as-span pattern)
    - src/domains/expenses/expenses.atoms.ts (gateableExpensesAtom — confirm the food-category filter for D-10)
    - src/pages/FoodPage.test.tsx (the RED scaffold from Plan 01 — make GREEN)
    - .planning/phases/04-food-contract-locked-floor/04-UI-SPEC.md — Surface 1 (sections top-to-bottom), "C1 Visual Contract — Non-Negotiable" (6 conditions), Status Badge spec, Copywriting Contract (incl NEVER-USE), Interaction States, Accessibility
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "FoodPage.tsx (component)" (read-only span, lock icon, currency, badge, two-column layout)
    - .planning/phases/04-food-contract-locked-floor/04-CONTEXT.md — D-09 (read-only derived), D-10 (gateable join), D-11 (badge + list)
  </read_first>
  <action>
    Create `src/pages/FoodPage.tsx` per UI-SPEC Surface 1. Heading "Food · {Month Year}". Sections:
    (1) Protected floor card: a `Lock` icon (lucide-react, 16px, `aria-hidden`, ALWAYS rendered when the floor renders) + "Protected floor" label + the `foodFloorAtom.floor` value as a `font-mono text-[20px]` `<span>` (NEVER an `<input>`, no adjacent pencil/edit) + explainer "Computed from your meal plan — protected" (`text-xs text-text-secondary`, never hidden) + the status badge inline. First-load placeholder `—` in `text-text-disabled`.
    (2) Flavor line (in/beside the protected card): "Flavor & condiments — protected" + `font-mono` amount + a NumberInput editing the monthly amount with UPWARD framing only (helper "Excluded from per-meal pricing. Covers syrups, sauces, and condiments.") wired to `saveFlavorLineAtom` — FOOD-10.
    (3) Gateable food layer: "Discretionary food (gateable)" + summed amount from `gateableExpensesAtom` filtered to food category (D-10 presentation join, read-only) + subtext "From your expense lines — edit in Expenses" linking to `/expenses`. Lay floor + gateable side-by-side via `grid grid-cols-1 sm:grid-cols-2 gap-sp-4` (UI-02).
    (4) Status badge (D-11): success token "Plan current" when `isClean`; warning token "Needs attention" when gaps exist. The badge is a `<button>` with `aria-expanded`/`aria-controls` and `min-h-[44px]` that expands a `role="list"` panel rendering each gap as its own row using the UI-SPEC copy: unpriced-ingredient → "Ingredient '{name}' has no unit cost — add it in Meal config"; undefined-meal → "Meal '{name}' has no definition — add ingredients in Meal config"; unset-flat-cost → "Meal '{name}' needs a cost — add it in Meal config"; stale-plan → "No meal plan covers today — showing fallback floor (last known: {date})".
    (5) Empty/stale states + a "Meal cost configuration" link to `/food/config`. Use UI-SPEC copy verbatim; use ZERO "cut/reduce/trim/save on food" framing (C1 NEVER-USE). All tokens from tailwind.config.ts, financials `font-mono`, interactive `min-h-[44px]`, accessibility per UI-SPEC. Make the Plan-01 FoodPage.test.tsx scaffold GREEN.
  </action>
  <verify>
    <automated>cd projects/budget-app && npm run test -- --run src/pages/FoodPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - FOOD-12/C1-cond-1: the rendered floor value is NOT an `<input>` and has no adjacent edit/pencil control — test queries the floor value and asserts it is a non-interactive element (span), and that no `<input>` exists in the protected-floor card region.
    - C1-cond-2: a Lock icon renders whenever the floor value renders (test asserts the icon present).
    - C1-cond-3: explainer "Computed from your meal plan — protected" is present and not behind a toggle.
    - C1-cond-4: test asserts NONE of "cut", "reduce", "trim", "save on food" (case-insensitive) appear in rendered copy.
    - FOOD-10/C1-cond-6: flavor line labeled "Flavor & condiments — protected" with an upward-only amount editor (no reduce/gate framing).
    - UI-02: discretionary gateable food total renders side-by-side with the locked floor (grid two-column on sm+).
    - EDGE-02: with a stale `foodFloorResult` (no current plan), the amber "Needs attention" badge renders and the expandable list contains the staleness row.
    - EDGE-03: with an unpriced-ingredient gap, the badge expands to a row naming the ingredient.
    - Badge toggle has `aria-expanded` and `min-h-[44px]`; detail list has `role="list"`.
    - `npm run test -- --run src/pages/FoodPage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The /food panel renders the floor as a structurally locked read-only value with the flavor line, the side-by-side gateable layer, and a one-badge/expandable uncertainty list — all 6 C1 visual conditions met (V6 UI half).</done>
</task>

<task type="auto">
  <name>Task 2: register /food route + FoodPage import</name>
  <read_first>
    - src/App.tsx (the file being modified — read the Route block; Plan 04 already added /food/config)
    - .planning/phases/04-food-contract-locked-floor/04-PATTERNS.md — section "src/App.tsx (config)"
  </read_first>
  <action>
    In `src/App.tsx`: import `FoodPage` from `./pages/FoodPage` and add `<Route path="/food" element={<FoodPage />} />` in the existing route block, beside the `/food/config` route added by Plan 04. (The AppShell Food nav link was added in Plan 04.)
  </action>
  <verify>
    <automated>cd projects/budget-app && npx tsc -b --noEmit && npm run test -- --run src/test/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "path=\"/food\"" src/App.tsx` returns 1 (the bare /food route; /food/config is a separate match).
    - `grep -c "FoodPage" src/App.tsx` ≥ 1 (import + element).
    - `npx tsc -b --noEmit` exits 0; `npm run test -- --run src/test/App.test.tsx` exits 0.
  </acceptance_criteria>
  <done>/food is reachable from the AppShell nav and renders the FoodPage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The /food panel: the protected food floor as a locked read-only value (Lock icon, "Computed from your meal plan — protected"), the flavor line ($50/mo, protected, upward-only edit), the discretionary gateable food layer side-by-side, and a status badge that expands to an itemized list of every unpriced ingredient / undefined meal / staleness reason. Editing a unit cost in /food/config recomputes this floor live, and the dashboard Survival-floor card now reflects the computed floor.</what-built>
  <how-to-verify>
    1. `npm run dev`; open `http://localhost:5173/budget-app/#/food`.
    2. Confirm the protected floor shows a dollar value with a Lock icon and the explainer "Computed from your meal plan — protected". Confirm there is NO input/pencil/edit control on the floor value itself, and NO "cut/reduce/trim/save on food" language anywhere on the page.
    3. Confirm the flavor line reads "Flavor & condiments — protected" and only allows raising the monthly amount.
    4. Confirm the discretionary (gateable) food total renders beside the locked floor.
    5. Tap the status badge — confirm it expands and lists each gap (unpriced ingredient / undefined meal / staleness) by name; collapse it again.
    6. Open `/food/config`, change a unit cost, return to `/food` — confirm the floor recomputed (and the dashboard Survival-floor card updated).
    7. Confirm the floor never displays below the last-known/fallback value when no current plan covers today (staleness badge present).
    8. Confirm phone-width readability (no horizontal scroll on the floor/flavor/gateable values).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (C1 lock visuals, framing copy, badge/list behavior, live recompute, staleness).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Floor value → UI | The C1-critical render: must be structurally read-only with no downward affordance |
| Gap state → user | Every uncertainty must be individually surfaced, never silent (BED safety = trust the displayed floor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering (C1 breach) | floor value render | mitigate | Floor is a `<span>`, never `<input>`; no edit/pencil/cut affordance; Lock icon always present. Test asserts no `<input>` in the floor card + absence of downward-framing copy (6 C1 conditions). |
| T-04-17 | Information disclosure (silent gap) | status badge / gap list | mitigate | Single badge + expandable `role=list` names every unpriced ingredient, undefined meal, and staleness reason (D-11). Color is never the sole signal (a11y). A hidden gap could mask an undercounted floor. |
| T-04-18 | Information disclosure | stale floor shown low | mitigate | EDGE-02: stale state shows `max(last, high-water)` (from Plan 03) + an explicit staleness row; the UI cannot render below the fallback. |
| T-04-19 | Spoofing / social (C1 framing) | page copy | mitigate | Flavor line and gateable layer use protected/neutral framing; NEVER-USE downward terms asserted absent by test. |
</threat_model>

<verification>
- `npm run test -- --run src/pages/FoodPage.test.tsx src/test/App.test.tsx` exits 0.
- `npx tsc -b --noEmit` exits 0.
- All 6 C1 visual conditions verified by test.
- Human-verify checkpoint approved.
- Full suite green: `npm run test -- --run` before /gsd-verify-work.
</verification>

<success_criteria>
- FOOD-12/UI-02: locked floor + gateable layer side-by-side; no downward-edit affordance.
- FOOD-10: flavor line protected, separate, upward-only.
- EDGE-02: staleness flagged; floor never below fallback.
- D-11: one badge expands to a per-gap itemized list.
</success_criteria>

<output>
After completion, create `.planning/phases/04-food-contract-locked-floor/04-05-SUMMARY.md` recording the FoodPage section layout, the 6 C1-condition test assertions, the badge/gap-list wiring, and confirmation the live recompute (config → floor → dashboard) works end-to-end.
</output>
