---
phase: 01-foundation-storage-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - tailwind.config.ts
  - postcss.config.cjs
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/test/setup.ts
  - src/test/storage.test.ts
  - src/test/settings.atoms.test.ts
  - src/test/App.test.tsx
  - src/test/BackupPage.test.tsx
  - .gitignore
  - .nvmrc
autonomous: true
requirements: [FOUND-01, DEP-03]
must_haves:
  truths:
    - "Vite + React + TypeScript project compiles with no type errors"
    - "Tailwind v3 custom tokens (bg-accent, bg-surface, text-primary, font-display) emit utility classes in built CSS"
    - "Vitest runs in jsdom with fake-indexeddb available; structuredClone polyfilled"
    - "All Wave 0 test stub files exist and either pass or fail with explicit TODO assertions (never silently absent)"
    - "Root /#/settings and /#/backup routes mount empty placeholder pages without crashing"
  artifacts:
    - path: "package.json"
      provides: "Locked dependency versions; tailwindcss pinned to ^3.4 (NOT ^4)"
      contains: '"tailwindcss": "^3.4'
    - path: "vite.config.ts"
      provides: "base path /budget-app/, vitest jsdom test block, setupFiles → src/test/setup.ts"
      contains: "base: '/budget-app/'"
    - path: "tailwind.config.ts"
      provides: "Custom tokens from UI-SPEC §Tailwind Config Snippet verbatim"
      contains: "surface"
    - path: "src/test/setup.ts"
      provides: "fake-indexeddb/auto + structuredClone polyfill + jest-dom"
      contains: "structuredClone"
    - path: "src/test/storage.test.ts"
      provides: "FOUND-02/03/04 test stubs"
      min_lines: 20
    - path: "src/test/settings.atoms.test.ts"
      provides: "FOUND-05/06 test stubs"
      min_lines: 15
  key_links:
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "test.setupFiles"
      pattern: "setupFiles.*test/setup"
    - from: "src/test/setup.ts"
      to: "globalThis.structuredClone"
      via: "polyfill assignment"
      pattern: "structuredClone"
    - from: "tailwind.config.ts"
      to: "src/index.css"
      via: "@tailwind directives"
      pattern: "@tailwind"
---

<objective>
Scaffold the Vite + React + TypeScript project with pinned Tailwind v3.4.x, configure strict TypeScript per D-17, set up Vitest with jsdom + fake-indexeddb + structuredClone polyfill, and create all Wave 0 test stub files referenced by 01-VALIDATION.md.

Purpose: Establish the deterministic foundation. Every subsequent task depends on this being correct. Tailwind v4 would silently break the UI-SPEC config; React 19 + atomWithObservable would break Settings; missing structuredClone polyfill would break every Dexie test. Lock all three pitfalls in this plan.

Output: A buildable, testable, render-empty project. No Dexie, no atoms, no UI — pure scaffold + test infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-storage-deploy/01-CONTEXT.md
@.planning/phases/01-foundation-storage-deploy/01-RESEARCH.md
@.planning/phases/01-foundation-storage-deploy/01-UI-SPEC.md
@.planning/phases/01-foundation-storage-deploy/01-VALIDATION.md
@.planning/phases/01-foundation-storage-deploy/SKELETON.md
@../../roles/FinancialAdviser/specs/budgeting_app_spec_v1.md
@../../roles/FinancialAdviser/specs/budget_app_build_CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Vite + React + TS with strict config and pinned Tailwind v3</name>
  <files>package.json, tsconfig.json, tsconfig.node.json, vite.config.ts, tailwind.config.ts, postcss.config.cjs, index.html, src/main.tsx, src/App.tsx, src/index.css, src/vite-env.d.ts, .gitignore, .nvmrc</files>
  <read_first>
    - .planning/phases/01-foundation-storage-deploy/01-CONTEXT.md (D-15..D-21)
    - .planning/phases/01-foundation-storage-deploy/01-RESEARCH.md (§Standard Stack, §Pattern 6, §Pitfall 2)
    - .planning/phases/01-foundation-storage-deploy/01-UI-SPEC.md (§Tailwind Config Snippet — verbatim source)
    - .planning/phases/01-foundation-storage-deploy/SKELETON.md (§Directory Layout, §Framework, §Styling)
  </read_first>
  <action>
    Run `npm create vite@latest budget-app -- --template react-ts` was already declined — Ian is operating inside the existing `budget-app/` repo. Scaffold by hand instead. Install exact dependencies via `npm install`:

    Runtime deps (D-01, D-04, D-15, UI-SPEC icon library):
    - react@^19.2.6
    - react-dom@^19.2.6
    - jotai@^2.20.0
    - dexie@^4.4.2
    - react-router-dom@^7.0.0
    - lucide-react@^0.577.0

    Dev deps:
    - vite@^8.0.12
    - @vitejs/plugin-react@^6.0.1
    - typescript@~5.6.0
    - @types/react@^19.0.0
    - @types/react-dom@^19.0.0
    - vitest@^4.1.6
    - @testing-library/react@^16.0.0
    - @testing-library/jest-dom@^29.1.1
    - @testing-library/user-event@^14.0.0
    - jsdom@^26.0.0
    - fake-indexeddb@^6.0.0
    - tailwindcss@^3.4.17  (PIN v3 — DO NOT install v4; v4 silently ignores tailwind.config.ts per RESEARCH.md Pitfall 2)
    - postcss@^8.4.0
    - autoprefixer@^10.4.0

    Add npm scripts: `"dev": "vite"`, `"build": "tsc -b && vite build"`, `"preview": "vite preview"`, `"test": "vitest"`, `"typecheck": "tsc -b --noEmit"`.

    `tsconfig.json` MUST set (per D-17):
    - `"strict": true`
    - `"noUncheckedIndexedAccess": true`
    - `"exactOptionalPropertyTypes": true`
    - `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "bundler"`
    - `"jsx": "react-jsx"`, `"esModuleInterop": true`, `"skipLibCheck": true`
    - `"types": ["vite/client", "vitest/globals", "@testing-library/jest-dom"]`

    `vite.config.ts` MUST set:
    - `base: '/budget-app/'` (hardcoded — RESEARCH.md Open Question 3)
    - Plugins: `[react()]`
    - Test block: `{ environment: 'jsdom', globals: true, setupFiles: './src/test/setup.ts', css: true }`
    - Add `/// <reference types="vitest" />` top of file

    `tailwind.config.ts` MUST be the UI-SPEC §Tailwind Config Snippet VERBATIM (custom `surface`, `surface-raised`, `surface-border`, `text-primary`/`secondary`/`disabled`, `accent`/`accent-hover`, `destructive`/`destructive-hover`, `success`, `warning` colors; `font-display`/`mono`/`sans` families; sp-1..sp-16 spacing; borderRadius sm/DEFAULT 2px, md 4px). Content paths: `['./index.html', './src/**/*.{ts,tsx}']`.

    `postcss.config.cjs` exports `{ plugins: { tailwindcss: {}, autoprefixer: {} } }`.

    `src/index.css` contains exactly `@tailwind base; @tailwind components; @tailwind utilities;` plus a `body { @apply bg-surface text-text-primary font-sans; }` rule and `html, body, #root { @apply min-h-screen; }`.

    `index.html` MUST include in `<head>`:
    - `<link rel="preconnect" href="https://fonts.googleapis.com">`
    - `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
    - `<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=IBM+Plex+Mono:wght@400&family=IBM+Plex+Sans:wght@400;600&display=swap" rel="stylesheet">`
    - `<title>Budget</title>`
    - `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`

    `src/main.tsx` mounts `<App />` inside `<React.StrictMode>` and imports `./index.css`. No JotaiProvider needed (Jotai uses module-level store by default).

    `src/App.tsx` exports a minimal component returning a `<div className="p-sp-4 bg-surface text-text-primary min-h-screen"><h1 className="font-display text-[28px]">Budget</h1><p className="font-sans text-sm">Phase 1 scaffold OK</p></div>`. NO routing yet (routing wired in plan 02).

    `.gitignore` MUST include: `node_modules/`, `dist/`, `.DS_Store`, `coverage/`, `*.local`, `.env*` (defense for C2/DEP-03 — no env files committed).

    `.nvmrc` contains `20` (matches GitHub Actions setup-node version used in plan 03).

    Do NOT create env files (DEP-03 / C2: zero VITE_ variables with credential content; the only env reference used downstream is `VITE_APP_VERSION` injected via `define` in vite.config.ts — add `define: { 'import.meta.env.VITE_APP_VERSION': JSON.stringify(process.env.npm_package_version) }`).
  </action>
  <verify>
    <automated>npm install &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"tailwindcss": "^3.4` literally (NOT `^4`)
    - `npm run build` exits 0
    - `dist/index.html` contains substring `/budget-app/assets/` (base path applied)
    - `dist/assets/` contains at least one `.css` file whose contents include the literal hex `#c0522a` (accent color emitted) AND `#161210` (surface color emitted) — confirms Tailwind v3 read tailwind.config.ts
    - `tsconfig.json` contains `"strict": true`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true`
    - `npm run typecheck` exits 0
    - No file in `src/` contains `localStorage` or `sessionStorage` or `fetch(` calls (DEP-03 / C2 defense — only Dexie/IndexedDB used; no runtime credentials or external services)
    - `.env`, `.env.local` do NOT exist in the repo tree
  </acceptance_criteria>
  <done>
    Project scaffolds with React 19 + Vite 8 + Tailwind v3 + strict TS. `npm run build` produces a `dist/` referencing `/budget-app/` paths. App renders a single "Budget — Phase 1 scaffold OK" page with accent + surface colors applied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wave 0 test infrastructure — setup, polyfills, and stub files</name>
  <files>src/test/setup.ts, src/test/storage.test.ts, src/test/settings.atoms.test.ts, src/test/App.test.tsx, src/test/BackupPage.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-storage-deploy/01-VALIDATION.md (§Wave 0 Requirements, §Per-Task Verification Map)
    - .planning/phases/01-foundation-storage-deploy/01-RESEARCH.md (§Pattern 5, §Pitfall 4)
    - vite.config.ts (just authored — for test config reference)
    - src/App.tsx (for App.test.tsx import)
  </read_first>
  <action>
    Create the test setup file and four test stub files. Stubs MUST contain real assertions that fail with explicit `expect.fail('TODO: implemented in plan 02')` rather than being empty — silent passing tests violate Nyquist sampling per 01-VALIDATION.md.

    `src/test/setup.ts`:
    - Import `'fake-indexeddb/auto'` (FIRST import — installs IDB globals before any module-level Dexie code)
    - Import `'@testing-library/jest-dom/vitest'`
    - Import `{ cleanup }` from `'@testing-library/react'` and `{ afterEach }` from `'vitest'`; register `afterEach(() => cleanup())`
    - Define `structuredClone` polyfill BEFORE the fake-indexeddb import has a chance to use it — use top-of-file guard:
      `if (typeof globalThis.structuredClone === 'undefined') { globalThis.structuredClone = (obj) => JSON.parse(JSON.stringify(obj)) as any }`
      Place this BEFORE the `import 'fake-indexeddb/auto'` line (use a separate statement at top, then the import). Required because fake-indexeddb v5+ calls structuredClone and jsdom does not implement it (RESEARCH.md Pitfall 4).

    `src/test/storage.test.ts` — stubs covering FOUND-02/03/04. Create `describe` blocks:
    - `describe('storage abstraction (FOUND-02)')` with three `it.todo` entries: `'getFloors returns defaults when settings table is empty'`, `'saveFloors writes to settings table keyed by floors'`, `'storage interface exposes no credential methods'`
    - `describe('export envelope (FOUND-03)')` with two `it.todo`: `'exportAll returns envelope with schemaVersion 1 and exportedAt ISO string'`, `'envelope.data contains settings.floors'`
    - `describe('import path (FOUND-04)')` with three `it.todo`: `'importAll with schemaVersion === current replaces state'`, `'importAll with schemaVersion > current throws VERSION_TOO_NEW'`, `'importAll with invalid JSON throws PARSE_ERROR'`
    Plus ONE non-todo placeholder test that imports nothing and asserts `expect(1 + 1).toBe(2)` to ensure the file is picked up by Vitest discovery.

    `src/test/settings.atoms.test.ts` — stubs covering FOUND-05/06. Same pattern:
    - `describe('floors atom (FOUND-05)')`: `it.todo('floorsLoadAtom loads from Dexie, not hardcoded defaults when row exists')`, `it.todo('floorsLoadAtom returns DEFAULT_FLOORS when settings row absent')`
    - `describe('derived recompute (FOUND-06)')`: `it.todo('derived atom recomputes when input atom value changes')`
    Plus the 1+1 placeholder.

    `src/test/App.test.tsx` — FOUND-01 smoke. Import `{ render, screen }` from `@testing-library/react` and `App` from `'../App'`. Single test: `it('renders the Budget app shell', () => { render(<App />); expect(screen.getByText(/Budget/i)).toBeInTheDocument() })`. This test MUST pass with the scaffold from Task 1 — proves the test pipeline runs RTL against the real component.

    `src/test/BackupPage.test.tsx` — UI-05 stub. `it.todo('Export backup button triggers download')`, `it.todo('Import backup button opens file picker')`, `it.todo('Import with schemaVersion > current shows version-too-new toast')`. Plus 1+1 placeholder.

    Use `it.todo(name)` (built into Vitest) — these surface as "todo" in test output without failing or being silent. This satisfies the Nyquist rule: every Wave 0 requirement has a named, discoverable test reference even before implementation.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` exits 0
    - Test output shows at least 4 passing tests (the 1+1 placeholders in storage/settings.atoms/BackupPage + the App smoke test)
    - Test output shows at least 11 "todo" entries (3+2+3+2+1+3 across the four spec files)
    - `src/test/setup.ts` contains the literal substring `structuredClone` AND the literal substring `'fake-indexeddb/auto'`
    - `src/test/setup.ts` has the structuredClone polyfill statement placed BEFORE the `import 'fake-indexeddb/auto'` line (grep: line number of structuredClone assignment is less than line number of fake-indexeddb import)
    - `src/test/App.test.tsx` passes (proves jsdom + RTL + jest-dom + tailwind-emitted markup all wired correctly)
  </acceptance_criteria>
  <done>
    Test infrastructure is wired. The structuredClone + fake-indexeddb pitfall is locked out. Every Wave 0 requirement from 01-VALIDATION.md has a corresponding `it.todo` placeholder waiting for plan 02 to fill in. `npm run test -- --run` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → runtime | npm dependencies pulled in at install; supply-chain risk if a typosquatted package is installed |
| Source code → bundle | No `.env*` content can leak into the client bundle (DEP-03 / C2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependency tree | medium | mitigate | Use package-lock.json (committed); pin Tailwind to ^3.4 explicitly to defeat silent v4 install; all listed packages verified in RESEARCH.md against npmjs.com registry (>500 weekly downloads each per Ian's global rule) |
| T-01-02 | Information disclosure | Build env vars | high | mitigate | `.gitignore` excludes `.env*`; no `VITE_*` variable in source references credentials; only `VITE_APP_VERSION` injected from package.json (non-sensitive); acceptance criterion greps source for `localStorage`/`fetch(` to enforce no runtime external services |
| T-01-03 | Tampering | Tailwind v4 silently installed instead of v3 | high | mitigate | package.json pins `^3.4`; acceptance criterion greps for `"tailwindcss": "^3.4`; built CSS verified to contain custom hex tokens (`#c0522a`, `#161210`) confirming v3 read the config file |
| T-01-04 | Denial of service | Test suite hangs from missing structuredClone | medium | mitigate | structuredClone polyfill placed BEFORE fake-indexeddb import in src/test/setup.ts; acceptance criterion verifies polyfill literal present and line-ordered before the IDB import |
| T-01-05 | Tampering | TypeScript strictness bypassed | medium | mitigate | tsconfig.json sets strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes (D-17); `npm run typecheck` is a Task 1 verify command |
</threat_model>

<verification>
- `npm install` exits 0 with no peer-dep errors blocking install
- `npm run build` exits 0
- `npm run test -- --run` exits 0
- `npm run typecheck` exits 0
- `dist/index.html` references `/budget-app/assets/`
- Built CSS contains `#c0522a` (accent) AND `#161210` (surface) — proves Tailwind v3 config was read
- No `.env*` files committed
- Wave 0 test files exist and emit at least 11 todo entries
</verification>

<success_criteria>
- Buildable scaffold with React 19 + Vite 8 + Tailwind v3.4 + strict TypeScript
- Test runner green; structuredClone polyfill locked in; fake-indexeddb available
- Every Wave 0 requirement has a discoverable `it.todo` reference
- Zero `localStorage`/`fetch(`/`.env*` in committed source (C2 / DEP-03 structural defense)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-storage-deploy/01-01-SUMMARY.md` covering: dependencies installed with exact versions, key config decisions (Tailwind v3 pin reason, vite base, tsconfig strictness), Wave 0 stub count, what plan 02 can now assume exists.
</output>
